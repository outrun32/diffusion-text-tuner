---
phase: 02-runtime-contracts-and-run-provenance
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - src/runtime/manifests.py
  - src/runtime/reproducibility.py
  - scripts/run_manifest.py
  - tests/test_runtime_manifests.py
autonomous: true
requirements: [RUN-01, RUN-03, RUN-04, CFG-03, STR-02]
must_haves:
  truths:
    - "User can create a local run directory with a manifest before launching a long-running stage."
    - "The manifest captures command, timestamp, git state, resolved config snapshot, environment summary, seeds, model IDs/revisions, inputs, outputs, metrics, notes, and artifact schema versions."
    - "User can inspect or resume a stage using the manifest and canonical output conventions."
  artifacts:
    - path: "src/runtime/manifests.py"
      provides: "Run directory creation, manifest schema, config snapshot writing, metrics/notes updates, inspect/resume helpers"
      exports: ["RunManifest", "create_run_manifest", "load_run_manifest", "update_run_manifest"]
    - path: "src/runtime/reproducibility.py"
      provides: "Git, environment, package, CUDA/cache, seed, and model revision summary helpers"
      exports: ["collect_git_state", "collect_environment_summary", "collect_model_revisions"]
    - path: "scripts/run_manifest.py"
      provides: "CLI for init, inspect, note, and metrics updates without running GPU workloads"
    - path: "tests/test_runtime_manifests.py"
      provides: "CPU-safe manifest schema and CLI tests"
  key_links:
    - from: "src/runtime/manifests.py"
      to: "src/runtime/config_io.py"
      via: "resolved config snapshot embedded in manifest"
      pattern: "resolve_config_snapshot"
    - from: "src/runtime/manifests.py"
      to: "src/runtime/artifacts.py"
      via: "artifact schema versions recorded in manifest"
      pattern: "schema_version"
    - from: "scripts/run_manifest.py"
      to: "runs/<run_id>/manifest.json"
      via: "CLI writes local ignored run directories"
      pattern: "create_run_manifest"
---

<objective>
Create the local run provenance layer and command surface.

Purpose: Researchers need durable provenance before comparison-grade generation, scoring, training, synthesis, and evaluation outputs are produced.
Output: Manifest schema/helpers, reproducibility collectors, CLI, and CPU-safe tests.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/01-execution-surface-and-pipeline-inventory/01-04-SUMMARY.md
@src/runtime/config_io.py
@src/runtime/paths.py
@src/runtime/artifacts.py
@docs/runtime_contracts.md
@.gitignore

<interfaces>
Contracts produced by earlier Phase 2 plans and consumed here:
```python
load_stage_config(stage: str, path: str | Path) -> SFTConfig | DPOConfig | MaskedSFTConfig
resolve_config_snapshot(config) -> dict[str, object]
resolve_stage_paths(stage: str, root: Path = Path.cwd()) -> RuntimePaths
validate_artifacts(stage: str, paths: RuntimePaths) -> ArtifactReport
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify run manifest schema and CLI behavior</name>
  <files>tests/test_runtime_manifests.py</files>
  <behavior>
    - `create_run_manifest(stage="sft", config_path=..., command=[...])` creates `runs/<timestamp>-sft-<slug>/manifest.json` and `config_snapshot.json`.
    - Manifest JSON includes `schema_version`, `run_id`, `stage`, UTC timestamp, command, git commit/dirty/untracked summary, Python/platform/package summary, config snapshot path, seeds, model IDs/revisions, inputs, outputs, metrics, notes, and artifact schema versions.
    - `scripts/run_manifest.py init ...`, `inspect ...`, `note ...`, and `metrics ...` operate on temp run directories without CUDA/model/OCR imports.
    - Existing manifests can be loaded for resume/inspection and update operations preserve prior fields immutably.
  </behavior>
  <action>Create CPU-safe tests that use temp dirs, monkeypatch subprocess/package collection where needed, and assert secret-safe environment summaries. The RED state should fail because manifest helpers and CLI do not exist. Include a manifest inspection test that prints run ID, stage, command, config snapshot path, outputs, metrics keys, and notes count.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_runtime_manifests.py -x</automated>
  </verify>
  <done>Tests fail only because manifest/reproducibility helpers and CLI are missing, with explicit assertions for RUN-01/RUN-03/RUN-04/CFG-03 fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement run manifests and reproducibility collectors</name>
  <files>src/runtime/manifests.py, src/runtime/reproducibility.py, tests/test_runtime_manifests.py</files>
  <behavior>
    - Manifest creation writes deterministic JSON with sorted keys and config snapshots copied immutably into the run directory.
    - Reproducibility collection records git state, Python/platform, selected installed package versions, CUDA/cache environment presence without secret values, configured seeds, and model IDs/revisions when present in config.
    - Manifest updates append notes and merge metrics without deleting existing provenance.
    - Generated outputs are recorded as paths and schema metadata, not committed artifacts.
  </behavior>
  <action>Create `src/runtime/reproducibility.py` using standard library subprocess/importlib metadata helpers. Create `src/runtime/manifests.py` with dataclasses or Pydantic models for manifest structure, run ID generation, atomic JSON writing, config snapshot writing via Plan 02-01 helpers, artifact schema linkage via Plan 02-02 constants, load/inspect/update functions, and resume metadata helpers. Treat Hugging Face tokens, env secrets, and private paths as presence/status fields only.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_runtime_manifests.py</automated>
  </verify>
  <done>Manifest helper tests pass and created manifests contain every RUN-01 field with config snapshots under ignored `runs/` temp roots.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add manifest CLI for init, inspect, notes, and metrics</name>
  <files>scripts/run_manifest.py, tests/test_runtime_manifests.py</files>
  <behavior>
    - `python -m scripts.run_manifest init --stage sft --config configs/sft.json --command "accelerate ..." --run-root runs` prints the run directory and writes manifest files.
    - `inspect` prints a concise human-readable summary and exits nonzero for missing/corrupt manifests.
    - `note` appends timestamped notes without overwriting existing notes.
    - `metrics` merges a JSON metrics payload or metrics file into the manifest.
  </behavior>
  <action>Create an argparse CLI in `scripts/run_manifest.py` backed by `src.runtime.manifests`. Keep it import-safe and CPU-only. Tests should invoke `main(argv)` directly and assert stdout/stderr behavior. Do not launch pipeline stages from this CLI; it manages provenance files that other commands can reference.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_runtime_manifests.py && PATH="/root/.local/bin:$PATH" uv run pytest</automated>
  </verify>
  <done>Users can initialize, inspect, annotate, and update local manifests from the command line before or after long-running stages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local environment/git -> manifest | Environment and git metadata are serialized into manifest JSON. |
| CLI args -> filesystem | User-provided run roots and manifest paths determine write targets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | I | `src/runtime/reproducibility.py` | mitigate | Record secret/token presence only; never serialize environment variable values matching token/key/password patterns. |
| T-02-08 | T | `scripts/run_manifest.py` path args | mitigate | Resolve run roots through path policy and refuse writes outside allowed project/runtime roots. |
| T-02-09 | R | manifest updates | mitigate | Append timestamped notes and preserve command/git/config fields so run history remains auditable. |
</threat_model>

<verification>
Run task-level commands and inspect one temp manifest during tests to ensure generated run directories live under ignored `runs/` semantics.
</verification>

<success_criteria>
Run provenance can be created and inspected locally with complete, secret-safe metadata and immutable config snapshots before expensive pipeline execution.
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-contracts-and-run-provenance/02-03-SUMMARY.md`
</output>
