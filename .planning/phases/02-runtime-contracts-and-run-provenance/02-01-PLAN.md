---
phase: 02-runtime-contracts-and-run-provenance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/runtime/__init__.py
  - src/runtime/config_io.py
  - tests/test_runtime_config_io.py
autonomous: true
requirements: [CFG-01, CFG-04, STR-02]
must_haves:
  truths:
    - "User can validate SFT, DPO, and masked-SFT JSON configs before trainer startup."
    - "User sees field-level errors for missing fields, invalid values, unknown keys, model ID mismatches, and unsafe path settings."
    - "Shared runtime config helpers exist outside trainer modules."
  artifacts:
    - path: "src/runtime/config_io.py"
      provides: "Pydantic-backed config loading, validation, resolved snapshots, and path policy helpers"
      exports: ["RuntimeConfigError", "load_stage_config", "resolve_config_snapshot", "validate_path_policy"]
    - path: "tests/test_runtime_config_io.py"
      provides: "CPU-safe tests for config validation and path policy failures"
    - path: "pyproject.toml"
      provides: "Pydantic v2 runtime dependency for config validation"
      contains: "pydantic"
  key_links:
    - from: "tests/test_runtime_config_io.py"
      to: "src/runtime/config_io.py"
      via: "imports and validates stage configs"
      pattern: "load_stage_config"
    - from: "src/runtime/config_io.py"
      to: "src/training/config.py"
      via: "returns existing dataclass config objects after validation"
      pattern: "SFTConfig|DPOConfig|MaskedSFTConfig"
---

<objective>
Create the shared validated config-loading contract for Phase 2.

Purpose: Phase 2 depends on catching config and path mistakes before long-running CUDA/model work starts, while preserving existing SFT, DPO, and masked-SFT dataclass consumers.
Output: Pydantic-backed runtime config helpers, dependency lock updates, and CPU-safe validation tests.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@src/training/config.py
@src/training/sft_trainer.py
@src/training/dpo_trainer.py
@src/training/masked_sft_trainer.py

<interfaces>
Existing config dataclasses to preserve as trainer-facing return types:
```python
class SFTConfig: model_id: str; latents_dir: str; text_embeds_dir: str; scores_csv: str; output_dir: str; seed: int; lora: LoraConfig
class DPOConfig: model_id: str; sft_lora_path: str | None; latents_dir: str; text_embeds_dir: str; scores_csv: str; output_dir: str; seed: int; lora: LoraConfig
class MaskedSFTConfig: model_id: str; data_dir: str; eval_suite_path: str | None; output_dir: str; seed: int; lora: MultiRankLoraConfig
```

Current trainer loaders read JSON directly and instantiate dataclasses; this plan creates shared helpers but does not modify trainers yet:
```python
def load_config(path: str) -> SFTConfig: ... return SFTConfig(lora=lora, **data)
def load_config(path: str) -> DPOConfig: ... return DPOConfig(lora=lora, **data)
def load_config(path: str) -> MaskedSFTConfig: ... return MaskedSFTConfig(lora=lora, **data)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify config validation behavior before implementation</name>
  <files>tests/test_runtime_config_io.py</files>
  <behavior>
    - Valid SFT, DPO, and masked-SFT JSON files load through one `load_stage_config(stage, path)` helper and return existing dataclass config objects.
    - Unknown fields, malformed JSON, negative/zero step counts, invalid mixed precision, invalid score thresholds, and model ID mismatches produce `RuntimeConfigError` with the config path and field name.
    - Absolute paths outside the repository, `..` traversal, and home-directory paths in committed configs fail path-policy validation unless explicitly marked as environment inputs.
    - `resolve_config_snapshot(config)` returns a JSON-serializable immutable dictionary including `schema_version`, `stage`, and nested LoRA fields without mutating the source dataclass.
  </behavior>
  <action>Create CPU-safe tests that construct temporary JSON configs for `sft`, `dpo`, and `masked_sft`. The RED state should fail because `src.runtime.config_io` does not exist. Use tiny temp paths only; do not require CUDA, model packages, real generated artifacts, or trainer execution. Include path-policy cases for relative paths under `configs/`, `data/`, `outputs/`, and `runs/` as allowed, and `/home/user/private`, `~/private`, and `../outside` as rejected per CFG-04 and STR-02.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_runtime_config_io.py -x</automated>
  </verify>
  <done>Tests fail only because runtime config helpers are missing, and test names cover all CFG-01/CFG-04 behaviors listed above.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared validated config loading</name>
  <files>pyproject.toml, uv.lock, src/runtime/__init__.py, src/runtime/config_io.py, tests/test_runtime_config_io.py</files>
  <behavior>
    - `load_stage_config("sft", path)` validates JSON with Pydantic v2, rejects unknown fields, validates numeric ranges, instantiates `SFTConfig`, and preserves nested `LoraConfig`.
    - `load_stage_config("dpo", path)` and `load_stage_config("masked_sft", path)` do the same for `DPOConfig` and `MaskedSFTConfig` / `MultiRankLoraConfig`.
    - Errors never expose secret environment variable values and always name the config path and failing field.
    - Snapshots are sorted JSON-serializable dictionaries with `schema_version` and `stage` metadata.
  </behavior>
  <action>Add `pydantic>=2,<3` to `pyproject.toml` dependencies and refresh `uv.lock`. Create `src/runtime/config_io.py` with Pydantic models mirroring the existing dataclass fields in `src/training/config.py`, strict extra-field rejection, per-stage model ID consistency rules, path policy validation, dataclass conversion helpers, and snapshot serialization. Export the public helpers from `src/runtime/__init__.py`. Do not move configs or modify trainer entry points in this plan; Plan 02-04 performs wiring after artifacts/manifests exist.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv lock --check && PATH="/root/.local/bin:$PATH" uv run pytest tests/test_runtime_config_io.py && PATH="/root/.local/bin:$PATH" uv run pytest</automated>
  </verify>
  <done>All config tests and the CPU-safe suite pass; `load_stage_config` returns existing dataclass types and rejects invalid configs before any heavy import or trainer execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local JSON config -> runtime loader | User-edited JSON crosses into trainer configuration. |
| config paths -> filesystem | Config values determine input/output paths used by later GPU jobs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `src/runtime/config_io.py` path policy | mitigate | Reject traversal, home expansion, and absolute paths outside the repository unless explicitly marked as environment inputs. |
| T-02-02 | I | config validation errors | mitigate | Include path/field context but never print environment secret values or token contents. |
| T-02-03 | D | trainer startup | mitigate | Validate numeric ranges and required paths before expensive model loading starts in later wiring. |
</threat_model>

<verification>
Run the task-level automated commands plus `PATH="/root/.local/bin:$PATH" uv run --extra lint ruff check src/runtime tests/test_runtime_config_io.py` if Ruff is available through the lint extra.
</verification>

<success_criteria>
`src.runtime.config_io` is import-safe, CPU-safe, tested, and capable of validating all existing SFT/DPO/masked-SFT JSON config families without launching CUDA/model/OCR work.
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-contracts-and-run-provenance/02-01-SUMMARY.md`
</output>
