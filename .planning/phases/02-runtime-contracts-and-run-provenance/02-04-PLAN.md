---
phase: 02-runtime-contracts-and-run-provenance
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - src/training/sft_trainer.py
  - src/training/dpo_trainer.py
  - src/training/masked_sft_trainer.py
  - scripts/preflight_runtime.py
  - tests/test_runtime_preflight.py
autonomous: true
requirements: [CFG-01, ART-01, RUN-04, STR-02]
must_haves:
  truths:
    - "Trainer CLI config loading uses the shared validation path before model loading."
    - "User can run a CPU-safe preflight for generation, scoring, SFT, DPO, masked-SFT, synthetic, and evaluation stages."
    - "Preflight output tells the user whether config, paths, artifacts, manifest, and resume conventions are ready."
  artifacts:
    - path: "scripts/preflight_runtime.py"
      provides: "CPU-safe preflight CLI for configs, paths, artifacts, manifests, and resume checks"
      exports: ["main"]
    - path: "tests/test_runtime_preflight.py"
      provides: "Tests for trainer loader wiring and preflight CLI behavior"
    - path: "src/training/sft_trainer.py"
      provides: "SFT loader delegates to shared runtime config validation"
    - path: "src/training/dpo_trainer.py"
      provides: "DPO loader delegates to shared runtime config validation"
    - path: "src/training/masked_sft_trainer.py"
      provides: "Masked-SFT loader delegates to shared runtime config validation"
  key_links:
    - from: "src/training/*_trainer.py"
      to: "src/runtime/config_io.py"
      via: "load_config delegates to load_stage_config"
      pattern: "load_stage_config"
    - from: "scripts/preflight_runtime.py"
      to: "src/runtime/artifacts.py"
      via: "artifact validation reports"
      pattern: "validate_artifacts"
    - from: "scripts/preflight_runtime.py"
      to: "src/runtime/manifests.py"
      via: "manifest inspect/resume checks"
      pattern: "load_run_manifest"
---

<objective>
Wire the runtime contract helpers into trainer startup and expose a CPU-safe preflight command.

Purpose: Phase 2 is only achieved if users can apply the config/artifact/manifest contracts before expensive stage execution, not just read helper modules.
Output: Trainer loader integration, preflight CLI, and CPU-safe tests.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@src/runtime/config_io.py
@src/runtime/paths.py
@src/runtime/artifacts.py
@src/runtime/manifests.py
@src/training/sft_trainer.py
@src/training/dpo_trainer.py
@src/training/masked_sft_trainer.py
@docs/commands.md

<interfaces>
Use these shared helpers created by earlier plans:
```python
load_stage_config(stage: str, path: str | Path) -> SFTConfig | DPOConfig | MaskedSFTConfig
validate_artifacts(stage: str, paths: RuntimePaths) -> ArtifactReport
load_run_manifest(path: str | Path) -> RunManifest
resolve_stage_paths(stage: str, root: Path = Path.cwd()) -> RuntimePaths
```

Existing trainer `main()` functions must continue accepting `--config configs/<stage>.json` and must not change training behavior after config loading succeeds.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify trainer loader wiring and preflight behavior</name>
  <files>tests/test_runtime_preflight.py</files>
  <behavior>
    - `src.training.sft_trainer.load_config`, `dpo_trainer.load_config`, and `masked_sft_trainer.load_config` call `src.runtime.config_io.load_stage_config` and return the same dataclass types as before.
    - Invalid config files fail before any `diffusers`, `transformers`, `paddleocr`, `vllm`, `mlx_lm`, or CUDA-only import is triggered.
    - `scripts.preflight_runtime main(["--stage", "sft", "--config", ...])` validates config, paths, artifacts, and optional manifest without launching trainers.
    - Preflight supports `--stage generate`, `score`, `sft`, `dpo`, `masked-sft`, `synthetic`, and `evaluation`; it exits nonzero on blocking errors and can emit JSON with `--json`.
  </behavior>
  <action>Create tests that monkeypatch `load_stage_config` to prove trainer delegation, inspect `sys.modules` to assert no heavy import side effects, and exercise the preflight CLI against temp fixtures from Plan 02-02/02-03. The RED state should fail because trainer wiring and preflight CLI are not implemented.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_runtime_preflight.py -x</automated>
  </verify>
  <done>Tests fail only because expected runtime preflight wiring is missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delegate trainer config loaders to shared validation</name>
  <files>src/training/sft_trainer.py, src/training/dpo_trainer.py, src/training/masked_sft_trainer.py, tests/test_runtime_preflight.py</files>
  <behavior>
    - Each trainer `load_config(path)` delegates to `load_stage_config` with the correct stage key and keeps the same public function name/signature.
    - The returned object type remains `SFTConfig`, `DPOConfig`, or `MaskedSFTConfig`.
    - Trainer `main()` behavior after successful config loading remains unchanged.
  </behavior>
  <action>Replace direct JSON/dataclass parsing in the three trainer `load_config` functions with calls to `src.runtime.config_io.load_stage_config`. Remove now-unused `json` imports only if they become unused. Do not move model loading, optimizer setup, training loops, or sampling code. This implements CFG-01 and STR-02 across current trainer entry points.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_runtime_preflight.py</automated>
  </verify>
  <done>Trainer loader tests pass and invalid JSON/config values are rejected through the shared runtime path before heavy trainer work starts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement CPU-safe runtime preflight CLI</name>
  <files>scripts/preflight_runtime.py, tests/test_runtime_preflight.py</files>
  <behavior>
    - CLI validates stage config when `--config` is provided and reports field/path errors with nonzero exit.
    - CLI validates artifact contracts from explicit path flags or canonical defaults and distinguishes blocking errors from warnings.
    - CLI can inspect a manifest with `--manifest runs/.../manifest.json` and report resume readiness for outputs/checkpoints/logs.
    - `--json` emits machine-readable report including `stage`, `config`, `artifacts`, `manifest`, `blocking_errors`, and `warnings`.
  </behavior>
  <action>Create `scripts/preflight_runtime.py` backed by `src.runtime.config_io`, `src.runtime.paths`, `src.runtime.artifacts`, and `src.runtime.manifests`. Support explicit flags for common paths (`--prompts`, `--output-dir`, `--images-dir`, `--text-embeds-dir`, `--scores-csv`, `--data-dir`, `--run-dir`, `--manifest`) while defaulting to canonical stage paths. Keep all imports CPU-safe and avoid model/OCR/CUDA imports.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_runtime_preflight.py && PATH="/root/.local/bin:$PATH" uv run pytest</automated>
  </verify>
  <done>Users can run preflight checks for every Phase 2 stage and receive actionable readiness reports before launching long jobs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> validators | User-provided paths/configs/manifests drive filesystem checks. |
| trainer config -> model loading | Config must be validated before expensive or unsafe runtime work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | D | trainer startup | mitigate | Shared validation rejects bad config before model loading, reducing wasted GPU/model jobs. |
| T-02-11 | I | preflight JSON output | mitigate | Report secret env var presence/status only through manifest helpers; do not print token values. |
| T-02-12 | T | preflight path flags | mitigate | Resolve and validate paths through Plan 02-01/02-02 path policy before inspection. |
</threat_model>

<verification>
Run task-level commands and confirm broad pytest still collects only CPU-safe tests under `tests/`.
</verification>

<success_criteria>
Trainer entry points and users share one runtime contract path for configs/artifacts/manifests, with a CPU-safe preflight command available for every Phase 2 pipeline stage.
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-contracts-and-run-provenance/02-04-SUMMARY.md`
</output>
