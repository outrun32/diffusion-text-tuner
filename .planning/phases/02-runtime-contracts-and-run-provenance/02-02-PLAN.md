---
phase: 02-runtime-contracts-and-run-provenance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/runtime/paths.py
  - src/runtime/artifacts.py
  - docs/runtime_contracts.md
  - tests/test_runtime_artifacts.py
autonomous: true
requirements: [ART-01, ART-02, ART-03, ART-04, RUN-03, STR-02]
must_haves:
  truths:
    - "User can see canonical paths and schema/version metadata for generated pipeline artifacts."
    - "User can validate prompt JSONL, generated image directories, latents, text embeddings, scores, masks, synthetic indexes, checkpoints, logs, eval outputs, and manifests before expensive stages."
    - "Generated artifacts remain under ignored runtime roots unless intentionally tiny fixtures or docs assets."
  artifacts:
    - path: "src/runtime/paths.py"
      provides: "Canonical runtime root and stage path resolution helpers"
      exports: ["RuntimePaths", "resolve_stage_paths", "assert_artifact_git_safety"]
    - path: "src/runtime/artifacts.py"
      provides: "Artifact schema constants and CPU-safe preflight validators"
      exports: ["ArtifactValidationError", "ArtifactReport", "validate_artifacts"]
    - path: "docs/runtime_contracts.md"
      provides: "Human-readable canonical path, artifact schema, version metadata, and git-safety contract"
    - path: "tests/test_runtime_artifacts.py"
      provides: "CPU-safe artifact validator tests with tiny temp fixtures"
  key_links:
    - from: "src/runtime/artifacts.py"
      to: "src/runtime/paths.py"
      via: "path resolution before validation"
      pattern: "resolve_stage_paths"
    - from: "docs/runtime_contracts.md"
      to: ".gitignore"
      via: "documents ignored roots and allowed fixture exceptions"
      pattern: "outputs/|runs/|\\*.pt|experiments/assets"
---

<objective>
Define artifact schemas, canonical paths, and validation helpers for local/SLURM pipeline outputs.

Purpose: Phase 2 must make hidden filesystem and tensor contracts explicit before generation, scoring, training, synthesis, or evaluation jobs consume them.
Output: Runtime path helpers, artifact validators, contract documentation, and CPU-safe fixture tests.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/TESTING.md
@.gitignore
@scripts/generate_images.py
@scripts/score_images.py
@src/training/dataset.py
@scripts/synth/build_dataset.py

<interfaces>
Existing artifact shapes and paths to encode as contracts:
```text
outputs/generated/latents/{prompt_id}/v{version}.pt        # dict with "latent"
outputs/generated/text_embeds/{prompt_id}.pt               # dict with "prompt_embeds", "target_text", "prompt"
outputs/generated/images/{prompt_id}/v{version}.png        # generated PNG for scoring
outputs/generated/scores.csv                               # id,version,score,target_text plus scorer columns
data/synth_cyrillic/masked_sft/latents/{sample_id}.pt      # dict with "latent", "mask_lat"
data/synth_cyrillic/masked_sft/text_embeds/{sample_id}.pt  # dict with "prompt_embeds"
data/synth_cyrillic/masked_sft/shapes.csv                  # id,H,W
outputs/{sft,dpo,masked_sft}/checkpoints/...               # LoRA checkpoints
runs/<run_id>/manifest.json                                # created by Plan 02-03
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify artifact validators and git-safety behavior</name>
  <files>tests/test_runtime_artifacts.py</files>
  <behavior>
    - Tiny prompt JSONL fixtures validate required `prompt` and optional `target_text` fields with line-number errors for malformed JSON.
    - Scores CSV fixtures validate required columns `id`, `version`, `score`, `target_text` and schema metadata sidecar support.
    - Generated image/latent/text-embed directory fixtures validate matching prompt IDs and versions without importing CUDA or loading models.
    - Synthetic masked-SFT fixtures validate `latents/`, `text_embeds/`, optional `shapes.csv`, and required tensor dictionary keys using `torch.load(..., weights_only=True)` only on tiny test tensors.
    - Git-safety checks report `outputs/`, `runs/`, generated `.pt`, checkpoint, log, and image paths as non-committable by default while allowing `experiments/assets/*.png` and tests fixtures.
  </behavior>
  <action>Create CPU-safe tests for `resolve_stage_paths`, `validate_artifacts`, and `assert_artifact_git_safety`. The RED state should fail because `src.runtime.paths` and `src.runtime.artifacts` do not exist. Keep fixtures under pytest `tmp_path`; do not add generated binary artifacts to the repository.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_runtime_artifacts.py -x</automated>
  </verify>
  <done>Tests fail only because runtime artifact/path helpers are missing, with named cases for ART-01 through ART-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement path contracts and artifact validation helpers</name>
  <files>src/runtime/paths.py, src/runtime/artifacts.py, tests/test_runtime_artifacts.py</files>
  <behavior>
    - `resolve_stage_paths(stage, root=Path.cwd())` returns canonical defaults for prompt generation, image generation, scoring, SFT, DPO, masked-SFT, synthetic, evaluation, plotting, and run manifests.
    - `validate_artifacts(stage, paths)` returns an `ArtifactReport` with `schema_version`, checked paths, warnings, and errors instead of raising on every missing optional output.
    - Required preflight inputs raise `ArtifactValidationError` or produce blocking errors before GPU-heavy stages.
    - Validation stays import-safe and model-download-free.
  </behavior>
  <action>Create `src/runtime/paths.py` with `RuntimePaths`, canonical roots (`data/`, `outputs/`, `runs/`, `configs/`), local/SLURM-compatible path resolution, and git-safety classification. Create `src/runtime/artifacts.py` with schema-version constants, `ArtifactReport`, validators for prompt JSONL, generated image/latent/text-embedding layouts, scores CSV, synthetic masked-SFT layout, checkpoint dirs, logs, eval outputs, and manifest files. Use existing project naming exactly; do not rename current generated roots.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_runtime_artifacts.py && PATH="/root/.local/bin:$PATH" uv run pytest</automated>
  </verify>
  <done>Artifact/path tests pass and validators report precise missing path, missing key, malformed line, or git-safety errors for all Phase 2 artifact families.</done>
</task>

<task type="auto">
  <name>Task 3: Document canonical runtime contracts</name>
  <files>docs/runtime_contracts.md, tests/test_runtime_artifacts.py</files>
  <action>Create `docs/runtime_contracts.md` with tables for every Phase 2 artifact family: canonical path, producer, consumer, required fields/keys, schema version metadata, preflight command hook, resume/inspect notes, and git-safety classification. Include local and SLURM path guidance that avoids personal absolute paths per CFG-04. Add a lightweight test assertion in `tests/test_runtime_artifacts.py` that required section headings and artifact names are present, so docs cannot drift silently.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_runtime_artifacts.py</automated>
  </verify>
  <done>`docs/runtime_contracts.md` covers prompts, images, latents, text embeddings, masks, scores, selected samples, preference pairs, checkpoints, samples, logs, eval outputs, and run manifests with schema/version and git-safety metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local artifacts -> validators | Local JSONL/CSV/PT/image paths are inspected before downstream ML stages trust them. |
| artifact paths -> git | Generated outputs must not cross into source control unintentionally. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T | `src/runtime/artifacts.py` tensor inspection | mitigate | Only load tiny/local test tensors with `weights_only=True`; document `.pt` artifacts as trusted local outputs. |
| T-02-05 | I | `docs/runtime_contracts.md` | mitigate | Classify generated prompt/output/log artifacts as environment-specific and non-committable. |
| T-02-06 | D | preflight validation | mitigate | Report all validation errors in `ArtifactReport` so users can fix contracts before long jobs. |
</threat_model>

<verification>
Run the task-level automated commands and confirm `git status --short` does not include generated outputs from `outputs/`, `runs/`, or generated `.pt` fixtures.
</verification>

<success_criteria>
Artifact contracts are explicit in code and docs, validators are CPU-safe, and generated artifact safety is enforceable before expensive runtime stages.
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-contracts-and-run-provenance/02-02-SUMMARY.md`
</output>
