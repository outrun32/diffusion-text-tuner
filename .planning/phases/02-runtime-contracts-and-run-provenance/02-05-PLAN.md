---
phase: 02-runtime-contracts-and-run-provenance
plan: 05
type: execute
wave: 4
depends_on: [02-03, 02-04]
files_modified:
  - configs/experiments/README.md
  - configs/experiments/sft/README.md
  - configs/experiments/dpo/README.md
  - configs/experiments/masked_sft/README.md
  - configs/experiments/reward/README.md
  - configs/experiments/evaluation/README.md
  - docs/commands.md
  - README.md
  - Makefile
  - tests/test_runtime_docs.py
autonomous: true
requirements: [CFG-02, ART-02, ART-04, RUN-01, RUN-03, RUN-04]
must_haves:
  truths:
    - "User can find the config family/naming convention for SFT, DPO, masked-SFT, reward, synthesis, evaluation, and ablation variants."
    - "User can run documented manifest and preflight commands from docs and Makefile."
    - "README points users to runtime contracts before long-running generation, scoring, training, synthesis, or evaluation work."
  artifacts:
    - path: "configs/experiments/README.md"
      provides: "Config organization contract and naming rules across experiment families"
    - path: "docs/commands.md"
      provides: "Manifest/preflight command catalog entries"
    - path: "Makefile"
      provides: "Aliases for runtime preflight and manifest commands"
    - path: "tests/test_runtime_docs.py"
      provides: "CPU-safe documentation/command-surface drift tests"
  key_links:
    - from: "docs/commands.md"
      to: "scripts/preflight_runtime.py"
      via: "documented preflight commands"
      pattern: "preflight_runtime"
    - from: "docs/commands.md"
      to: "scripts/run_manifest.py"
      via: "documented manifest commands"
      pattern: "run_manifest"
    - from: "README.md"
      to: "docs/runtime_contracts.md"
      via: "Execution Surface links"
      pattern: "runtime_contracts"
---

<objective>
Publish the Phase 2 runtime contract command surface and config organization contract.

Purpose: The runtime helpers must be discoverable and runnable from the standard docs/Makefile surface, and config variants need a stable home before more experiments are added.
Output: Config-family docs, command catalog updates, Makefile aliases, README links, and docs tests.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-execution-surface-and-pipeline-inventory/01-04-SUMMARY.md
@docs/commands.md
@README.md
@Makefile
@docs/runtime_contracts.md
@scripts/preflight_runtime.py
@scripts/run_manifest.py
@configs/sft.json
@configs/dpo.json
@configs/masked_sft.json

<interfaces>
Commands created by earlier Phase 2 plans and documented here:
```bash
python -m scripts.run_manifest init --stage sft --config configs/sft.json --command "..."
python -m scripts.run_manifest inspect --manifest runs/<run_id>/manifest.json
python -m scripts.preflight_runtime --stage sft --config configs/sft.json --manifest runs/<run_id>/manifest.json
python -m scripts.preflight_runtime --stage generate --prompts data/prompts_simple.jsonl --output-dir outputs/generated
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define config family organization and naming rules</name>
  <files>configs/experiments/README.md, configs/experiments/sft/README.md, configs/experiments/dpo/README.md, configs/experiments/masked_sft/README.md, configs/experiments/reward/README.md, configs/experiments/evaluation/README.md</files>
  <action>Create `configs/experiments/` family directories with README contracts for SFT, DPO, masked-SFT, reward/scoring, synthesis, evaluation, and ablation naming. The docs must state that existing root configs (`configs/sft.json`, `configs/dpo.json`, `configs/masked_sft.json`, current variants) remain runnable compatibility entry points while new variants belong under family directories with names like `{stage}_{reward_or_data}_{purpose}.json`. Include required metadata fields for stage, model IDs/revisions, seeds, inputs, outputs, and manifest expectations per CFG-02/CFG-03. Do not move or delete existing root configs in this plan; preserve current documented commands.</action>
  <verify>
    <automated>python -c "from pathlib import Path; required=[Path('configs/experiments/README.md'),Path('configs/experiments/sft/README.md'),Path('configs/experiments/dpo/README.md'),Path('configs/experiments/masked_sft/README.md'),Path('configs/experiments/reward/README.md'),Path('configs/experiments/evaluation/README.md')]; missing=[str(p) for p in required if not p.exists()]; assert not missing, missing; text=Path('configs/experiments/README.md').read_text(); assert 'configs/sft.json' in text and 'configs/dpo.json' in text and 'configs/masked_sft.json' in text; assert '{stage}_{reward_or_data}_{purpose}.json' in text"</automated>
  </verify>
  <done>Users have a documented config family/naming contract without breaking existing config paths or local/SLURM commands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add docs tests for runtime command surface</name>
  <files>tests/test_runtime_docs.py</files>
  <behavior>
    - Docs mention `docs/runtime_contracts.md`, `scripts.preflight_runtime`, `scripts.run_manifest`, `configs/experiments`, `runs/<run_id>/manifest.json`, and generated-artifact safety.
    - Makefile has aliases for config preflight, artifact preflight, manifest init, and manifest inspect dry-runs.
    - README links to runtime contracts and states preflight/manifest steps happen before long-running GPU/model work.
  </behavior>
  <action>Create tests that assert the required docs and Makefile strings. The RED state should fail until Task 3 updates docs/Makefile/README. Keep tests as string/dry-run checks only; do not run CUDA/model/OCR commands.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_runtime_docs.py -x</automated>
  </verify>
  <done>Docs tests fail only because the runtime command surface has not been published yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Publish manifest and preflight commands in docs and Makefile</name>
  <files>docs/commands.md, README.md, Makefile, tests/test_runtime_docs.py</files>
  <behavior>
    - `docs/commands.md` includes a Runtime contracts section with exact manifest init/inspect/note/metrics and preflight examples for generation, scoring, SFT, DPO, masked-SFT, synthetic, and evaluation.
    - `Makefile` includes safe aliases such as `preflight-sft`, `preflight-dpo`, `preflight-masked-sft`, `manifest-init-sft`, and `manifest-inspect`, all CPU-safe and dry-run friendly where needed.
    - README Execution Surface links `docs/runtime_contracts.md` and `configs/experiments/README.md` and tells users to create/inspect manifests and run preflight before expensive jobs.
  </behavior>
  <action>Update `docs/commands.md`, `README.md`, and `Makefile` to expose the Phase 2 commands from Plans 02-03 and 02-04. Preserve Phase 1 CPU-safe defaults and opt-in smoke/diagnostic separation. Do not imply that preflight launches jobs; it validates readiness. Ensure generated artifacts remain documented as ignored/non-committable. Update tests if needed to match exact command strings.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_runtime_docs.py && make -n preflight-sft preflight-dpo preflight-masked-sft manifest-init-sft manifest-inspect && PATH="/root/.local/bin:$PATH" uv run pytest</automated>
  </verify>
  <done>The standard command surface exposes runtime contracts, manifests, preflight, config organization, and artifact safety without triggering heavy work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> user execution | Users copy documented commands into local/SLURM environments. |
| config organization -> experiment outputs | New config variants determine generated artifact/run provenance naming. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | D | docs/Makefile commands | mitigate | Keep runtime contract aliases CPU-safe and label GPU/model/OCR work as separate opt-in execution. |
| T-02-14 | I | docs/manifest examples | mitigate | Use generic `runs/<run_id>` and local paths; do not include personal absolute paths or tokens. |
| T-02-15 | R | config family docs | mitigate | Require metadata fields and manifest links so experiment configs are traceable to runs. |
</threat_model>

<verification>
Run task-level commands and check that docs/Makefile additions do not alter existing Phase 1 test/lint/smoke aliases.
</verification>

<success_criteria>
Phase 2 runtime contracts are discoverable from README, command docs, Makefile, and config-family docs; users can run manifest and preflight commands before long-running work.
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-contracts-and-run-provenance/02-05-SUMMARY.md`
</output>
