---
phase: 07-moderate-structure-and-extension-cleanup
plan: 06
type: execute
wave: 2
depends_on: [07-01, 07-02, 07-03, 07-04, 07-05]
files_modified:
  - src/toolkit/__init__.py
  - src/toolkit/extension_points.py
  - tests/test_extension_points_docs.py
  - docs/structure_and_extension.md
  - docs/commands.md
  - README.md
  - Makefile
autonomous: true
requirements: [STR-01, STR-05, STR-06]
must_haves:
  truths:
    - "User can see a single extension-point index for prompt generation, image generation, scoring, synthesis, training, evaluation, plotting, run comparison, diagnostics, and thesis outputs."
    - "User can verify Phase 7 structure/importable-module seams through one CPU-safe command."
    - "User can follow exact docs to add future experiments or pipelines through configs, importable modules, thin scripts, manifests, tests, and docs without hidden assumptions."
  artifacts:
    - path: "src/toolkit/extension_points.py"
      provides: "Importable extension-point registry"
      exports: ["ExtensionPoint", "list_extension_points", "get_extension_point"]
    - path: "docs/structure_and_extension.md"
      provides: "Final integrated Phase 7 structure and extension guide"
      contains: "Extension checklist"
    - path: "tests/test_extension_points_docs.py"
      provides: "CPU-safe registry/docs/command drift tests"
    - path: "Makefile"
      provides: "Phase 7 focused verification alias"
      contains: "phase7-structure-tests"
  key_links:
    - from: "docs/structure_and_extension.md"
      to: "src/toolkit/extension_points.py"
      via: "extension-point table mirrors registry entries"
      pattern: "list_extension_points"
    - from: "docs/commands.md"
      to: "Makefile"
      via: "phase7-structure-tests command"
      pattern: "phase7-structure-tests"
---

<objective>
Publish the final Phase 7 extension-point registry, integrated docs, and focused verification command.

Purpose: STR-01, STR-05, and STR-06 are complete only when users can both navigate the cleaned structure and verify the importable seams from a single documented command surface.
Output: `src.toolkit.extension_points`, docs/README/Makefile updates, and CPU-safe drift tests.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-moderate-structure-and-extension-cleanup/07-01-SUMMARY.md
@.planning/phases/07-moderate-structure-and-extension-cleanup/07-02-SUMMARY.md
@.planning/phases/07-moderate-structure-and-extension-cleanup/07-03-SUMMARY.md
@.planning/phases/07-moderate-structure-and-extension-cleanup/07-04-SUMMARY.md
@.planning/phases/07-moderate-structure-and-extension-cleanup/07-05-SUMMARY.md
@docs/structure_and_extension.md
@docs/commands.md
@README.md
@Makefile

<interfaces>
Expected importable seams after Wave 1:
- `src.generation.pipeline.run_generation`
- `src.scoring.pipeline.run_scoring`
- `src.synthesis.dataset_builder.build_dataset`
- `src.plotting.training_metrics.plot_training_metrics`
- Existing `src.evaluation.heldout.write_evaluation_plan`, `src.evaluation.diagnostics`, `src.evaluation.thesis_outputs`, `src.runtime.manifest_diff`, and `src.training.*` helper seams from Phases 5 and 6.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify extension registry and command docs drift tests</name>
  <files>tests/test_extension_points_docs.py</files>
  <behavior>
    - Test 1: `list_extension_points()` includes prompt generation, image generation, scoring, synthesis, training, evaluation, plotting, run comparison, diagnostics, and thesis outputs with module paths and script/config/doc references.
    - Test 2: Every registered module path imports in CPU-safe tests without loading FLUX/Qwen/PaddleOCR/CUDA/model weights.
    - Test 3: docs/structure_and_extension.md, docs/commands.md, README.md, and Makefile all mention `phase7-structure-tests`.
    - Test 4: The extension checklist names config, artifact/manifest contract, importable module, thin CLI wrapper, CPU-safe tests, command docs, and generated-artifact safety.
  </behavior>
  <action>Create CPU-safe tests first. Assert exact registry stage names and documentation strings. Use order-independent import-safety checks that compare newly imported heavy modules rather than failing if another test already imported torch.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_extension_points_docs.py -x</automated>
  </verify>
  <done>Tests fail on missing `src.toolkit.extension_points` and final docs/Makefile command strings before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement extension-point registry</name>
  <files>src/toolkit/__init__.py, src/toolkit/extension_points.py, tests/test_extension_points_docs.py</files>
  <behavior>
    - Test 1: Registry entries include stage name, purpose, implementation module, thin script when applicable, config home, docs path, test target, and generated-artifact notes.
    - Test 2: `get_extension_point("scoring")` returns the scoring entry and raises a clear `KeyError` for unknown stage names.
    - Test 3: Registry modules import without newly loading heavy optional stacks.
  </behavior>
  <action>Create a small standard-library-only `src/toolkit/extension_points.py` module with a frozen `ExtensionPoint` dataclass, immutable registry tuple, `list_extension_points() -> tuple[ExtensionPoint, ...]`, and `get_extension_point(name: str) -> ExtensionPoint`. Register existing and Wave 1 seams: prompt generation (`src.prompt_pipeline.generate`), image generation (`src.generation.pipeline`), scoring (`src.scoring.pipeline`), synthesis (`src.synthesis.dataset_builder`), training (`src.training.*` helper docs), evaluation (`src.evaluation.heldout`/diagnostics/thesis outputs), plotting (`src.plotting.training_metrics`), run comparison (`src.runtime.manifest_diff` and `scripts.compare_training_runs`), diagnostics (`scripts/diagnose_*.py`), and thesis outputs (`src.evaluation.thesis_outputs`). Keep the registry descriptive only; do not build a dynamic plugin loader or execute commands.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_extension_points_docs.py -q</automated>
  </verify>
  <done>`src.toolkit.extension_points` provides a CPU-safe extension-point index with registry tests passing except docs strings deferred to Task 3 if separated by test assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Publish final Phase 7 command surface and extension checklist</name>
  <files>docs/structure_and_extension.md, docs/commands.md, README.md, Makefile, tests/test_extension_points_docs.py</files>
  <behavior>
    - Test 1: The structure guide mirrors the extension registry and includes the exact extension checklist.
    - Test 2: `docs/commands.md` and README advertise the focused Phase 7 CPU-safe command.
    - Test 3: `make -n phase7-structure-tests` prints the expected pytest file selection without running heavy jobs.
  </behavior>
  <action>Update the Phase 7 guide with an extension-point table sourced from the registry entries, add an explicit checklist for adding new experiments/pipelines, and document which files to touch for config, artifact/manifest contracts, importable code, thin wrapper, tests, command docs, README links, and generated-artifact safety. Add `phase7-structure-tests` to `Makefile` with a focused pytest command covering all Phase 7 structure/seam tests. Add the same command to `docs/commands.md` and link the final extension guide from README. Preserve existing command sections and aliases.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_structure_extension_docs.py tests/test_generation_pipeline_contracts.py tests/test_scoring_pipeline_contracts.py tests/test_synthesis_pipeline_contracts.py tests/test_plotting_pipeline_contracts.py tests/test_extension_points_docs.py -q && make -n phase7-structure-tests && PATH="/root/.local/bin:$PATH" uv run --extra lint ruff check src/toolkit/extension_points.py tests/test_extension_points_docs.py</automated>
  </verify>
  <done>Final Phase 7 registry, docs, README link, Makefile alias, and focused CPU-safe verification command are complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry/docs→implementation | Users rely on registry/docs to decide where to add future code. |
| Makefile/docs→shell | Users may run documented verification aliases. |
| extension guidance→artifact handling | Users may create configs, outputs, manifests, and thesis artifacts based on the checklist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Tampering | extension registry | mitigate | Tests assert registry entries and module importability so stale or wrong seams are detected. |
| T-07-06-02 | Denial of Service | `phase7-structure-tests` | mitigate | Alias runs only CPU-safe tests and `make -n` verification; no GPU/model/OCR commands are hidden behind it. |
| T-07-06-03 | Information Disclosure | extension checklist | mitigate | Checklist requires generated-artifact safety and manifest/privacy review for outputs that may include prompt text, scores, or local paths. |
</threat_model>

<verification>
Run the focused Phase 7 pytest selection, Makefile dry-run alias, and Ruff on the registry/tests.
</verification>

<success_criteria>
- Extension registry lists all Phase 7-relevant seams and imports safely.
- `docs/structure_and_extension.md`, `docs/commands.md`, README, and Makefile expose the same Phase 7 verification command.
- User has a complete checklist for adding future experiments/pipelines through documented extension points.
</success_criteria>

<output>
After completion, create `.planning/phases/07-moderate-structure-and-extension-cleanup/07-06-SUMMARY.md`
</output>
