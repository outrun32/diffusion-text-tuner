---
phase: 07-moderate-structure-and-extension-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/generation/__init__.py
  - src/generation/pipeline.py
  - scripts/generate_images.py
  - tests/test_generation_pipeline_contracts.py
autonomous: true
requirements: [STR-05, STR-06]
must_haves:
  truths:
    - "User can invoke image generation through the existing `python -m scripts.generate_images` command."
    - "Future generation variants can import generation configuration, prompt loading, output-path resolution, seed planning, and run orchestration without editing unrelated scripts."
    - "Importing the generation module in CPU-safe tests does not load FLUX, CUDA, torchvision, or model weights."
  artifacts:
    - path: "src/generation/pipeline.py"
      provides: "Importable image-generation implementation seam"
      exports: ["GenerationConfig", "GenerationPaths", "load_prompt_records", "resolve_generation_paths", "plan_generation_seed", "run_generation"]
    - path: "scripts/generate_images.py"
      provides: "Thin CLI wrapper around src.generation.pipeline"
      contains: "run_generation"
    - path: "tests/test_generation_pipeline_contracts.py"
      provides: "CPU-safe generation seam tests"
  key_links:
    - from: "scripts/generate_images.py"
      to: "src/generation/pipeline.py"
      via: "CLI parser builds GenerationConfig then calls run_generation"
      pattern: "from src\.generation\.pipeline import"
---

<objective>
Put image generation behind an importable module while preserving the existing CLI entry point.

Purpose: STR-05 requires thin scripts backed by importable implementation modules, and STR-06 requires a stable seam for future generation variants.
Output: `src.generation.pipeline` plus a thin `scripts.generate_images` wrapper and CPU-safe contract tests.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@scripts/generate_images.py
@src/training/flux2_utils.py

<interfaces>
Current `scripts/generate_images.py` responsibilities:
```python
def main():
    # Parses --prompts, --output_dir, --model_id, --lora_path,
    # --versions_per_prompt, --batch_size, --num_inference_steps,
    # --guidance_scale, --resolution, --seed, --start_idx, --end_idx,
    # --save_latents, --save_png.
```
Existing generation outputs:
- `{output_dir}/latents/{prompt_id}/v{version}.pt`
- `{output_dir}/text_embeds/{prompt_id}.pt`
- `{output_dir}/images/{prompt_id}/v{version}.png`
- `{output_dir}/manifest.json`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify generation module contracts</name>
  <files>tests/test_generation_pipeline_contracts.py</files>
  <behavior>
    - Test 1: `GenerationConfig` preserves CLI defaults and selected prompt index slicing semantics.
    - Test 2: `resolve_generation_paths(Path("outputs/generated"))` returns latents, text embeddings, images, and manifest paths without creating model objects.
    - Test 3: `plan_generation_seed(seed=42, prompt_index=3, versions_per_prompt=5, version=2)` returns the same deterministic seed formula currently used by the script.
    - Test 4: Importing `src.generation.pipeline` does not newly import `diffusers`, `torchvision`, or CUDA/model modules.
  </behavior>
  <action>Create CPU-safe tests for the importable generation seam before moving implementation. Use temporary JSONL fixtures for prompt loading. Assert that the script imports `run_generation` from `src.generation.pipeline` after implementation.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_generation_pipeline_contracts.py -x</automated>
  </verify>
  <done>Tests fail on missing `src.generation.pipeline` before production code is changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement import-safe generation pipeline module</name>
  <files>src/generation/__init__.py, src/generation/pipeline.py, tests/test_generation_pipeline_contracts.py</files>
  <behavior>
    - Test 1: Prompt JSONL loading returns records sliced by `start_idx:end_idx` and preserves `prompt`/`target_text` fields.
    - Test 2: Directory/path resolution is deterministic and path-object based.
    - Test 3: Heavy imports occur inside `run_generation`, not at module import time.
  </behavior>
  <action>Create `src/generation/pipeline.py` with frozen dataclasses `GenerationConfig` and `GenerationPaths`, pure helpers `load_prompt_records`, `resolve_generation_paths`, and `plan_generation_seed`, plus `run_generation(config: GenerationConfig) -> None`. Move the current generation loop into `run_generation` without changing file names, prompt IDs, text-embedding payload keys, latent payload keys, manifest keys, seed formula, skip-if-existing behavior, or logging semantics. Keep `diffusers`, `torchvision`, FLUX utility, and CUDA-dependent imports inside `run_generation` so import-safe tests remain CPU/model-download-free.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_generation_pipeline_contracts.py -q</automated>
  </verify>
  <done>`src.generation.pipeline` exposes the generation seam and passes CPU-safe contract tests without importing FLUX/model stacks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Thin the generation CLI wrapper</name>
  <files>scripts/generate_images.py, tests/test_generation_pipeline_contracts.py</files>
  <behavior>
    - Test 1: `scripts.generate_images.main(argv)` builds `GenerationConfig` from the same arguments and delegates to `run_generation`.
    - Test 2: CLI defaults match current behavior for output dir, model ID, versions, inference steps, guidance, resolution, seed, save_latents, and save_png.
  </behavior>
  <action>Replace in-script generation implementation with argument parsing plus delegation to `src.generation.pipeline.run_generation`. Make `main(argv: list[str] | None = None) -> int` testable, call `raise SystemExit(main())` under the module guard, and keep the existing module invocation `python -m scripts.generate_images` working. Do not rename arguments or output paths.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_generation_pipeline_contracts.py -q && PATH="/root/.local/bin:$PATH" uv run --extra lint ruff check src/generation/pipeline.py scripts/generate_images.py tests/test_generation_pipeline_contracts.py</automated>
  </verify>
  <done>`scripts/generate_images.py` is a thin wrapper around importable generation code, with targeted tests and Ruff passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prompt JSONL→generation | User-provided prompt records influence generated files and manifest metadata. |
| CLI paths→filesystem writes | User-provided output paths determine where images, tensors, embeddings, and manifest files are written. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | `load_prompt_records` | mitigate | Preserve JSONL parsing and required `prompt` access; let malformed records fail before expensive work rather than silently substituting values. |
| T-07-02-02 | Denial of Service | `run_generation` | mitigate | Preserve `start_idx`, `end_idx`, skip-existing behavior, and explicit command invocation so users control expensive GPU/model work. |
| T-07-02-03 | Information Disclosure | output manifest | mitigate | Preserve existing manifest shape and avoid adding raw environment secrets or cache paths. |
</threat_model>

<verification>
Run targeted generation contract tests and Ruff on plan-owned files. Do not run FLUX, CUDA, model download, or image generation.
</verification>

<success_criteria>
- Existing `python -m scripts.generate_images` arguments are preserved.
- `src.generation.pipeline` can be imported in CPU-safe tests without loading FLUX/CUDA/model stacks.
- Generation implementation is accessible through `run_generation(config)` for future variants.
</success_criteria>

<output>
After completion, create `.planning/phases/07-moderate-structure-and-extension-cleanup/07-02-SUMMARY.md`
</output>
