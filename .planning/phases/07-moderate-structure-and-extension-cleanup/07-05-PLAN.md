---
phase: 07-moderate-structure-and-extension-cleanup
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/plotting/__init__.py
  - src/plotting/training_metrics.py
  - scripts/plot_metrics.py
  - tests/test_plotting_pipeline_contracts.py
autonomous: true
requirements: [STR-05, STR-06]
must_haves:
  truths:
    - "User can keep invoking training metric plotting through `python scripts/plot_metrics.py` or `python -m scripts.plot_metrics`."
    - "Future plotting variants can import metric loading, smoothing, summary calculation, and plot rendering without editing unrelated scripts."
    - "Importing plotting helpers in CPU-safe tests does not require a display or immediately import plotting backends."
  artifacts:
    - path: "src/plotting/training_metrics.py"
      provides: "Importable plotting implementation seam"
      exports: ["TrainingMetrics", "load_metrics", "smooth", "summarize_metrics", "plot_training_metrics"]
    - path: "scripts/plot_metrics.py"
      provides: "Thin plotting CLI wrapper"
      contains: "plot_training_metrics"
    - path: "tests/test_plotting_pipeline_contracts.py"
      provides: "CPU-safe plotting seam tests"
  key_links:
    - from: "scripts/plot_metrics.py"
      to: "src/plotting/training_metrics.py"
      via: "CLI parser delegates to plot_training_metrics"
      pattern: "from src\.plotting\.training_metrics import"
---

<objective>
Move training metric plotting behind an importable plotting module while preserving the existing script command.

Purpose: STR-05 includes plotting among thin CLI scripts, and STR-06 requires future plot/thesis-output additions to use stable seams.
Output: `src.plotting.training_metrics`, thin `scripts.plot_metrics`, and CPU-safe tests for loading/smoothing/summary behavior.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/STRUCTURE.md
@scripts/plot_metrics.py
@scripts/thesis_plots/plot_sft_losses.py
@scripts/thesis_plots/plot_dpo_metrics.py

<interfaces>
Current `scripts/plot_metrics.py` functions:
```python
def load_metrics(csv_path: str) -> dict[str, list[float]]: ...
def smooth(values: list[float], window: int = 5) -> np.ndarray: ...
def plot(metrics_csv: str, output_dir: str | None = None): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify plotting module contracts</name>
  <files>tests/test_plotting_pipeline_contracts.py</files>
  <behavior>
    - Test 1: `load_metrics` reads a tiny CSV into typed metric lists for step, loss, reward, grad_norm, lr, and elapsed_s.
    - Test 2: `smooth([1, 2, 3, 4, 5], window=3)` returns moving averages `[2, 3, 4]` without requiring matplotlib.
    - Test 3: `summarize_metrics` returns steps, reward start/end/max/mean, grad-norm mean/max, and last-minus-first trend values.
    - Test 4: Importing `src.plotting.training_metrics` does not require an interactive display and does not import matplotlib until plotting is requested.
  </behavior>
  <action>Create CPU-safe tests first. Use temporary CSV files and pure helper assertions. Include a script-thinness assertion after implementation.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_plotting_pipeline_contracts.py -x</automated>
  </verify>
  <done>Tests fail on missing `src.plotting.training_metrics` before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement importable plotting helpers</name>
  <files>src/plotting/__init__.py, src/plotting/training_metrics.py, tests/test_plotting_pipeline_contracts.py</files>
  <behavior>
    - Test 1: Metric loading, smoothing, and summary calculations are deterministic.
    - Test 2: `plot_training_metrics` preserves the current output file name `training_curves.png` and printed summary fields.
    - Test 3: Matplotlib backend setup occurs inside plotting, not at module import.
  </behavior>
  <action>Create `src/plotting/training_metrics.py` with a `TrainingMetrics` dataclass or equivalent typed return shape, pure `load_metrics`, `smooth`, and `summarize_metrics` helpers, and `plot_training_metrics(metrics_csv: str | Path, output_dir: str | Path | None = None) -> Path`. Move current plotting behavior without changing chart contents, output file name, or summary semantics. Import `matplotlib`/`pyplot` inside `plot_training_metrics` and set the Agg backend there so CPU-safe helper imports do not need plotting backends.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_plotting_pipeline_contracts.py -q</automated>
  </verify>
  <done>`src.plotting.training_metrics` provides pure metric helpers and a plotting function with CPU-safe tests passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Thin the plotting CLI wrapper</name>
  <files>scripts/plot_metrics.py, tests/test_plotting_pipeline_contracts.py</files>
  <behavior>
    - Test 1: `scripts.plot_metrics.main(argv)` delegates to `plot_training_metrics` with the parsed metrics CSV and output directory.
    - Test 2: Existing positional `metrics_csv` and optional `--output-dir` arguments are unchanged.
    - Test 3: Existing imports of `load_metrics`, `smooth`, or `plot` from `scripts.plot_metrics` remain compatible through re-exports or aliases.
  </behavior>
  <action>Replace `scripts/plot_metrics.py` with a thin argparse wrapper. Re-export `load_metrics`, `smooth`, and a compatibility `plot` alias if needed. Support both direct script execution and `python -m scripts.plot_metrics` with `raise SystemExit(main())`.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_plotting_pipeline_contracts.py -q && PATH="/root/.local/bin:$PATH" uv run --extra lint ruff check src/plotting/training_metrics.py scripts/plot_metrics.py tests/test_plotting_pipeline_contracts.py</automated>
  </verify>
  <done>`scripts/plot_metrics.py` is a thin wrapper, plotting contracts pass, and Ruff is clean for plan-owned files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| metrics CSV→plotting | Local run metrics are parsed into plots and printed summaries. |
| output path→filesystem writes | User-provided output directory controls where plot images are written. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Tampering | `load_metrics` | mitigate | Preserve explicit CSV columns and fail on malformed numeric values rather than substituting hidden defaults. |
| T-07-05-02 | Information Disclosure | `plot_training_metrics` | mitigate | Write only to caller-selected output directory and keep plots under ignored runtime/thesis-output roots as documented. |
| T-07-05-03 | Denial of Service | plotting backend | mitigate | Keep plotting optional and backend import inside plotting function so importing helpers stays lightweight. |
</threat_model>

<verification>
Run plotting contract tests and Ruff. Do not require GPU, model, OCR, or generated run artifacts.
</verification>

<success_criteria>
- Existing plotting command works through a thin wrapper.
- Metric loading/smoothing/summary helpers are importable from `src.plotting.training_metrics`.
- Plotting backend imports occur only when rendering plots.
</success_criteria>

<output>
After completion, create `.planning/phases/07-moderate-structure-and-extension-cleanup/07-05-SUMMARY.md`
</output>
