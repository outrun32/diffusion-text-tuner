---
phase: 07-moderate-structure-and-extension-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/structure_and_extension.md
  - scripts/README.md
  - tests/test_structure_extension_docs.py
  - README.md
autonomous: true
requirements: [STR-01, STR-06]
must_haves:
  truths:
    - "User can identify where reusable source modules, thin scripts, cluster launchers, configs, diagnostics, experiments, generated outputs, tests, and thesis artifacts live."
    - "User can distinguish supported command wrappers from diagnostics, historical experiments, and generated runtime outputs."
    - "User can find extension rules before adding a new experiment, trainer, reward variant, dataset, pipeline, plot, or thesis-output step."
  artifacts:
    - path: "docs/structure_and_extension.md"
      provides: "Canonical structure map and extension rules"
      contains: "Clear homes for reusable modules, scripts, cluster launchers, configs, diagnostics, experiments, generated outputs, tests, and thesis artifacts"
    - path: "scripts/README.md"
      provides: "Script-family navigation and supported-wrapper boundaries"
      contains: "Thin CLI wrappers"
    - path: "tests/test_structure_extension_docs.py"
      provides: "CPU-safe docs drift coverage for Phase 7 structure and extension guidance"
  key_links:
    - from: "README.md"
      to: "docs/structure_and_extension.md"
      via: "Phase 7 structure/extension link"
      pattern: "structure_and_extension\.md"
    - from: "scripts/README.md"
      to: "docs/structure_and_extension.md"
      via: "scripts navigation guidance"
      pattern: "Thin CLI wrappers"
---

<objective>
Create the Phase 7 navigation and extension contract for moderate brownfield cleanup.

Purpose: STR-01 and STR-06 require users to know where code, scripts, configs, diagnostics, generated outputs, tests, and thesis artifacts belong before later plans move or wrap implementation code.
Output: Tested docs that classify repository homes, script families, generated-artifact boundaries, and extension rules.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/02-runtime-contracts-and-run-provenance/02-05-SUMMARY.md
@.planning/phases/06-reward-and-evaluation-validity/06-07-SUMMARY.md
@docs/pipeline_inventory.md
@docs/commands.md
@README.md

<interfaces>
Existing command-discovery pattern:
- `docs/commands.md` is the standard command catalog.
- `README.md` links phase guides from the project front door.
- `Makefile` aliases are used for dry-run command discoverability.
- Generated images, tensors, checkpoints, logs, reports, contact sheets, thesis bundles, and private run outputs remain runtime artifacts under ignored roots unless intentionally tiny fixtures.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify structure and script-navigation drift tests</name>
  <files>tests/test_structure_extension_docs.py</files>
  <behavior>
    - Test 1: docs/structure_and_extension.md must name clear homes for reusable source modules, thin scripts, cluster launchers, configs, diagnostics, experiments, generated outputs, tests, and thesis artifacts.
    - Test 2: scripts/README.md must classify supported wrappers, diagnostics, cluster jobs, synthesis helpers, plotting helpers, and historical/experiment scripts without treating generated runtime outputs as source.
    - Test 3: README.md must link docs/structure_and_extension.md.
  </behavior>
  <action>Create CPU-safe pytest docs drift tests before writing docs. Use exact strings for STR-01/STR-06-critical headings and artifact-safety wording so later cleanup cannot silently drop the navigation contract.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_structure_extension_docs.py -x</automated>
  </verify>
  <done>Tests fail for missing docs/README strings before implementation, proving the expected structure contract is captured.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish the repository structure and script-home contract</name>
  <files>docs/structure_and_extension.md, scripts/README.md, README.md, tests/test_structure_extension_docs.py</files>
  <behavior>
    - Test 1: The structure guide lists each required home and states whether it is source, wrapper, diagnostic, config, test, generated runtime output, or thesis evidence.
    - Test 2: The script README identifies thin CLI wrappers backed by importable modules and marks diagnostics as explicit/manual.
    - Test 3: The README front door links the new guide.
  </behavior>
  <action>Write `docs/structure_and_extension.md` as the canonical Phase 7 navigation guide. Include sections for `src/`, `scripts/`, `scripts/cluster/`, `scripts/synth/`, `scripts/thesis_plots/`, `configs/`, `configs/experiments/`, `tests/`, `experiments/`, ignored `outputs/`/`runs/`, and thesis-output artifacts. Write `scripts/README.md` to classify supported wrappers versus diagnostics and experiment/historical helpers. Update `README.md` with a concise link to the new guide. Keep all guidance behavior-preserving: no deletion instructions and no generated artifacts committed.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_structure_extension_docs.py -q</automated>
  </verify>
  <done>Users can navigate Phase 7 structure homes and script families from README, docs, and scripts/README; docs drift tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs→shell commands | Users may copy commands from docs into local/SLURM environments. |
| docs→artifact handling | Users may decide whether to commit or ignore files based on structure docs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | docs/structure_and_extension.md | mitigate | State generated artifact roots remain ignored and should not be committed unless intentionally tiny fixtures. |
| T-07-01-02 | Information Disclosure | scripts/README.md | mitigate | Mark diagnostics and generated outputs as local/runtime surfaces that can contain private prompt text, scores, paths, or run metadata. |
| T-07-01-03 | Repudiation | README.md guide link | mitigate | Docs drift tests require the README link so structure guidance stays discoverable. |
</threat_model>

<verification>
Run the targeted docs drift test and confirm no generated artifacts are created.
</verification>

<success_criteria>
- `tests/test_structure_extension_docs.py` passes.
- `docs/structure_and_extension.md` and `scripts/README.md` clearly separate source, wrappers, diagnostics, configs, tests, generated outputs, and thesis artifacts.
- README links the Phase 7 structure guide.
</success_criteria>

<output>
After completion, create `.planning/phases/07-moderate-structure-and-extension-cleanup/07-01-SUMMARY.md`
</output>
