---
phase: 07-moderate-structure-and-extension-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scoring/__init__.py
  - src/scoring/pipeline.py
  - scripts/score_images.py
  - tests/test_scoring_pipeline_contracts.py
autonomous: true
requirements: [STR-05, STR-06]
must_haves:
  truths:
    - "User can keep invoking reward scoring through `python -m scripts.score_images`."
    - "Future scoring variants can import task collection, canonical row conversion, sidecar writing, and scoring orchestration without editing unrelated scripts."
    - "Canonical Phase 6 score fields and missing-evidence behavior remain shared with evaluation reports."
  artifacts:
    - path: "src/scoring/pipeline.py"
      provides: "Importable scoring implementation seam"
      exports: ["CANONICAL_SCORE_COLUMNS", "ScoringConfig", "ScoringTask", "collect_scoring_tasks", "build_canonical_score_row", "write_score_schema_sidecar", "run_scoring"]
    - path: "scripts/score_images.py"
      provides: "Thin scoring CLI wrapper"
      contains: "run_scoring"
    - path: "tests/test_scoring_pipeline_contracts.py"
      provides: "CPU-safe scoring seam tests"
  key_links:
    - from: "scripts/score_images.py"
      to: "src/scoring/pipeline.py"
      via: "CLI parser builds ScoringConfig then calls run_scoring"
      pattern: "from src\.scoring\.pipeline import"
    - from: "src/scoring/pipeline.py"
      to: "src/evaluation/reward_interface.py"
      via: "product score and metadata helpers"
      pattern: "compute_product_score"
---

<objective>
Move reward scoring implementation behind an importable scoring module while preserving the existing CLI and canonical Phase 6 score outputs.

Purpose: STR-05 requires scoring to be a thin script over reusable code, and STR-06 requires future reward/scoring variants to have a documented seam.
Output: `src.scoring.pipeline`, thin `scripts.score_images`, and CPU-safe tests for canonical score/task contracts.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/phases/06-reward-and-evaluation-validity/06-01-SUMMARY.md
@.planning/phases/06-reward-and-evaluation-validity/06-04-SUMMARY.md
@scripts/score_images.py
@src/evaluation/reward_interface.py
@src/training/rewards.py

<interfaces>
Current canonical score exports in `scripts/score_images.py`:
```python
CANONICAL_SCORE_COLUMNS = ["id", "sample_id", "version", "score", "product_score", ...]
def build_canonical_score_row(*, sample_id: str, version: int, target_text: str, evidence: dict[str, Any], formula: ProductScoreFormula | None = None, manifest_path: str = "") -> dict[str, Any]: ...
def write_score_schema_sidecar(output_csv: str | Path, *, formula: ProductScoreFormula | None = None, source_manifest_paths: list[str] | tuple[str, ...] = (), generated_at: str | None = None) -> Path: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify scoring seam contracts</name>
  <files>tests/test_scoring_pipeline_contracts.py</files>
  <behavior>
    - Test 1: `build_canonical_score_row` remains importable from `src.scoring.pipeline` and preserves `product_score`, `missing_components`, `formula_complete`, exact-match, character metrics, and legacy `id/version/score/target_text` fields.
    - Test 2: `collect_scoring_tasks` reads tiny fixture text-embedding metadata with `torch.load(..., map_location="cpu", weights_only=True)` and returns deterministic `ScoringTask` records.
    - Test 3: `write_score_schema_sidecar` writes the same sidecar schema fields required by Phase 6 validators.
    - Test 4: Importing `src.scoring.pipeline` does not newly import `diffusers`, `transformers`, `paddleocr`, or CUDA/model stacks.
  </behavior>
  <action>Create CPU-safe tests first. Use temporary directories with tiny `.pt` metadata files and empty image-path names; do not open real images or instantiate Qwen/PaddleOCR. Include a script-thinness assertion for `scripts.score_images` delegation after implementation.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_scoring_pipeline_contracts.py -x</automated>
  </verify>
  <done>Tests fail on missing `src.scoring.pipeline` before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement importable scoring pipeline module</name>
  <files>src/scoring/__init__.py, src/scoring/pipeline.py, tests/test_scoring_pipeline_contracts.py</files>
  <behavior>
    - Test 1: Canonical row conversion and sidecar writing are unchanged from Phase 6.
    - Test 2: Scoring task discovery handles missing embedding metadata by skipping with warnings as before.
    - Test 3: Heavy scorer construction happens inside `run_scoring`, not at module import.
  </behavior>
  <action>Create `src/scoring/pipeline.py` with dataclasses `ScoringConfig` and `ScoringTask`; move `CANONICAL_SCORE_COLUMNS`, row helpers, sidecar writer, task collection, resume/shard handling, scorer selection, CSV writing, and final sidecar writing from `scripts/score_images.py` into `run_scoring(config: ScoringConfig) -> None`. Preserve scorer choices (`vlm`, `ocr`, `both`), CSV columns, shard formula, resume behavior, warning behavior, source-manifest sidecar metadata, and compatibility fields. Keep Qwen/PaddleOCR scorer imports or construction inside `run_scoring` branches.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_scoring_pipeline_contracts.py tests/test_evaluation_scoring_outputs.py tests/test_reward_wrapper_contracts.py -q</automated>
  </verify>
  <done>`src.scoring.pipeline` owns scoring implementation contracts and Phase 6 score-output tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Thin the scoring CLI wrapper</name>
  <files>scripts/score_images.py, tests/test_scoring_pipeline_contracts.py</files>
  <behavior>
    - Test 1: `scripts.score_images.main(argv)` builds `ScoringConfig` and delegates to `run_scoring`.
    - Test 2: CLI arguments and defaults are unchanged.
    - Test 3: Existing imports of canonical score helpers from script continue through compatibility re-exports if tests still rely on them.
  </behavior>
  <action>Replace scoring implementation in `scripts/score_images.py` with argument parsing and delegation. Re-export canonical helpers from `src.scoring.pipeline` when necessary to preserve existing test/import compatibility. Make `main(argv: list[str] | None = None) -> int` return a process code and keep `python -m scripts.score_images` behavior unchanged.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_scoring_pipeline_contracts.py tests/test_evaluation_scoring_outputs.py tests/test_reward_wrapper_contracts.py -q && PATH="/root/.local/bin:$PATH" uv run --extra lint ruff check src/scoring/pipeline.py scripts/score_images.py tests/test_scoring_pipeline_contracts.py</automated>
  </verify>
  <done>`scripts/score_images.py` is a thin wrapper while canonical score tests, reward wrapper tests, and Ruff pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local `.pt` metadata→scoring tasks | Text-embedding metadata is loaded from generated local artifacts. |
| generated image paths→scorer models | Local PNG paths are sent to OCR/VLM scorer objects during explicit scoring runs. |
| scoring results→CSV/sidecar | Reward evidence becomes comparison and thesis evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering | `collect_scoring_tasks` | mitigate | Preserve `torch.load(..., map_location="cpu", weights_only=True)` and skip missing metadata with warnings. |
| T-07-03-02 | Information Disclosure | score rows and sidecars | mitigate | Preserve manifest-link fields without adding raw secrets/cache paths; keep generated score outputs as ignored runtime artifacts. |
| T-07-03-03 | Denial of Service | `run_scoring` scorer branches | mitigate | Preserve explicit scorer choice, shard controls, resume behavior, and batch-size controls; do not run scorers in tests. |
</threat_model>

<verification>
Run scoring seam tests, existing Phase 6 scoring output tests, reward wrapper tests, and Ruff on plan-owned files only.
</verification>

<success_criteria>
- `python -m scripts.score_images` remains the scoring command.
- `src.scoring.pipeline` exposes reusable scoring contracts.
- Canonical score row/sidecar behavior stays compatible with Phase 6 validation and diagnostics.
</success_criteria>

<output>
After completion, create `.planning/phases/07-moderate-structure-and-extension-cleanup/07-03-SUMMARY.md`
</output>
