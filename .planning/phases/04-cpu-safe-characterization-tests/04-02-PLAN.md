---
phase: 04-cpu-safe-characterization-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_training_dataset_contracts.py
  - src/training/dataset.py
  - src/training/selection.py
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "User can run CPU-safe tests for SFT, DPO, and masked-SFT dataset loading."
    - "User can verify collator padding, preference pair construction, selected sample semantics, and resolution buckets with tiny fixtures."
  artifacts:
    - path: "tests/test_training_dataset_contracts.py"
      provides: "Dataset/collator/selection characterization tests"
      min_lines: 120
    - path: "src/training/dataset.py"
      provides: "Dataset and collator contracts under characterization"
    - path: "src/training/selection.py"
      provides: "Materialized selection contracts under characterization"
  key_links:
    - from: "tests/test_training_dataset_contracts.py"
      to: "src/training/dataset.py"
      via: "SFTDataset, DPODataset, MaskedSFTDataset, collators, ResolutionBucketSampler"
      pattern: "SFTDataset|DPODataset|MaskedSFTDataset|ResolutionBucketSampler"
    - from: "tests/test_training_dataset_contracts.py"
      to: "src/training/selection.py"
      via: "materialized selected sample / DPO pair edge cases"
      pattern: "materialize_"
---

<objective>
Lock dataset loading, collator, pair/sample selection, and resolution bucket behavior with CPU-safe tiny fixtures.

Purpose: protect file-backed training data contracts before Phase 5 changes trainer modes and comparability plumbing.
Output: dataset characterization tests plus narrow fixes if existing contracts fail.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-cpu-safe-characterization-tests/04-CONTEXT.md
@.planning/phases/04-cpu-safe-characterization-tests/04-RESEARCH.md
@.planning/phases/04-cpu-safe-characterization-tests/04-PATTERNS.md
@.planning/phases/03-data-curriculum-and-dataset-quality/03-04-SUMMARY.md
@src/training/dataset.py
@src/training/selection.py

<interfaces>
From `src.training.dataset`:
```python
class SFTDataset(Dataset): ...
def sft_collate_fn(batch: list[dict]) -> dict: ...
class DPODataset(Dataset): ...
def dpo_collate_fn(batch: list[dict]) -> dict: ...
class MaskedSFTDataset(Dataset): ...
def masked_sft_collate_fn(batch: list[dict]) -> dict: ...
class ResolutionBucketSampler(Sampler[list[int]]): ...
```

From `src.training.selection`:
```python
def materialize_sft_samples(...): ...
def materialize_dpo_pairs(...): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Characterize SFT and DPO dataset/collator contracts</name>
  <files>tests/test_training_dataset_contracts.py, src/training/dataset.py</files>
  <behavior>
    - Test 1: `SFTDataset` reads a tiny scores CSV and tensor directories, filters by threshold, and returns latent/prompt embedding/score fields with expected shapes.
    - Test 2: `sft_collate_fn` stacks latents and pads prompt embeddings to the longest sequence in the batch.
    - Test 3: `DPODataset` creates best-vs-worst pairs only when winner score and margin pass thresholds.
    - Test 4: `dpo_collate_fn` stacks winner/loser latents and pads shared prompt embeddings.
  </behavior>
  <action>Create `tmp_path` CSV and `.pt` fixtures with tiny tensors, run RED if the characterization is missing, then adjust `src/training/dataset.py` only to preserve the documented constructor and collator behavior. Do not make trainers consume materialized artifacts in this phase.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_training_dataset_contracts.py -x</automated>
  </verify>
  <done>SFT/DPO datasets and collators are characterized by deterministic CPU-safe fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Characterize masked-SFT dataset and resolution buckets</name>
  <files>tests/test_training_dataset_contracts.py, src/training/dataset.py</files>
  <behavior>
    - Test 1: `MaskedSFTDataset` fails fast for missing `latents/` or `text_embeds/` directories.
    - Test 2: matched tiny latent/text-embed fixtures load `sample_id`, `latent`, `mask_lat`, and `prompt_embeds` with expected shapes.
    - Test 3: `masked_sft_collate_fn` stacks masks/latents and pads text embeddings.
    - Test 4: `ResolutionBucketSampler` groups by `shapes.csv`, yields deterministic batches for a fixed seed, and avoids per-sample latent fallback when shapes are complete.
  </behavior>
  <action>Extend the same test file for masked-SFT and bucket behavior. If a bug is exposed, update only `src/training/dataset.py` while preserving current public class/function names.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_training_dataset_contracts.py tests/test_losses.py -q</automated>
  </verify>
  <done>Masked-SFT loading, collator padding, and resolution bucket batching have CPU-safe characterization coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Characterize selection artifacts against dataset semantics</name>
  <files>tests/test_training_dataset_contracts.py, src/training/selection.py</files>
  <behavior>
    - Test 1: materialized SFT selections and `SFTDataset` threshold selection agree on included prompt/version IDs for the same tiny score CSV.
    - Test 2: materialized DPO pairs reject ambiguous or equal-score rows and preserve strict winner-over-loser semantics.
  </behavior>
  <action>Add boundary tests linking Phase 3 materialized selection behavior to existing dataset constructor semantics for `TEST-02`. Fix `src/training/selection.py` only if strict winner/loser or threshold behavior drifts from the Phase 3 contract.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_training_dataset_contracts.py tests/test_training_selection_artifacts.py -q</automated>
  </verify>
  <done>Dataset constructor semantics and materialized selection artifact semantics are cross-checked by tiny fixture tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tiny tensor fixtures→dataset loaders | Local `.pt` files cross into `torch.load` dataset paths. |
| scores CSV→selection/dataset logic | Local CSV rows drive sample and preference selection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | T | `torch.load` dataset fixtures | mitigate | Use trusted tiny tensor dictionaries and preserve `weights_only=True` loading. |
| T-04-02-02 | R | DPO pair semantics | mitigate | Assert strict winner-over-loser and margin decisions in tests. |
| T-04-02-03 | D | default pytest | mitigate | Fixtures live in `tmp_path`; tests do not access generated runtime roots. |
</threat_model>

<verification>
Run `PATH="/root/.local/bin:$PATH" uv run pytest tests/test_training_dataset_contracts.py tests/test_training_selection_artifacts.py tests/test_losses.py -q`.
</verification>

<success_criteria>
- `TEST-02` dataset, collator, selection, and path/shape behavior is covered by deterministic CPU-safe tests.
- No generated data, tensors, images, or runtime outputs are committed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cpu-safe-characterization-tests/04-02-SUMMARY.md`.
</output>
