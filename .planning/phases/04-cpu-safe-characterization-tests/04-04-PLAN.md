---
phase: 04-cpu-safe-characterization-tests
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_prompt_generation_determinism.py
  - src/prompt_pipeline/generate.py
  - src/data_quality/curriculum.py
autonomous: true
requirements: [TEST-04]
must_haves:
  truths:
    - "User can run deterministic prompt-generation tests under fixed seeds."
    - "User can verify prompt curriculum allocation and stage-family text policies without LLM/model backends."
  artifacts:
    - path: "tests/test_prompt_generation_determinism.py"
      provides: "Prompt generation determinism tests"
      min_lines: 100
    - path: "src/prompt_pipeline/generate.py"
      provides: "Config-driven prompt generation behavior under determinism tests"
    - path: "src/data_quality/curriculum.py"
      provides: "Prompt curriculum allocation behavior under determinism tests"
  key_links:
    - from: "tests/test_prompt_generation_determinism.py"
      to: "src/prompt_pipeline/generate.py"
      via: "_build_generation_plan, _apply_stage_text_policy, generate_dataset with fakes"
      pattern: "_build_generation_plan|generate_dataset"
    - from: "tests/test_prompt_generation_determinism.py"
      to: "src/data_quality/curriculum.py"
      via: "load_prompt_generation_config / PromptGenerationConfig allocation"
      pattern: "PromptGenerationConfig"
---

<objective>
Add CPU-safe deterministic prompt-generation characterization around fixed seeds, explicit configs, and curriculum stage policies.

Purpose: prevent prompt/data distribution drift before additional experiments rely on generated datasets.
Output: prompt determinism tests plus narrow fixes if config allocation or generation policies drift.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-cpu-safe-characterization-tests/04-CONTEXT.md
@.planning/phases/04-cpu-safe-characterization-tests/04-RESEARCH.md
@.planning/phases/04-cpu-safe-characterization-tests/04-PATTERNS.md
@.planning/phases/03-data-curriculum-and-dataset-quality/03-01-SUMMARY.md
@src/prompt_pipeline/generate.py
@src/data_quality/curriculum.py
@configs/prompts/simple.json
@configs/prompts/full.json
@configs/prompts/curriculum.json

<interfaces>
From `src.prompt_pipeline.generate`:
```python
def generate_dataset(n: int, output_path: str, llm=None, seed: int = 42, batch_size: int = 1, prompt_config: PromptGenerationConfig | None = None): ...
def _build_generation_plan(*, n: int, rng: random.Random, text_gen: TextGenerator, prompt_config: PromptGenerationConfig | None) -> list[dict[str, object]]: ...
def _apply_stage_text_policy(text: str, item: dict[str, object], text_gen: TextGenerator, rng: random.Random) -> str: ...
```

From `src.data_quality.curriculum`:
```python
class PromptGenerationConfig: ...
def load_prompt_generation_config(path: str | Path) -> PromptGenerationConfig: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Characterize curriculum allocation and stage metadata determinism</name>
  <files>tests/test_prompt_generation_determinism.py, src/data_quality/curriculum.py, src/prompt_pipeline/generate.py</files>
  <behavior>
    - Test 1: loading each committed prompt config is deterministic and returns stable stage names, families, and sample allocations.
    - Test 2: `_build_generation_plan` returns identical plans for identical seeds/configs and different order/choices for materially different seeds where random choices are involved.
    - Test 3: every config-driven plan item includes `stage_name` and `stage_family`; fallback legacy items are explicit when config sample counts do not fill `n`.
  </behavior>
  <action>Add tests first for `TEST-04`. Use a tiny fake text generator exposing `sample_tier` and `sample_case`; do not instantiate LLM clients. If behavior is nondeterministic, fix only `src/data_quality/curriculum.py` or `src/prompt_pipeline/generate.py` to make seed/config behavior stable.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_prompt_generation_determinism.py -x</automated>
  </verify>
  <done>Prompt curriculum allocation and generation-plan metadata are deterministic under fixed seeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Characterize stage-family text policies and config-driven record provenance</name>
  <files>tests/test_prompt_generation_determinism.py, src/prompt_pipeline/generate.py</files>
  <behavior>
    - Test 1: single-letter, digits, punctuation, mixed-case, and multiline stage policies transform text predictably under a seeded RNG.
    - Test 2: config-driven generated records include `prompt_mode`, `curriculum_stage`, and `curriculum_family` provenance fields.
    - Test 3: `--no-llm` / no-LLM paths do not import `LLMClient`, vLLM, MLX, Qwen, FLUX, or CUDA.
  </behavior>
  <action>Use monkeypatched lightweight `TextGenerator`, `StyleGenerator`, `ScenePool`, and `Assembler` fakes if full data files are unavailable. Preserve the real public `generate_dataset` signature and CLI behavior.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_prompt_generation_determinism.py tests/test_prompt_curriculum.py -q</automated>
  </verify>
  <done>Prompt generation under explicit configs produces deterministic provenance-tagged records without model backends.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prompt config→generation plan | Local JSON config controls generated prompt distribution. |
| fake generator components→record output | Test doubles stand in for data/model-heavy components. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | R | prompt determinism | mitigate | Assert fixed-seed config allocation and generation plan equality. |
| T-04-04-02 | T | prompt config paths | mitigate | Reuse existing config path validation; tests use committed configs and `tmp_path` outputs. |
| T-04-04-03 | D | no-LLM default tests | mitigate | Tests must not instantiate `LLMClient` or optional backends. |
</threat_model>

<verification>
Run `PATH="/root/.local/bin:$PATH" uv run pytest tests/test_prompt_generation_determinism.py tests/test_prompt_curriculum.py -q`.
</verification>

<success_criteria>
- `TEST-04` is covered by deterministic prompt-generation and curriculum tests.
- No LLM/model/OCR/GPU dependency is loaded by the new tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cpu-safe-characterization-tests/04-04-SUMMARY.md`.
</output>
