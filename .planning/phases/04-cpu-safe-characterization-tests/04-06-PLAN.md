---
phase: 04-cpu-safe-characterization-tests
plan: 06
type: execute
wave: 2
depends_on: [04-01, 04-02, 04-03, 04-04, 04-05]
files_modified:
  - tests/test_characterization_docs.py
  - docs/commands.md
  - docs/runtime_contracts.md
  - README.md
  - Makefile
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TRN-01]
must_haves:
  truths:
    - "User can discover and run the Phase 4 CPU-safe characterization suite from docs and Makefile aliases."
    - "User can distinguish default tests from optional slow/GPU/OCR/model/integration/manual diagnostics after Phase 4."
  artifacts:
    - path: "tests/test_characterization_docs.py"
      provides: "Docs/Makefile drift tests for characterization command surface"
      min_lines: 60
    - path: "docs/commands.md"
      provides: "Characterization test command documentation"
    - path: "docs/runtime_contracts.md"
      provides: "Runtime/test fixture contract updates"
    - path: "README.md"
      provides: "Front-door Phase 4 test guidance"
    - path: "Makefile"
      provides: "Characterization test aliases"
  key_links:
    - from: "Makefile"
      to: "tests/test_characterization_*.py and Phase 4 test files"
      via: "make characterization-test aliases"
      pattern: "characterization"
    - from: "tests/test_characterization_docs.py"
      to: "docs/commands.md"
      via: "required command strings"
      pattern: "CPU-safe characterization"
---

<objective>
Publish the Phase 4 characterization command surface and protect it with docs drift tests.

Purpose: make the completed CPU-safe tests discoverable and keep default pytest separate from optional heavy diagnostics.
Output: docs, Makefile aliases, and documentation tests.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-cpu-safe-characterization-tests/04-CONTEXT.md
@.planning/phases/04-cpu-safe-characterization-tests/04-RESEARCH.md
@.planning/phases/04-cpu-safe-characterization-tests/04-PATTERNS.md
@.planning/phases/04-cpu-safe-characterization-tests/04-01-SUMMARY.md
@.planning/phases/04-cpu-safe-characterization-tests/04-02-SUMMARY.md
@.planning/phases/04-cpu-safe-characterization-tests/04-03-SUMMARY.md
@.planning/phases/04-cpu-safe-characterization-tests/04-04-SUMMARY.md
@.planning/phases/04-cpu-safe-characterization-tests/04-05-SUMMARY.md
@docs/commands.md
@docs/runtime_contracts.md
@README.md
@Makefile
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add docs drift tests for characterization commands</name>
  <files>tests/test_characterization_docs.py, docs/commands.md, README.md, Makefile</files>
  <behavior>
    - Test 1: docs mention CPU-safe characterization tests for config/artifacts, datasets/collators, objective math/DPO, prompt determinism, and reward fakes.
    - Test 2: Makefile exposes aliases for full characterization and focused runtime/dataset/objective/prompt/reward characterization groups.
    - Test 3: docs continue to state that default pytest does not load CUDA, FLUX, Qwen, PaddleOCR, vLLM, MLX, or SynthTIGER.
  </behavior>
  <action>Write the docs test first and confirm RED against current docs. The test should search for concrete command strings and warnings, not comments in itself. Avoid self-invalidating grep gates; use Python path reads/assertions in pytest.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_characterization_docs.py -x</automated>
  </verify>
  <done>Documentation drift tests fail until the Phase 4 command surface is published.</done>
</task>

<task type="auto">
  <name>Task 2: Publish Phase 4 commands and runtime fixture guidance</name>
  <files>docs/commands.md, docs/runtime_contracts.md, README.md, Makefile</files>
  <action>Update docs and Makefile after Wave 1 plans exist. Add command examples for `uv run pytest` and focused files: `tests/test_characterization_config_artifacts.py`, `tests/test_training_dataset_contracts.py`, `tests/test_training_objective_math.py`, `tests/test_prompt_generation_determinism.py`, and `tests/test_reward_wrapper_contracts.py`. Add Makefile aliases that run these exact files and preserve existing Phase 1-3 aliases. Document tiny fixture rules, `tmp_path`, `weights_only=True`, and optional marker boundaries for slow/GPU/OCR/model/integration/manual checks. Do not document any command that downloads models or requires CUDA as part of default characterization.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_characterization_docs.py && make -n characterization-test characterization-runtime characterization-datasets characterization-objectives characterization-prompts characterization-rewards</automated>
  </verify>
  <done>Docs and Makefile expose discoverable CPU-safe Phase 4 characterization commands and clear heavy-diagnostic boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Verify full Phase 4 characterization surface</name>
  <files>tests/test_characterization_docs.py, docs/commands.md, docs/runtime_contracts.md, README.md, Makefile</files>
  <action>Run the docs tests, Makefile dry-run aliases, all Wave 1 Phase 4 test files, and the full default pytest suite. If any docs string or alias drifts, fix the docs/Makefile rather than weakening assertions.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_characterization_config_artifacts.py tests/test_training_dataset_contracts.py tests/test_training_objective_math.py tests/test_prompt_generation_determinism.py tests/test_reward_wrapper_contracts.py tests/test_characterization_docs.py && PATH="/root/.local/bin:$PATH" uv run pytest -q</automated>
  </verify>
  <done>All Phase 4 characterization tests are documented, Makefile-discoverable, and included in the passing default CPU-safe suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs→user command execution | Users copy documented commands into local/SLURM environments. |
| Makefile alias→test suite | Alias targets select which tests run by default/focus. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | I | docs | mitigate | State generated artifacts and private prompts remain out of git. |
| T-04-06-02 | D | Makefile aliases | mitigate | Dry-run aliases and tests ensure characterization aliases do not invoke GPU/model/OCR diagnostics. |
| T-04-06-03 | R | command docs | mitigate | Docs drift tests assert exact command surface and optional marker boundaries. |
</threat_model>

<verification>
Run docs tests, Makefile dry-run aliases, all Phase 4 focused tests, and full `PATH="/root/.local/bin:$PATH" uv run pytest -q`.
</verification>

<success_criteria>
- All Phase 4 requirements are discoverable through docs and Makefile aliases.
- Default test command remains CPU-safe and model-download-free.
- Optional slow/GPU/OCR/model/integration/manual diagnostics remain explicitly separated.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cpu-safe-characterization-tests/04-06-SUMMARY.md`.
</output>
