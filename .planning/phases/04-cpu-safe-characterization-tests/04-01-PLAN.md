---
phase: 04-cpu-safe-characterization-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_characterization_config_artifacts.py
  - src/runtime/config_io.py
  - src/runtime/artifacts.py
autonomous: true
requirements: [TEST-01, TEST-02]
must_haves:
  truths:
    - "User can run CPU-safe characterization tests for committed config parsing and validation."
    - "User can validate tiny artifact path/shape contracts without model downloads or CUDA."
  artifacts:
    - path: "tests/test_characterization_config_artifacts.py"
      provides: "Committed-config and tiny-artifact characterization tests"
      min_lines: 80
    - path: "src/runtime/config_io.py"
      provides: "Strict config validation behavior under characterization tests"
    - path: "src/runtime/artifacts.py"
      provides: "CPU-safe artifact validation behavior under tiny fixtures"
  key_links:
    - from: "tests/test_characterization_config_artifacts.py"
      to: "src/runtime/config_io.py"
      via: "load_stage_config / RuntimeConfigError assertions"
      pattern: "load_stage_config"
    - from: "tests/test_characterization_config_artifacts.py"
      to: "src/runtime/artifacts.py"
      via: "validate_artifacts with tmp_path fixtures"
      pattern: "validate_artifacts"
---

<objective>
Add CPU-safe characterization coverage for the real runtime config and artifact contracts that Phase 5 trainer/evaluation work will rely on.

Purpose: lock existing config validation and artifact path/shape behavior before high-risk trainer and reward refactors.
Output: a focused pytest module plus narrow runtime fixes only if tests expose contract drift.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-cpu-safe-characterization-tests/04-CONTEXT.md
@.planning/phases/04-cpu-safe-characterization-tests/04-RESEARCH.md
@.planning/phases/04-cpu-safe-characterization-tests/04-PATTERNS.md
@.planning/phases/02-runtime-contracts-and-run-provenance/02-01-SUMMARY.md
@.planning/phases/02-runtime-contracts-and-run-provenance/02-02-SUMMARY.md
@.planning/phases/02-runtime-contracts-and-run-provenance/02-04-SUMMARY.md
@src/runtime/config_io.py
@src/runtime/artifacts.py
@configs/sft.json
@configs/dpo.json
@configs/masked_sft.json

<interfaces>
From `src.runtime.config_io`:
```python
def load_stage_config(stage: str, path: str | Path) -> SFTConfig | DPOConfig | MaskedSFTConfig: ...
class RuntimeConfigError(ValueError): ...
```

From `src.runtime.artifacts`:
```python
def validate_artifacts(stage: str, paths: Mapping[str, str | Path] | None = None, *, require_ready: bool = False) -> ArtifactReport: ...
class ArtifactValidationError(RuntimeError): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Characterize committed training config validation</name>
  <files>tests/test_characterization_config_artifacts.py, src/runtime/config_io.py</files>
  <behavior>
    - Test 1: `configs/sft.json`, `configs/dpo.json`, and `configs/masked_sft.json` load through `load_stage_config` into their existing dataclass types.
    - Test 2: an invalid temporary config reports the failing field and config path through `RuntimeConfigError` without echoing raw secret-like values.
    - Test 3: unknown fields remain rejected rather than silently accepted.
  </behavior>
  <action>Write the tests first for `TEST-01`, run them to confirm RED only if a contract is currently missing, then update `src/runtime/config_io.py` only to preserve strict, secret-safe validation. Do not loosen validation to make malformed configs pass.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_characterization_config_artifacts.py -x</automated>
  </verify>
  <done>Committed SFT, DPO, and masked-SFT configs are covered by CPU-safe characterization tests, and malformed configs fail with field-specific secret-safe errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Characterize tiny artifact path and shape contracts</name>
  <files>tests/test_characterization_config_artifacts.py, src/runtime/artifacts.py</files>
  <behavior>
    - Test 1: temporary prompt JSONL, scores CSV, generated image/latent/text-embedding directories, and masked-SFT tensor layouts produce ready or warning reports as documented.
    - Test 2: generated tensor fixtures are tiny trusted local dictionaries and are inspected on CPU with `weights_only=True`.
    - Test 3: `require_ready=True` still raises `ArtifactValidationError` with aggregate blocking context.
  </behavior>
  <action>Add tiny `tmp_path` fixtures and assertions for `TEST-02`; if existing validators miss required path/shape details, update `src/runtime/artifacts.py` narrowly while preserving CPU-only behavior and aggregate reporting.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_characterization_config_artifacts.py tests/test_runtime_artifacts.py -q</automated>
  </verify>
  <done>Tiny artifact fixtures prove prompt, score, generated, and masked-SFT path/shape validators are CPU-safe and fail before expensive stages launch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture→runtime config loader | Local JSON config content crosses into strict Pydantic/dataclass validation. |
| test fixture→artifact validator | Local JSONL/CSV/PT paths cross into artifact inspection code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | I | `RuntimeConfigError` | mitigate | Assert secret-like invalid values are not echoed in config error messages. |
| T-04-01-02 | T | `validate_artifacts` tensor inspection | mitigate | Use tiny trusted local tensor dictionaries and preserve `torch.load(..., map_location="cpu", weights_only=True)`. |
| T-04-01-03 | D | default pytest | mitigate | Tests must not import or instantiate CUDA/model/OCR stacks. |
</threat_model>

<verification>
Run targeted tests first, then `PATH="/root/.local/bin:$PATH" uv run pytest tests/test_characterization_config_artifacts.py tests/test_runtime_config_io.py tests/test_runtime_artifacts.py -q`.
</verification>

<success_criteria>
- `TEST-01` is covered by real config-loader characterization tests.
- `TEST-02` artifact path/shape coverage exists for tiny CPU-safe fixtures.
- No generated runtime artifacts are committed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cpu-safe-characterization-tests/04-01-SUMMARY.md`.
</output>
