---
phase: 04-cpu-safe-characterization-tests
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_reward_wrapper_contracts.py
  - src/training/rewards.py
  - scripts/score_images.py
autonomous: true
requirements: [TEST-05]
must_haves:
  truths:
    - "User can run reward wrapper tests with fakes/mocks and no Qwen/PaddleOCR model loading."
    - "Importing reward wrappers during default pytest does not require optional OCR/model packages."
  artifacts:
    - path: "tests/test_reward_wrapper_contracts.py"
      provides: "Fake-based reward wrapper characterization tests"
      min_lines: 100
    - path: "src/training/rewards.py"
      provides: "Import-safe reward helper and wrapper behavior"
    - path: "scripts/score_images.py"
      provides: "Scoring boundary remains compatible after reward import-safety changes"
  key_links:
    - from: "tests/test_reward_wrapper_contracts.py"
      to: "src/training/rewards.py"
      via: "fake Qwen/OCR wrapper tests"
      pattern: "QwenYesProbReward|OcrCerEntropyReward"
    - from: "scripts/score_images.py"
      to: "src/training/rewards.py"
      via: "lazy reward class imports inside scorer selection"
      pattern: "from src.training.rewards import"
---

<objective>
Make reward wrapper behavior testable in the CPU-safe default suite using fakes/mocks instead of real Qwen or PaddleOCR stacks.

Purpose: prevent reward drift and optional-dependency import failures before Phase 6 reward centralization work.
Output: import-safe reward module behavior and fake-based wrapper tests.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-cpu-safe-characterization-tests/04-CONTEXT.md
@.planning/phases/04-cpu-safe-characterization-tests/04-RESEARCH.md
@.planning/phases/04-cpu-safe-characterization-tests/04-PATTERNS.md
@src/training/rewards.py
@scripts/score_images.py

<interfaces>
From `src.training.rewards`:
```python
class QwenYesProbReward:
    PROMPT_TEMPLATE: str
    SYSTEM_PROMPT: str
    def score_single(self, image_tensor: torch.Tensor, target_text: str) -> torch.Tensor: ...
    def score_batch(self, images: torch.Tensor, target_texts: list[str]) -> torch.Tensor: ...

class OcrCerEntropyReward:
    def score(self, image_path: str, target_text: str) -> dict: ...
    def score_pil(self, image: Image.Image, target_text: str) -> dict: ...

def _normalize_homoglyphs(text: str) -> str: ...
def _char_error_rate(hypothesis_lines, reference_words) -> float: ...
def _ctc_entropy_stats(preds_np) -> dict: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove reward module import safety and pure helper behavior</name>
  <files>tests/test_reward_wrapper_contracts.py, src/training/rewards.py</files>
  <behavior>
    - Test 1: `import src.training.rewards` succeeds without `paddleocr`, `paddlex`, Qwen, CUDA, or model weights being imported or installed.
    - Test 2: `_normalize_homoglyphs` maps Latin/Cyrillic lookalikes consistently.
    - Test 3: `_char_error_rate` and `_ctc_entropy_stats` return deterministic values for tiny fake inputs.
  </behavior>
  <action>Write tests first. The intended RED is the current import-time Paddle/PaddleX dependency in `src/training/rewards.py` if optional OCR packages are unavailable. Move OCR/Paddle imports and CTC monkey-patching behind `OcrCerEntropyReward.__init__` or a lazy setup helper; do not remove the OCR formula or Qwen wrapper contracts.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_reward_wrapper_contracts.py -x</automated>
  </verify>
  <done>`src.training.rewards` is import-safe for default tests, and pure reward helpers are characterized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Characterize Qwen and OCR wrappers with fakes</name>
  <files>tests/test_reward_wrapper_contracts.py, src/training/rewards.py</files>
  <behavior>
    - Test 1: a fake `QwenYesProbReward` instance with a fake `score_single` proves `score_batch` preserves order and returns a tensor without model initialization.
    - Test 2: a fake OCR object plus seeded raw CTC predictions proves `OcrCerEntropyReward.score` computes CER, entropy, and `reward_ocr` deterministically.
    - Test 3: `score_pil` uses a temporary PNG and deletes it after fake scoring.
  </behavior>
  <action>Use fakes, monkeypatches, and `object.__new__` as needed. Do not instantiate real Qwen, Transformers model classes, PaddleOCR, or CUDA devices. Keep wrapper public method names compatible with `scripts/score_images.py`.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_reward_wrapper_contracts.py -q</automated>
  </verify>
  <done>Reward wrapper behavior is covered by fake-based CPU-safe tests for `TEST-05`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Guard scoring script compatibility after reward import changes</name>
  <files>tests/test_reward_wrapper_contracts.py, scripts/score_images.py</files>
  <behavior>
    - Test 1: `scripts.score_images` remains importable without instantiating reward models.
    - Test 2: reward class imports remain inside scorer selection paths so import-only test collection stays CPU-safe.
  </behavior>
  <action>Add import-safety tests for `scripts.score_images`. Modify `scripts/score_images.py` only if reward imports moved to module scope or if compatibility with the import-safe reward module requires a narrow adjustment.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_reward_wrapper_contracts.py && PATH="/root/.local/bin:$PATH" uv run pytest -q</automated>
  </verify>
  <done>Default pytest can collect reward and scoring code without optional model/OCR stacks, while scoring CLI behavior remains compatible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fake model/OCR output→reward wrappers | Test doubles emulate untrusted external scorer outputs. |
| optional dependency imports→default pytest | Optional model/OCR packages must not be required at import/collection time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | D | `src.training.rewards` import | mitigate | Lazy-load OCR/Paddle dependencies and assert import safety. |
| T-04-05-02 | T | fake OCR outputs | mitigate | Use deterministic tiny arrays and explicit formula assertions. |
| T-04-05-03 | R | reward semantics | mitigate | Characterize CER, entropy, homoglyph normalization, and batch ordering. |
</threat_model>

<verification>
Run `PATH="/root/.local/bin:$PATH" uv run pytest tests/test_reward_wrapper_contracts.py -q`, then full `PATH="/root/.local/bin:$PATH" uv run pytest -q`.
</verification>

<success_criteria>
- `TEST-05` is satisfied with fake/mock-based reward wrapper tests.
- Reward and scoring modules remain import-safe for default CPU pytest.
- No real Qwen/PaddleOCR/model/CUDA load is introduced in tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cpu-safe-characterization-tests/04-05-SUMMARY.md`.
</output>
