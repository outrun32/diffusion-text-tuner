---
phase: 01-execution-surface-and-pipeline-inventory
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - scripts/smoke_environment.py
  - tests/test_smoke_environment.py
autonomous: true
requirements:
  - ENV-04
  - TEST-06
  - TEST-07
must_haves:
  truths:
    - "User can list explicit smoke checks without loading CUDA, FLUX, Qwen, PaddleOCR, or SynthTIGER."
    - "User can run an import-safe smoke check for Python/project/module availability."
    - "User can explicitly choose CUDA, model-access, OCR, and cache/runtime path checks before launching long jobs."
  artifacts:
    - path: "scripts/smoke_environment.py"
      provides: "Smoke-check CLI for imports, CUDA, model access, OCR, and cache/runtime paths"
      exports: ["main"]
    - path: "tests/test_smoke_environment.py"
      provides: "CPU-safe tests proving smoke CLI command behavior"
      contains: "test_list_outputs_all_checks"
  key_links:
    - from: "tests/test_smoke_environment.py"
      to: "scripts/smoke_environment.py"
      via: "imports CLI helper functions without model loading"
      pattern: "from scripts\.smoke_environment import"
---

<objective>
Add explicit, tested smoke-check commands for environment readiness while keeping default tests CPU-safe.

Purpose: Users need preflight checks for imports, CUDA, Hugging Face model access/cache, PaddleOCR, and runtime paths before expensive jobs; those checks must be opt-in and testable without model downloads.
Output: `scripts/smoke_environment.py` plus `tests/test_smoke_environment.py`.
</objective>

<execution_context>
@/root/.config/opencode/get-shit-done/workflows/execute-plan.md
@/root/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/INTEGRATIONS.md
@.planning/phases/01-execution-surface-and-pipeline-inventory/01-RESEARCH.md
@.planning/phases/01-execution-surface-and-pipeline-inventory/01-PATTERNS.md
@pyproject.toml
@tests/test_losses.py

<interfaces>
New `scripts/smoke_environment.py` must expose these importable functions for tests:

```python
CHECKS = ("imports", "cuda", "cache", "model-access", "ocr")

def build_parser() -> argparse.ArgumentParser: ...
def list_checks() -> str: ...
def run_check(name: str, *, allow_missing: bool = False) -> int: ...
def main(argv: list[str] | None = None) -> int: ...
```

`main(["--list"])` returns `0` and prints all check names. `main(["--check", "imports"])` performs import-safe checks only and returns `0` when required lightweight modules are discoverable.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write CPU-safe smoke CLI tests first</name>
  <files>tests/test_smoke_environment.py</files>
  <read_first>tests/test_losses.py, pyproject.toml, .planning/codebase/TESTING.md</read_first>
  <behavior>
    - Test 1: `list_checks()` output contains `imports`, `cuda`, `cache`, `model-access`, and `ocr`.
    - Test 2: `main(["--list"])` returns `0` and writes all check names to stdout.
    - Test 3: `run_check("not-a-check")` returns nonzero and does not raise an unhandled exception.
    - Test 4: importing `scripts.smoke_environment` does not import `torch`, `diffusers`, `transformers`, `paddleocr`, `vllm`, or `mlx_lm` as a side effect.
  </behavior>
  <action>Create `tests/test_smoke_environment.py` before implementing the production script. The tests must use `capsys` and `sys.modules` assertions, must not require CUDA, model downloads, PaddleOCR, vLLM, MLX, or SynthTIGER, and must import only `scripts.smoke_environment` plus standard-library/pytest helpers. Run `uv run pytest tests/test_smoke_environment.py` and confirm the tests fail because `scripts.smoke_environment` is missing or incomplete; record the RED result in the summary.</action>
  <acceptance_criteria>`tests/test_smoke_environment.py` contains tests named `test_list_outputs_all_checks`, `test_main_list_prints_checks`, `test_unknown_check_returns_nonzero`, and `test_import_has_no_heavy_side_effects`; the initial test run produces a RED result caused by the missing/incomplete smoke module.</acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; text=Path('tests/test_smoke_environment.py').read_text(); assert all(s in text for s in ['test_list_outputs_all_checks','test_main_list_prints_checks','test_unknown_check_returns_nonzero','test_import_has_no_heavy_side_effects'])" && (uv run pytest tests/test_smoke_environment.py && exit 1 || exit 0)</automated>
  </verify>
  <done>RED-stage smoke CLI tests exist and fail for the intended missing implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement explicit smoke-check CLI</name>
  <files>scripts/smoke_environment.py, tests/test_smoke_environment.py</files>
  <read_first>tests/test_smoke_environment.py, scripts/__init__.py, .planning/codebase/CONVENTIONS.md, .planning/codebase/INTEGRATIONS.md</read_first>
  <behavior>
    - `python -m scripts.smoke_environment --list` exits `0` and prints all available check names.
    - `python -m scripts.smoke_environment --check imports` exits `0` when Python version and lightweight package/module discovery pass.
    - `python -m scripts.smoke_environment --check cuda` imports `torch` only inside the CUDA check and reports `torch.cuda.is_available()`.
    - `python -m scripts.smoke_environment --check model-access --allow-missing` checks Hugging Face-related package availability/cache env vars without loading FLUX or Qwen models.
    - `python -m scripts.smoke_environment --check ocr --allow-missing` checks PaddleOCR package discoverability without constructing OCR models.
  </behavior>
  <action>Implement `scripts/smoke_environment.py` with `from __future__ import annotations`, `argparse`, `importlib.util.find_spec`, `os`, `sys`, and `pathlib`. Expose `CHECKS = ("imports", "cuda", "cache", "model-access", "ocr")`, `build_parser()`, `list_checks()`, `run_check(name: str, *, allow_missing: bool = False) -> int`, and `main(argv: list[str] | None = None) -> int`. Use `find_spec` for package presence checks. The `imports` check must verify Python 3.11 and discoverability of local packages/modules `src`, `scripts`, `src.training.losses`, `src.training.config`, `src.prompt_pipeline.generate`, and `scripts.generate_images` without importing heavy ML modules. The `cache` check must print `HF_HOME`, `HF_HUB_CACHE`, `TRANSFORMERS_CACHE`, `outputs/`, and `runs/` path status without printing secret values. The `model-access` check must report Hugging Face cache/env presence and package discoverability but must not call `from_pretrained`. The `ocr` check must report PaddleOCR package discoverability but must not instantiate `PaddleOCR`. The `cuda` check may import `torch` inside that function only. Return nonzero for missing requirements unless `--allow-missing` is supplied. End with `if __name__ == "__main__": raise SystemExit(main())`.</action>
  <acceptance_criteria>`scripts/smoke_environment.py` defines `CHECKS`, `build_parser`, `list_checks`, `run_check`, and `main`; no top-level line imports `torch`, `diffusers`, `transformers`, `paddleocr`, `vllm`, or `mlx_lm`; `uv run pytest tests/test_smoke_environment.py` exits 0; `uv run python -m scripts.smoke_environment --list` exits 0.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_smoke_environment.py && uv run python -m scripts.smoke_environment --list && uv run python -m scripts.smoke_environment --check imports</automated>
  </verify>
  <done>GREEN-stage smoke CLI implementation satisfies ENV-04 and keeps optional diagnostics explicit for TEST-06 and TEST-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI user→smoke script | User-supplied check names select which environment inspection code runs. |
| Smoke script→local environment | Script reads package availability, cache path env vars, CUDA availability, and local filesystem path presence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | I | `scripts/smoke_environment.py` environment reporting | mitigate | Report only presence/path names for Hugging Face/cache env vars; never print token or secret values; use `find_spec` and avoid `from_pretrained` in smoke checks. |
| T-01-06 | D | CUDA/model/OCR smoke commands | mitigate | Keep heavy checks behind explicit `--check cuda`, `--check model-access`, and `--check ocr`; default `--list` and import-safe tests do not load models. |
</threat_model>

<verification>
Run RED test first for Task 1, then run the GREEN command in Task 2. After implementation, run `uv run pytest` to ensure the new smoke tests coexist with existing CPU-safe tests.
</verification>

<success_criteria>
- `scripts/smoke_environment.py` is import-safe.
- Tests prove listing, unknown-check handling, and no heavy import side effects.
- Explicit smoke checks exist for imports, CUDA, cache paths, model access, and OCR.
- Default pytest remains CPU-safe.
</success_criteria>

<output>
After completion, create `.planning/phases/01-execution-surface-and-pipeline-inventory/01-03-SUMMARY.md`.
</output>
