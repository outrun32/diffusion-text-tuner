---
phase: 01-execution-surface-and-pipeline-inventory
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .python-version
  - pyproject.toml
  - uv.lock
autonomous: true
requirements:
  - ENV-01
  - ENV-02
  - ENV-03
  - ENV-05
  - TEST-06
must_haves:
  truths:
    - "User can install Python 3.11 dependencies from committed manifests."
    - "User can choose optional dependency groups for GPU, OCR/reward, synthesis, vLLM, MLX, tests, linting, plotting, and analysis."
    - "User can run a standard CPU-safe pytest command and Ruff lint/format commands from committed configuration."
  artifacts:
    - path: ".python-version"
      provides: "Python runtime pin"
      contains: "3.11"
    - path: "pyproject.toml"
      provides: "Project metadata, optional dependency groups, pytest config, and Ruff config"
      contains: "[project.optional-dependencies]"
    - path: "uv.lock"
      provides: "Resolved dependency lock"
      contains: "version = 1"
  key_links:
    - from: "pyproject.toml"
      to: "tests/"
      via: "pytest testpaths"
      pattern: "testpaths.*tests"
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "uv lock resolution"
      pattern: "uv lock --check"
---

<objective>
Add the committed Python 3.11 dependency, test, lint, and formatting contract for the research toolkit.

Purpose: Every later phase depends on repeatable installation and CPU-safe automation; Phase 1 must replace dependency prose with committed tooling surfaces.
Output: `.python-version`, `pyproject.toml`, and `uv.lock`.
</objective>

<execution_context>
@/root/.config/opencode/get-shit-done/workflows/execute-plan.md
@/root/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/01-execution-surface-and-pipeline-inventory/01-RESEARCH.md
@.planning/phases/01-execution-surface-and-pipeline-inventory/01-PATTERNS.md
@README.md
@tests/test_losses.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Python 3.11 uv project manifest with optional groups</name>
  <files>.python-version, pyproject.toml</files>
  <read_first>README.md, .gitignore, .planning/codebase/STACK.md, .planning/codebase/CONCERNS.md, .planning/research/SUMMARY.md</read_first>
  <action>Create `.python-version` containing exactly `3.11`. Create `pyproject.toml` with `[project]` name `diffusion-text-tuner`, version `0.1.0`, description `Brownfield thesis toolkit for diffusion-based multilingual text rendering`, `requires-python = ">=3.11,<3.12"`, and baseline dependencies needed for importable project/runtime utilities: `accelerate`, `diffusers`, `transformers`, `peft`, `tqdm`, `pillow`, and `datasets`. Add `[project.optional-dependencies]` groups named exactly `gpu`, `ocr`, `reward`, `synthesis`, `vllm`, `mlx`, `test`, `lint`, `plotting`, and `analysis`. Populate them with concrete packages: `gpu` has `torch`, `torchvision`, `bitsandbytes`; `ocr` has `paddleocr`; `reward` has `paddleocr`, `bitsandbytes`; `synthesis` has `synthtiger`, `opencv-python`, `requests`; `vllm` has `vllm`; `mlx` has `mlx-lm`; `test` has `pytest`, `pytest-cov`; `lint` has `ruff`; `plotting` has `matplotlib`, `pandas`; `analysis` has `numpy`, `pandas`, `scipy`, `scikit-learn`. Add `[tool.pytest.ini_options]` with `testpaths = ["tests"]`, `python_files = ["test_*.py"]`, `addopts = "--strict-markers"`, and markers `slow`, `gpu`, `model`, `ocr`, `integration`, `manual`. Add `[tool.ruff]` with `target-version = "py311"` and `line-length = 100`, plus `[tool.ruff.lint] select = ["E", "F", "I", "UP", "B"]`. Do not add generated artifacts or secrets.</action>
  <acceptance_criteria>`.python-version` equals `3.11`; `pyproject.toml` contains every optional group name listed in the action; pytest `testpaths` is exactly `tests`; Ruff target version is `py311`.</acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; import tomllib; assert Path('.python-version').read_text().strip() == '3.11'; data=tomllib.loads(Path('pyproject.toml').read_text()); optional=data['project']['optional-dependencies']; required=['gpu','ocr','reward','synthesis','vllm','mlx','test','lint','plotting','analysis']; missing=[g for g in required if g not in optional]; assert not missing, missing; assert data['tool']['pytest']['ini_options']['testpaths'] == ['tests']; assert data['tool']['ruff']['target-version'] == 'py311'"</automated>
  </verify>
  <done>The repository has a committed Python 3.11 manifest with explicit optional dependency groups for ENV-01 and ENV-02.</done>
</task>

<task type="auto">
  <name>Task 2: Lock dependencies and verify CPU-safe tooling configuration</name>
  <files>uv.lock, pyproject.toml</files>
  <read_first>pyproject.toml, tests/test_losses.py, .planning/codebase/TESTING.md</read_first>
  <action>Generate and commit `uv.lock` from `pyproject.toml` using uv. If uv is not installed, install or invoke it with a standard Python-safe mechanism available in the environment, then run `uv lock`. After the lock exists, run `uv lock --check`, `uv run pytest --collect-only`, and `uv run pytest`. The collect-only output must discover formal tests under `tests/` and must not collect `scripts/test_gradient_flow.py`, `scripts/test_grad_magnitude.py`, or `experiments/ocr_reward_tests/test_*.py`. If `uv run pytest` exposes an existing import/runtime issue unrelated to Phase 1 config, keep the pyproject testpaths restriction and document the failure in the summary instead of broadening discovery.</action>
  <acceptance_criteria>`uv.lock` exists; `uv lock --check` exits 0; `uv run pytest --collect-only` output contains `tests/test_losses.py`; collect-only output does not contain `scripts/test_gradient_flow.py`, `scripts/test_grad_magnitude.py`, or `experiments/ocr_reward_tests`.</acceptance_criteria>
  <verify>
    <automated>uv lock --check && uv run pytest --collect-only && uv run pytest</automated>
  </verify>
  <done>The lockfile and CPU-safe pytest discovery contract satisfy ENV-03, ENV-05, and TEST-06 foundation needs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer→dependency resolver | `uv lock` resolves third-party packages into a committed lockfile. |
| Pytest discovery→local machine | Test discovery must not trigger GPU/model/OCR diagnostics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | T | `pyproject.toml` dependency groups | mitigate | Keep heavyweight packages in optional groups named `gpu`, `ocr`, `reward`, `synthesis`, `vllm`, and `mlx`; default install does not require users to run model/OCR stacks. |
| T-01-04 | D | pytest discovery | mitigate | Set `testpaths = ["tests"]` and strict markers so default test runs do not collect `scripts/` or `experiments/ocr_reward_tests/` diagnostics. |
</threat_model>

<verification>
Run `uv lock --check`, `uv run pytest --collect-only`, and `uv run pytest`. Do not run CUDA/model/OCR smoke checks in this plan.
</verification>

<success_criteria>
- `.python-version`, `pyproject.toml`, and `uv.lock` exist.
- Optional dependency groups cover GPU, OCR/reward, synthesis, vLLM, MLX, test, lint, plotting, and analysis workflows.
- Default pytest discovery is CPU-safe and restricted to `tests/`.
- Ruff lint/format configuration exists for later command catalog wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/01-execution-surface-and-pipeline-inventory/01-02-SUMMARY.md`.
</output>
