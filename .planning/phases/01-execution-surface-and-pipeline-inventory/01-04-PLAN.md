---
phase: 01-execution-surface-and-pipeline-inventory
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - README.md
  - Makefile
  - docs/commands.md
  - scripts/test_gradient_flow.py
  - scripts/test_grad_magnitude.py
  - scripts/diagnose_gradient_flow.py
  - scripts/diagnose_grad_magnitude.py
autonomous: true
requirements:
  - INV-01
  - INV-02
  - INV-03
  - INV-04
  - ENV-03
  - ENV-04
  - ENV-05
  - ENV-06
  - TEST-06
  - TEST-07
must_haves:
  truths:
    - "User can run documented CPU-safe test, lint, and format commands."
    - "User can find explicit smoke and diagnostic commands separately from default automation."
    - "User can compare local and SLURM variants of supported generation, scoring, SFT, DPO, masked-SFT, synthesis, evaluation, and plotting flows."
  artifacts:
    - path: "docs/commands.md"
      provides: "Standard command catalog with setup, tests, smokes, local commands, SLURM commands, and diagnostics"
      contains: "## CPU-safe default commands"
    - path: "Makefile"
      provides: "Short aliases for setup/test/lint/format/smoke commands"
      contains: "smoke-imports"
    - path: "README.md"
      provides: "Front-door links to inventory and commands"
      contains: "docs/pipeline_inventory.md"
  key_links:
    - from: "Makefile"
      to: "scripts/smoke_environment.py"
      via: "smoke-* targets"
      pattern: "python -m scripts\.smoke_environment"
    - from: "docs/commands.md"
      to: "scripts/cluster/*.sbatch"
      via: "SLURM command catalog"
      pattern: "sbatch scripts/cluster"
---

<objective>
Publish the standard command surface and separate manual diagnostics from default tests.

Purpose: Phase 1 is complete only when users can install, test, lint/format, smoke-check, and run local/SLURM variants from documented, comparable commands without accidentally launching expensive diagnostics.
Output: `docs/commands.md`, `Makefile`, README links/quick start updates, and renamed manual gradient diagnostics.
</objective>

<execution_context>
@/root/.config/opencode/get-shit-done/workflows/execute-plan.md
@/root/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-execution-surface-and-pipeline-inventory/01-RESEARCH.md
@.planning/phases/01-execution-surface-and-pipeline-inventory/01-PATTERNS.md
@docs/pipeline_inventory.md
@pyproject.toml
@scripts/smoke_environment.py
@README.md
@scripts/test_gradient_flow.py
@scripts/test_grad_magnitude.py
@scripts/cluster/generate_images.sbatch
@scripts/cluster/score_images.sbatch
@scripts/cluster/sft.sbatch
@scripts/cluster/dpo.sbatch
@scripts/cluster/setup_env.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename manual gradient diagnostics and guard execution</name>
  <files>scripts/test_gradient_flow.py, scripts/test_grad_magnitude.py, scripts/diagnose_gradient_flow.py, scripts/diagnose_grad_magnitude.py</files>
  <read_first>scripts/test_gradient_flow.py, scripts/test_grad_magnitude.py, .planning/codebase/TESTING.md, .planning/codebase/CONCERNS.md</read_first>
  <action>Move `scripts/test_gradient_flow.py` to `scripts/diagnose_gradient_flow.py` and `scripts/test_grad_magnitude.py` to `scripts/diagnose_grad_magnitude.py`. Preserve their existing diagnostic behavior, but ensure each new file only executes GPU/model work from an explicit `main()` guarded by `if __name__ == "__main__": raise SystemExit(main())`. If existing code executes work at import time, wrap that code in `main()` without changing its diagnostic logic. Delete the old `scripts/test_gradient_flow.py` and `scripts/test_grad_magnitude.py` paths so pytest-style broad filename matching cannot discover them as tests. Do not modify `experiments/ocr_reward_tests/test_*.py` in this task; those remain classified/documented experiments and are excluded by `pyproject.toml` testpaths.</action>
  <acceptance_criteria>`scripts/diagnose_gradient_flow.py` and `scripts/diagnose_grad_magnitude.py` exist; old `scripts/test_gradient_flow.py` and `scripts/test_grad_magnitude.py` do not exist; both new files contain `def main(` and `if __name__ == "__main__"`; `uv run pytest --collect-only` output does not include `diagnose_gradient_flow`, `diagnose_grad_magnitude`, `scripts/test_gradient_flow.py`, or `scripts/test_grad_magnitude.py`.</acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; assert Path('scripts/diagnose_gradient_flow.py').exists(); assert Path('scripts/diagnose_grad_magnitude.py').exists(); assert not Path('scripts/test_gradient_flow.py').exists(); assert not Path('scripts/test_grad_magnitude.py').exists(); text1=Path('scripts/diagnose_gradient_flow.py').read_text(); text2=Path('scripts/diagnose_grad_magnitude.py').read_text(); assert 'def main(' in text1 and 'if __name__ == \"__main__\"' in text1; assert 'def main(' in text2 and 'if __name__ == \"__main__\"' in text2" && uv run pytest --collect-only</automated>
  </verify>
  <done>Manual gradient diagnostics are opt-in scripts rather than default test-discovery candidates, satisfying TEST-06 and TEST-07.</done>
</task>

<task type="auto">
  <name>Task 2: Add command catalog, Makefile aliases, and README links</name>
  <files>docs/commands.md, Makefile, README.md</files>
  <read_first>docs/pipeline_inventory.md, pyproject.toml, scripts/smoke_environment.py, README.md, scripts/cluster/generate_images.sbatch, scripts/cluster/score_images.sbatch, scripts/cluster/sft.sbatch, scripts/cluster/dpo.sbatch, scripts/cluster/setup_env.sh</read_first>
  <action>Create `docs/commands.md` with title `# Command Catalog`. Include sections `## Setup`, `## CPU-safe default commands`, `## Smoke checks`, `## Local pipeline commands`, `## SLURM command variants`, `## Manual diagnostics`, and `## Generated artifacts and git safety`. The CPU-safe section must list `uv run pytest`, `uv run ruff check .`, and `uv run ruff format --check .`. The smoke section must list `python -m scripts.smoke_environment --check imports`, `--check cuda --allow-missing`, `--check model-access --allow-missing`, `--check ocr --allow-missing`, and `--check cache --allow-missing`. The local pipeline section must include concrete commands for prompt generation, `python -m scripts.generate_images`, `python -m scripts.score_images`, SFT, DPO, masked-SFT, synthetic data, evaluation, and thesis plotting. The SLURM section must include `bash scripts/cluster/setup_env.sh`, `sbatch --array=0-15 scripts/cluster/generate_images.sbatch`, `sbatch --array=0-7 scripts/cluster/score_images.sbatch`, `bash scripts/cluster/merge_scores.sh`, `sbatch scripts/cluster/sft.sbatch`, and `sbatch scripts/cluster/dpo.sbatch`. Create a `Makefile` with phony targets `setup`, `test`, `lint`, `format`, `smoke-imports`, `smoke-cuda`, `smoke-model-access`, `smoke-ocr`, and `smoke-cache` that wrap the uv/Ruff/pytest/smoke commands. Update `README.md` near the top with a `## Execution Surface` section linking `docs/pipeline_inventory.md` and `docs/commands.md`, stating that default automated tests are CPU-safe and GPU/model/OCR diagnostics are explicit opt-in commands.</action>
  <acceptance_criteria>`docs/commands.md` contains every section name and command listed in the action; `Makefile` contains every phony target listed in the action; `README.md` contains `## Execution Surface`, `docs/pipeline_inventory.md`, `docs/commands.md`, `CPU-safe`, and `opt-in`.</acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; docs=Path('docs/commands.md').read_text(); make=Path('Makefile').read_text(); readme=Path('README.md').read_text(); required_docs=['## Setup','## CPU-safe default commands','## Smoke checks','## Local pipeline commands','## SLURM command variants','## Manual diagnostics','uv run pytest','uv run ruff check .','python -m scripts.smoke_environment --check imports','sbatch --array=0-15 scripts/cluster/generate_images.sbatch','sbatch scripts/cluster/sft.sbatch']; missing=[s for s in required_docs if s not in docs]; assert not missing, missing; targets=['setup','test','lint','format','smoke-imports','smoke-cuda','smoke-model-access','smoke-ocr','smoke-cache']; missing_targets=[t for t in targets if f'{t}:' not in make]; assert not missing_targets, missing_targets; required_readme=['## Execution Surface','docs/pipeline_inventory.md','docs/commands.md','CPU-safe','opt-in']; missing_readme=[s for s in required_readme if s not in readme]; assert not missing_readme, missing_readme" && make -n test lint format smoke-imports smoke-cuda smoke-model-access smoke-ocr smoke-cache</automated>
  </verify>
  <done>Users have documented CPU-safe defaults, opt-in smokes/diagnostics, and comparable local/SLURM command variants for ENV-03 through ENV-06 and TEST-06/TEST-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User→Makefile/docs commands | Users run commands copied from docs and Makefile targets. |
| Docs→SLURM/local runtime | Commands may launch GPU/model/OCR/cluster work when explicitly selected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | D | `docs/commands.md` and `Makefile` | mitigate | Separate CPU-safe default commands from `smoke-*` and manual diagnostics; label GPU/model/OCR commands as explicit opt-in. |
| T-01-08 | I | README/docs artifact guidance | mitigate | Include generated artifact/git-safety notes so outputs, runs, logs, tensors, checkpoints, and generated images are not committed. |
</threat_model>

<verification>
Run both task automated checks plus `uv run pytest` after the old diagnostic filenames are removed.
</verification>

<success_criteria>
- Manual gradient diagnostics have non-test filenames and guarded entry points.
- `docs/commands.md` covers setup, CPU-safe tests, lint/format, smokes, local flows, SLURM variants, diagnostics, and git safety.
- `Makefile` provides short aliases for common safe commands and explicit smokes.
- README points users to the inventory and command catalog and explains CPU-safe versus opt-in diagnostics.
</success_criteria>

<output>
After completion, create `.planning/phases/01-execution-surface-and-pipeline-inventory/01-04-SUMMARY.md`.
</output>
