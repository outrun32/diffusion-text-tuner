---
phase: 01-execution-surface-and-pipeline-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/pipeline_inventory.md
autonomous: true
requirements:
  - INV-01
  - INV-02
  - INV-03
  - INV-04
must_haves:
  truths:
    - "User can open `docs/pipeline_inventory.md` and see all supported runnable pipeline families."
    - "User can distinguish supported entry points from outdated, duplicate, experimental, and manual diagnostic scripts."
    - "User can trace historical experiment tracks for reward-filtered SFT/DPO, synthetic masked-MSE, OCR/VLM/product rewards, and thesis plotting/report flows."
  artifacts:
    - path: "docs/pipeline_inventory.md"
      provides: "Pipeline inventory, classifications, inputs, outputs, optimization targets, thesis support, and historical tracks"
      contains: "Supported toolkit entry points"
  key_links:
    - from: "docs/pipeline_inventory.md"
      to: "README.md"
      via: "documents current README commands without changing them"
      pattern: "scripts\.generate_images|scripts\.score_images|src\.training\.sft_trainer|src\.training\.dpo_trainer"
---

<objective>
Create the Phase 1 execution inventory so users can understand what already runs before installing or changing anything.

Purpose: Phase 1 starts by making the brownfield toolkit discoverable; later tooling and command work depends on knowing which entry points are supported, historical, experimental, or manual diagnostics.
Output: `docs/pipeline_inventory.md` with concrete tables covering pipeline families, artifact flow, supported entry points, diagnostics, experiments, and historical tracks.
</objective>

<execution_context>
@/root/.config/opencode/get-shit-done/workflows/execute-plan.md
@/root/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/STACK.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-execution-surface-and-pipeline-inventory/01-RESEARCH.md
@.planning/phases/01-execution-surface-and-pipeline-inventory/01-PATTERNS.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document supported pipeline families</name>
  <files>docs/pipeline_inventory.md</files>
  <read_first>README.md, .planning/codebase/ARCHITECTURE.md, .planning/codebase/STRUCTURE.md, .planning/codebase/STACK.md, .planning/REQUIREMENTS.md</read_first>
  <action>Create `docs/pipeline_inventory.md` with title `# Pipeline Inventory`. Add a section `## Supported toolkit entry points` containing a markdown table with exactly these pipeline family rows: `Prompt generation`, `Dataset download`, `Image generation`, `Reward scoring`, `SFT training`, `DPO training`, `Masked-SFT training`, `Synthetic data`, `Evaluation`, `Plotting`, `SLURM launchers`. For each row include columns `Current entry point`, `Consumes`, `Produces`, `Optimizes / measures`, `Thesis support`, and `Status`. Use concrete commands and paths from the codebase map: `python -m src.prompt_pipeline.generate`, `python scripts/download_dataset.py`, `python -m scripts.generate_images`, `python -m scripts.score_images`, `accelerate launch --config_file configs/accelerate/single_gpu.yaml -m src.training.sft_trainer --config configs/sft.json`, `accelerate launch --config_file configs/accelerate/single_gpu.yaml -m src.training.dpo_trainer --config configs/dpo.json`, `python -m src.training.masked_sft_trainer --config configs/masked_sft.json`, `python -m scripts.synth.build_dataset`, `python -m src.evaluation.generate_baseline`, `python -m src.evaluation.evaluate_rewards`, `python scripts/thesis_plots/plot_sft_losses.py`, `python scripts/thesis_plots/plot_dpo_metrics.py`, and `sbatch scripts/cluster/*.sbatch`. Do not invent new commands in this task; this is inventory of current entry points.</action>
  <acceptance_criteria>`docs/pipeline_inventory.md` contains `## Supported toolkit entry points`; it contains all eleven pipeline family names listed in the action; it contains the exact command strings `python -m scripts.generate_images`, `python -m scripts.score_images`, `src.training.sft_trainer`, `src.training.dpo_trainer`, and `python -m scripts.synth.build_dataset`.</acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; text=Path('docs/pipeline_inventory.md').read_text(); required=['## Supported toolkit entry points','Prompt generation','Dataset download','Image generation','Reward scoring','SFT training','DPO training','Masked-SFT training','Synthetic data','Evaluation','Plotting','SLURM launchers','python -m scripts.generate_images','python -m scripts.score_images','src.training.sft_trainer','src.training.dpo_trainer','python -m scripts.synth.build_dataset']; missing=[s for s in required if s not in text]; assert not missing, missing"</automated>
  </verify>
  <done>`docs/pipeline_inventory.md` gives a complete current supported-entry-point inventory for INV-01 and INV-02.</done>
</task>

<task type="auto">
  <name>Task 2: Classify diagnostics, experiments, duplicates, and historical tracks</name>
  <files>docs/pipeline_inventory.md</files>
  <read_first>docs/pipeline_inventory.md, .planning/codebase/TESTING.md, .planning/codebase/CONCERNS.md, .planning/codebase/STRUCTURE.md, .planning/REQUIREMENTS.md</read_first>
  <action>Extend `docs/pipeline_inventory.md` with sections `## Non-default diagnostics and experiments`, `## Historical experiment tracks`, and `## Artifact safety notes`. In `## Non-default diagnostics and experiments`, add separate subsections or tables for `Manual diagnostics`, `Experimental scripts`, `Legacy or superseded configs`, and `Supported toolkit entry points`. Include concrete entries for `scripts/test_gradient_flow.py`, `scripts/test_grad_magnitude.py`, `experiments/ocr_reward_tests/test_qwen_yes_prob.py`, `experiments/ocr_reward_tests/test_paddleocr.py`, `configs/train_base.json`, `configs/train_v4_highLR.json`, `configs/sft_ocr.json`, `configs/sft_vlm.json`, `configs/sft_product.json`, `configs/dpo_ocr.json`, `configs/dpo_vlm.json`, and `configs/dpo_product.json`. In `## Historical experiment tracks`, describe four tracks: reward-filtered generated-image SFT/DPO, synthetic masked-MSE training, OCR/VLM/product reward variants, and thesis plotting/report flows. In `## Artifact safety notes`, state that `outputs/`, `runs/`, generated images, tensors, checkpoints, large generated datasets, and logs stay out of git unless intentionally tiny fixtures or documentation assets.</action>
  <acceptance_criteria>`docs/pipeline_inventory.md` contains `Manual diagnostics`, `Experimental scripts`, `Legacy or superseded configs`, `Historical experiment tracks`, `reward-filtered generated-image SFT/DPO`, `synthetic masked-MSE`, `OCR/VLM/product reward variants`, `thesis plotting/report flows`, `outputs/`, and `runs/`.</acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; text=Path('docs/pipeline_inventory.md').read_text(); required=['Manual diagnostics','Experimental scripts','Legacy or superseded configs','Historical experiment tracks','reward-filtered generated-image SFT/DPO','synthetic masked-MSE','OCR/VLM/product reward variants','thesis plotting/report flows','scripts/test_gradient_flow.py','experiments/ocr_reward_tests/test_qwen_yes_prob.py','configs/train_base.json','outputs/','runs/']; missing=[s for s in required if s not in text]; assert not missing, missing"</automated>
  </verify>
  <done>The inventory satisfies INV-03 and INV-04 by making risky/manual/historical paths explicitly visible and separate from supported toolkit commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User→documentation | Users rely on inventory text to decide which local commands are safe to run. |
| Documentation→filesystem artifacts | Inventory names generated output roots that must stay out of git. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | I | `docs/pipeline_inventory.md` artifact descriptions | mitigate | Add `## Artifact safety notes` naming `outputs/`, `runs/`, tensors, checkpoints, logs, generated images, and large datasets as non-committed outputs. |
| T-01-02 | D | Manual diagnostics documented as tests | mitigate | Classify `scripts/test_gradient_flow.py`, `scripts/test_grad_magnitude.py`, and `experiments/ocr_reward_tests/test_*.py` as non-default diagnostics so users do not accidentally trigger GPU/model work. |
</threat_model>

<verification>
Run both task automated checks. Do not run GPU/model/OCR diagnostics for this documentation plan.
</verification>

<success_criteria>
- `docs/pipeline_inventory.md` exists.
- All Phase 1 pipeline families are listed with concrete current entry points.
- Supported, manual diagnostic, experimental, legacy/superseded, and historical tracks are separated.
- Artifact safety notes explicitly keep generated ML outputs out of git.
</success_criteria>

<output>
After completion, create `.planning/phases/01-execution-surface-and-pipeline-inventory/01-01-SUMMARY.md`.
</output>
