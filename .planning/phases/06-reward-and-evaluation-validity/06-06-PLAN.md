---
phase: 06-reward-and-evaluation-validity
plan: 06
type: execute
wave: 3
depends_on: [06-04, 06-05]
files_modified:
  - src/evaluation/thesis_outputs.py
  - scripts/build_thesis_outputs.py
  - tests/test_thesis_outputs.py
  - docs/thesis_outputs.md
autonomous: true
requirements: [EVAL-08, RUN-05]
must_haves:
  truths:
    - "User can build thesis-ready tables, SVG plots, and contact sheets from recorded runs and reports."
    - "User can trace each thesis output back to exact run manifests, score files, diagnostic reports, and artifact paths."
    - "User can detect missing provenance before treating an output as thesis-ready."
  artifacts:
    - path: "src/evaluation/thesis_outputs.py"
      provides: "Thesis output bundle builder and provenance validator"
      exports: ["build_thesis_output_bundle", "format_thesis_output_markdown"]
    - path: "scripts/build_thesis_outputs.py"
      provides: "Thin CLI for thesis-ready output bundle JSON/Markdown"
    - path: "tests/test_thesis_outputs.py"
      provides: "CPU-safe thesis output provenance tests"
    - path: "docs/thesis_outputs.md"
      provides: "Thesis output bundle documentation"
  key_links:
    - from: "src/evaluation/thesis_outputs.py"
      to: "src/runtime/manifests.py"
      via: "loads/records manifest paths"
      pattern: "manifest"
    - from: "scripts/build_thesis_outputs.py"
      to: "src/evaluation/thesis_outputs.py"
      via: "imports bundle builder"
      pattern: "build_thesis_output_bundle"
---

<objective>
Build the thesis output bundle generator that turns recorded evaluation runs, score files, diagnostics, and artifact references into traceable thesis-ready tables, SVG plots, and contact sheets.

Purpose: RUN-05 and EVAL-08 require thesis outputs to be generated from recorded evidence rather than manual/static numbers.
Output: Bundle module, CLI, tests, and docs.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-reward-and-evaluation-validity/06-RESEARCH.md
@.planning/phases/06-reward-and-evaluation-validity/06-04-SUMMARY.md
@.planning/phases/06-reward-and-evaluation-validity/06-05-SUMMARY.md
@src/runtime/manifests.py
@scripts/thesis_plots/plot_sft_losses.py
@scripts/thesis_plots/plot_dpo_metrics.py

<interfaces>
From `src/runtime/manifests.py`:
```python
def load_run_manifest(path: str | Path) -> RunManifest: ...
class RunManifest:
    run_id: str
    stage: str
    git: dict[str, Any]
    config_snapshot: dict[str, Any]
    inputs: dict[str, Any]
    outputs: dict[str, Any]
    metrics: dict[str, Any]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify thesis output provenance bundle</name>
  <files>tests/test_thesis_outputs.py</files>
  <behavior>
    - Test bundle input accepts run manifest paths, score report paths, diagnostic report paths, table specs, SVG plot specs, and contact-sheet specs.
    - Test output includes schema version, source manifests, git/config provenance, evidence availability, generated table rows, SVG plot paths, contact-sheet paths, and missing-provenance warnings.
    - Test malformed or missing provenance is surfaced as blocking readiness errors.
  </behavior>
  <action>Write RED tests with tiny manifest/report JSON fixtures and tiny PIL images under pytest `tmp_path`. Assert the bundle creates deterministic Markdown/CSV tables, simple SVG plots, and bounded contact sheets while recording source evidence paths.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_thesis_outputs.py -x</automated>
  </verify>
  <done>Tests fail only because thesis output module/CLI are missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement thesis output bundle module and CLI</name>
  <files>src/evaluation/thesis_outputs.py, scripts/build_thesis_outputs.py, tests/test_thesis_outputs.py</files>
  <behavior>
    - `build_thesis_output_bundle` reads local manifest/report JSON, builds deterministic tables, writes simple SVG plots, writes bounded contact sheets when image paths are provided, and records all source paths.
    - CLI supports `--config`, `--output-bundle`, `--markdown-summary`, and returns nonzero when required provenance is missing.
    - Markdown summary includes source manifest table, evidence warnings, and output artifact destinations.
  </behavior>
  <action>Create the bundle builder and CLI. Use `load_run_manifest` where real manifest files are supplied, but allow tests to use minimal valid manifest JSON. Generate plots as simple deterministic SVG using standard-library XML/text output and generate contact sheets with PIL only; do not import matplotlib, torch, diffusers, transformers, or OCR stacks.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_thesis_outputs.py -q</automated>
  </verify>
  <done>Thesis output bundle and CLI tests pass using CPU-safe fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Document thesis output evidence workflow</name>
  <files>docs/thesis_outputs.md, tests/test_thesis_outputs.py</files>
  <behavior>
    - Docs include bundle config fields, exact source manifest/report requirements, table/SVG plot/contact-sheet outputs, readiness errors, and generated-artifact safety.
    - Docs explain how thesis plots/results map back to manifests and artifacts.
  </behavior>
  <action>Add docs drift assertions, then create `docs/thesis_outputs.md` with command examples and evidence-readiness rules. Ensure docs state that generated tables, SVG plots, contact sheets, and bundle JSON/Markdown remain runtime artifacts tied to recorded manifests and reports.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_thesis_outputs.py -q && PATH="/root/.local/bin:$PATH" uv run --extra lint ruff check src/evaluation/thesis_outputs.py scripts/build_thesis_outputs.py tests/test_thesis_outputs.py</automated>
  </verify>
  <done>Thesis output docs, CLI, and provenance tests pass; Ruff passes for plan-owned Python files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recorded manifests/reports -> thesis bundle | Local evidence files become thesis output provenance. |
| bundle config -> output paths | User-supplied paths determine generated report destinations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | R | thesis bundle | mitigate | Record every source manifest/report path, run ID, git state, and config snapshot reference. |
| T-06-06-02 | T | bundle config parser | mitigate | Validate required source/output fields and report missing provenance as blocking readiness errors. |
| T-06-06-03 | I | markdown summary | mitigate | Do not include raw secret/cache path metadata; rely on existing manifest privacy fields. |
</threat_model>

<verification>
Run Task 3 verification. Confirm tests create only temporary JSON/Markdown files and no rendered plots/images under committed paths.
</verification>

<success_criteria>
Users can generate thesis-ready evidence bundles that trace tables, SVG plots, and contact sheets back to exact run manifests, score outputs, diagnostic reports, configs, git state, and artifact paths.
</success_criteria>

<output>
After completion, create `.planning/phases/06-reward-and-evaluation-validity/06-06-SUMMARY.md`.
</output>
