---
phase: 06-reward-and-evaluation-validity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/evaluation/reward_interface.py
  - tests/test_evaluation_reward_interface.py
  - docs/reward_evaluation.md
autonomous: true
requirements: [EVAL-01, EVAL-02, STR-03]
must_haves:
  truths:
    - "User can describe every reward result through one canonical schema."
    - "User can compute product scores from documented VLM, OCR, CER, entropy, and exact-text components."
    - "User can inspect formula metadata, thresholds, scorer versions, and manifest links without loading OCR or VLM models."
  artifacts:
    - path: "src/evaluation/reward_interface.py"
      provides: "Canonical reward result dataclasses, product formula, score-file metadata helpers"
      exports: ["RewardResult", "ProductScoreFormula", "compute_product_score", "build_score_metadata"]
    - path: "tests/test_evaluation_reward_interface.py"
      provides: "CPU-safe canonical reward and product score tests"
    - path: "docs/reward_evaluation.md"
      provides: "Reward interface and product score formula documentation"
  key_links:
    - from: "src/evaluation/reward_interface.py"
      to: "docs/reward_evaluation.md"
      via: "documented field/formula names"
      pattern: "compute_product_score"
---

<objective>
Create the canonical, import-safe reward result and product score contract that scoring, training, evaluation, diagnostics, and thesis outputs can share.

Purpose: Phase 6 evidence is only comparable if Qwen/VLM, OCR/CER/entropy, exact text metrics, product score formula, scorer versions, thresholds, and manifest links are represented consistently.
Output: Pure Python reward interface module, CPU-safe tests, and formula documentation.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-reward-and-evaluation-validity/06-RESEARCH.md
@.planning/phases/04-cpu-safe-characterization-tests/04-05-SUMMARY.md
@src/training/rewards.py
@scripts/score_images.py

<interfaces>
From `src/training/rewards.py`:
```python
class QwenYesProbReward:
    PROMPT_TEMPLATE = "...{target_text}..."
    SYSTEM_PROMPT = "..."
    def score_single(self, image_tensor: torch.Tensor, target_text: str) -> torch.Tensor: ...
    def score_batch(self, images: torch.Tensor, target_texts: list[str]) -> torch.Tensor: ...

class OcrCerEntropyReward:
    def score(self, image_path: str, target_text: str) -> dict: ...
    def score_pil(self, image: Image.Image, target_text: str) -> dict: ...
```

From `scripts/score_images.py` current CSV fields:
```python
base_fields = ["id", "version", "score", "target_text"]
optional OCR fields = ["official_conf", "cer", "entropy", "min_p", "frac_unc", "ocr_detected"]
optional both fields = ["score_vlm", "score_ocr"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify canonical reward records and product formula</name>
  <files>tests/test_evaluation_reward_interface.py</files>
  <behavior>
    - Test canonical `RewardResult` serialization includes `sample_id`, `version`, `target_text`, component scores, exact-match/text metrics, scorer metadata, thresholds, and `manifest_path`.
    - Test `compute_product_score` combines VLM and OCR evidence with explicit weights and threshold flags, returning both numeric score and component breakdown.
    - Test missing VLM or OCR evidence is represented in `missing_components` rather than silently treated as comparable.
  </behavior>
  <action>Write RED tests for the canonical reward/result schema and product score formula before creating production code. Keep fixtures as inline dictionaries; do not import Qwen, PaddleOCR, diffusers, CUDA, or generated artifacts.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_evaluation_reward_interface.py -x</automated>
  </verify>
  <done>Tests fail only because `src.evaluation.reward_interface` does not exist or lacks the specified exports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement import-safe reward interface module</name>
  <files>src/evaluation/reward_interface.py, tests/test_evaluation_reward_interface.py</files>
  <behavior>
    - `RewardResult.to_row()` returns deterministic CSV/JSON-safe fields.
    - `ProductScoreFormula` records formula name, weights, thresholds, and scorer versions.
    - `build_score_metadata` returns schema metadata with formula details, scorer versions, thresholds, source manifest paths, and generated-at timestamp supplied by caller or injected clock.
  </behavior>
  <action>Create `src/evaluation/reward_interface.py` as a pure module with frozen dataclasses/functions. Use standard-library-only logic plus `math`; do not import `torch`, `PIL`, `transformers`, `paddleocr`, `diffusers`, or existing heavy scorer wrappers. Product scores must expose missing evidence explicitly and produce deterministic component dictionaries for score sidecars and reports.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_evaluation_reward_interface.py -q</automated>
  </verify>
  <done>Canonical reward interface tests pass and module import does not load heavy optional model/OCR stacks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Document reward schema and product score formula</name>
  <files>docs/reward_evaluation.md, tests/test_evaluation_reward_interface.py</files>
  <behavior>
    - Docs name canonical fields, product formula terms, scorer version metadata, thresholds, manifest links, and generated-artifact safety.
    - Docs explain that missing VLM/OCR evidence is reported explicitly and not treated as comparable.
  </behavior>
  <action>Add docs drift assertions to `tests/test_evaluation_reward_interface.py`, then create `docs/reward_evaluation.md` with exact field names and formula semantics from `src/evaluation/reward_interface.py`. Avoid promising model quality; document only evidence contracts and reproducibility metadata.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_evaluation_reward_interface.py -q && PATH="/root/.local/bin:$PATH" uv run --extra lint ruff check src/evaluation/reward_interface.py tests/test_evaluation_reward_interface.py</automated>
  </verify>
  <done>Reward formula docs and tests agree on exact contract names; Ruff passes for plan-owned Python files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local score records -> product formula | Untrusted/malformed CSV/JSON score values become numeric evidence. |
| run manifests -> score metadata | Local paths and provenance references enter reports. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | T | `compute_product_score` | mitigate | Coerce/validate finite numeric components and surface missing components explicitly. |
| T-06-01-02 | I | `build_score_metadata` | mitigate | Store manifest paths and scorer versions, not environment secrets or raw cache paths. |
| T-06-01-03 | D | score record parsing | accept | Pure in-memory tests use tiny records; larger file iteration is handled in downstream CLI plans. |
</threat_model>

<verification>
Run the targeted pytest and Ruff command in Task 3. Confirm `python - <<'PY'\nimport sys; before=set(sys.modules); import src.evaluation.reward_interface; print(any(m in sys.modules and m not in before for m in ['transformers','paddleocr','diffusers']))\nPY` prints `False` if additional import-safety evidence is needed.
</verification>

<success_criteria>
`src.evaluation.reward_interface` is the canonical CPU-safe contract for reward rows, product score formulas, score metadata, missing evidence, thresholds, scorer versions, and manifest links.
</success_criteria>

<output>
After completion, create `.planning/phases/06-reward-and-evaluation-validity/06-01-SUMMARY.md`.
</output>
