---
phase: 06-reward-and-evaluation-validity
plan: 05
type: execute
wave: 2
depends_on: [06-01, 06-03]
files_modified:
  - src/evaluation/diagnostics.py
  - scripts/analyze_reward_diagnostics.py
  - tests/test_reward_diagnostics.py
  - docs/evaluation_diagnostics.md
autonomous: true
requirements: [EVAL-05, EVAL-06, EVAL-07]
must_haves:
  truths:
    - "User can inspect VLM-vs-OCR reward disagreement with correlation/scatter summaries."
    - "User can identify false positives, false negatives, per-character confusions, and affected difficulty slices."
    - "User can generate diagnostic reports without invoking OCR/VLM models."
  artifacts:
    - path: "src/evaluation/diagnostics.py"
      provides: "Reward disagreement, false-positive/negative, correlation, contact sheet, and confusion summaries"
      exports: ["analyze_reward_disagreement", "format_diagnostics_markdown"]
    - path: "scripts/analyze_reward_diagnostics.py"
      provides: "Thin CLI for diagnostics JSON/Markdown reports"
    - path: "tests/test_reward_diagnostics.py"
      provides: "CPU-safe diagnostics tests"
    - path: "docs/evaluation_diagnostics.md"
      provides: "Reward disagreement diagnostics guide"
  key_links:
    - from: "src/evaluation/diagnostics.py"
      to: "src/evaluation/slices.py"
      via: "per-slice disagreement summaries"
      pattern: "classify_text_slices"
    - from: "src/evaluation/diagnostics.py"
      to: "src/evaluation/reward_interface.py"
      via: "canonical score fields"
      pattern: "product_score"
---

<objective>
Create reward disagreement diagnostics for VLM/OCR/product scores across difficulty slices and gold benchmark expectations.

Purpose: Thesis evidence needs diagnostic transparency when reward signals disagree or fail on specific Russian text cases.
Output: Diagnostics module, CLI, tests, and docs.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-reward-and-evaluation-validity/06-RESEARCH.md
@.planning/phases/06-reward-and-evaluation-validity/06-01-SUMMARY.md
@.planning/phases/06-reward-and-evaluation-validity/06-03-SUMMARY.md

<interfaces>
From Plan 06-03 expected exports:
```python
from src.evaluation.slices import classify_text_slices, summarize_slices
from src.evaluation.gold_benchmark import evaluate_gold_predictions
```

From Plan 06-01 expected canonical score fields:
```python
score_vlm, score_ocr, cer, entropy, exact_match, char_accuracy, product_score, missing_components
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify reward disagreement reports</name>
  <files>tests/test_reward_diagnostics.py</files>
  <behavior>
    - Test correlation/scatter summary for records with both VLM and OCR scores.
    - Test false positives and false negatives against exact-match or gold labels using configurable thresholds.
    - Test per-character confusion summary and per-slice disagreement counts.
    - Test optional contact sheet generation creates a tiny PIL contact sheet from fixture images and lists captions/source paths in the report.
  </behavior>
  <action>Write RED tests with inline canonical score records and tiny temp CSV/JSONL files. Tests must validate missing evidence is counted separately and must not load OCR/VLM/image libraries.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_reward_diagnostics.py -x</automated>
  </verify>
  <done>Tests fail only because diagnostics module/CLI are missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement diagnostics module and CLI</name>
  <files>src/evaluation/diagnostics.py, scripts/analyze_reward_diagnostics.py, tests/test_reward_diagnostics.py</files>
  <behavior>
    - `analyze_reward_disagreement` returns schema version, record counts, missing evidence, VLM/OCR correlation when available, false positive/negative rows, per-character confusion counts, per-slice summaries, and contact sheet entries.
    - CLI supports `--scores`, `--gold`, `--output-report`, `--markdown-summary`, `--contact-sheet`, and threshold flags.
    - CLI writes deterministic JSON/Markdown and returns nonzero only for malformed inputs, not for discovered disagreements.
  </behavior>
  <action>Create diagnostics logic and a thin CLI. Use CSV/JSONL/JSON parsing, canonical score fields, Plan 06-03 slice/gold helpers, standard-library statistics, and PIL-only contact sheet rendering for selected false-positive/false-negative rows when `--contact-sheet` is provided. Keep image rendering optional and bounded by a CLI limit so default diagnostics remain CPU-safe and model-free.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_reward_diagnostics.py -q</automated>
  </verify>
  <done>Diagnostics reports and CLI tests pass using CPU-safe score/gold fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend diagnostics documentation</name>
  <files>docs/evaluation_diagnostics.md, tests/test_reward_diagnostics.py</files>
  <behavior>
    - Docs include VLM-vs-OCR scatter/correlation, false-positive/false-negative definitions, PIL contact sheet fields, per-character confusion, slice summaries, and gold benchmark linkage.
    - Docs explicitly state diagnostics consume recorded score outputs and do not run reward models.
  </behavior>
  <action>Add docs drift assertions, then update `docs/evaluation_diagnostics.md` without deleting Plan 06-03 slice/gold sections. Include exact CLI examples and generated-artifact safety guidance for diagnostic reports and contact sheets.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_reward_diagnostics.py -q && PATH="/root/.local/bin:$PATH" uv run --extra lint ruff check src/evaluation/diagnostics.py scripts/analyze_reward_diagnostics.py tests/test_reward_diagnostics.py</automated>
  </verify>
  <done>Diagnostics docs and CLI behavior are guarded by tests; Ruff passes for plan-owned Python files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recorded scores/gold labels -> diagnostics | Local score files and human labels become disagreement evidence. |
| image paths -> contact sheet | Local image paths are opened only when the user requests a bounded PIL contact sheet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | T | diagnostics input parsing | mitigate | Validate required canonical fields and keep malformed rows in report errors. |
| T-06-05-02 | R | false positive/negative report | mitigate | Include thresholds, source paths, and counts for missing evidence. |
| T-06-05-03 | I | contact sheet | mitigate | Generate only user-requested bounded contact sheets under runtime output paths; never commit generated images. |
</threat_model>

<verification>
Run Task 3 verification. Confirm reports created by tests are under `tmp_path` and not committed runtime outputs.
</verification>

<success_criteria>
Users can generate CPU-safe reward disagreement diagnostics that expose correlation, false positives/negatives, per-character confusion, per-slice summaries, missing evidence, and bounded PIL contact sheets from recorded score files.
</success_criteria>

<output>
After completion, create `.planning/phases/06-reward-and-evaluation-validity/06-05-SUMMARY.md`.
</output>
