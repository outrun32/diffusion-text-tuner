---
phase: 06-reward-and-evaluation-validity
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/evaluation/slices.py
  - src/evaluation/gold_benchmark.py
  - tests/test_evaluation_slices_gold.py
  - tests/fixtures/evaluation/gold_diagnostic.jsonl
  - docs/evaluation_diagnostics.md
autonomous: true
requirements: [EVAL-05, EVAL-07]
must_haves:
  truths:
    - "User can label evaluation records by Russian text difficulty slices."
    - "User can validate reward signals against a small gold diagnostic benchmark schema."
    - "User can run slice and gold checks without model, OCR, CUDA, or generated image dependencies."
  artifacts:
    - path: "src/evaluation/slices.py"
      provides: "Russian text difficulty slice classification"
      exports: ["classify_text_slices", "summarize_slices"]
    - path: "src/evaluation/gold_benchmark.py"
      provides: "Gold diagnostic benchmark loading and metric validation"
      exports: ["load_gold_benchmark", "evaluate_gold_predictions", "format_gold_report_markdown"]
    - path: "tests/fixtures/evaluation/gold_diagnostic.jsonl"
      provides: "Tiny committed gold diagnostic fixture"
    - path: "docs/evaluation_diagnostics.md"
      provides: "Slice and gold benchmark documentation"
  key_links:
    - from: "src/evaluation/gold_benchmark.py"
      to: "src/evaluation/slices.py"
      via: "gold report includes slice labels"
      pattern: "classify_text_slices"
---

<objective>
Add CPU-safe Russian text difficulty slicing and gold diagnostic benchmark contracts.

Purpose: Reward validity must be checked across text difficulty slices and against hand-labeled/gold diagnostic evidence before scores become thesis evidence.
Output: Slice module, gold benchmark module, tiny fixture, tests, and diagnostics docs.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-reward-and-evaluation-validity/06-RESEARCH.md
@.planning/phases/03-data-curriculum-and-dataset-quality/03-02-SUMMARY.md
@docs/dataset_quality.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify Russian text difficulty slices</name>
  <files>tests/test_evaluation_slices_gold.py</files>
  <behavior>
    - Test rare Cyrillic letters, word length, phrase length, digits, punctuation, mixed case, multiline, font/style, and scene/background slice labels.
    - Test slice summary counts records by slice and surfaces records with missing target text separately.
  </behavior>
  <action>Write RED tests for `classify_text_slices` and `summarize_slices` using inline record dictionaries. Slice rules must be deterministic and pure; no model/OCR/image dependencies.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_evaluation_slices_gold.py -x</automated>
  </verify>
  <done>Tests fail only because `src.evaluation.slices` does not exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement slices and gold diagnostic benchmark</name>
  <files>src/evaluation/slices.py, src/evaluation/gold_benchmark.py, tests/test_evaluation_slices_gold.py, tests/fixtures/evaluation/gold_diagnostic.jsonl</files>
  <behavior>
    - `load_gold_benchmark` validates schema fields: `sample_id`, `target_text`, `image_path`, `expected_exact_match`, `expected_ocr_detected`, `human_label`, and optional `notes`.
    - `evaluate_gold_predictions` joins prediction records by `sample_id`, computes exact agreement, OCR text agreement, missing prediction counts, per-slice summaries, and markdown-ready findings.
    - Tiny fixture includes Cyrillic, digit/punctuation, mixed-case, and multiline examples.
  </behavior>
  <action>Create pure modules for slice classification and gold benchmark validation/evaluation. Add the committed tiny JSONL fixture under `tests/fixtures/evaluation/`; it must contain metadata only and no generated image binaries. Gold evaluation must expose missing predictions and disagreement counts explicitly.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_evaluation_slices_gold.py -q</automated>
  </verify>
  <done>Slice and gold benchmark tests pass using only inline records and the tiny fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Document slice and gold diagnostic contracts</name>
  <files>docs/evaluation_diagnostics.md, tests/test_evaluation_slices_gold.py</files>
  <behavior>
    - Docs name every supported slice and gold benchmark field.
    - Docs explain that gold validation is a diagnostic safeguard and that missing predictions/disagreements are explicit evidence, not hidden pass conditions.
  </behavior>
  <action>Add docs drift assertions, then create `docs/evaluation_diagnostics.md` sections for difficulty slices and gold diagnostic validation. Include fixture schema and generated-artifact safety notes; do not define a broad human-evaluation taxonomy beyond the required gold diagnostic benchmark.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_evaluation_slices_gold.py -q && PATH="/root/.local/bin:$PATH" uv run --extra lint ruff check src/evaluation/slices.py src/evaluation/gold_benchmark.py tests/test_evaluation_slices_gold.py</automated>
  </verify>
  <done>Docs, slice rules, gold schema, and tests agree; Ruff passes for plan-owned Python files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local JSONL benchmark -> gold report | Human-authored labels and paths become evaluation evidence. |
| prediction records -> diagnostic summaries | Local score records are grouped into thesis-relevant slices. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | T | `load_gold_benchmark` | mitigate | Validate JSONL object schema per line and aggregate malformed/missing-field errors. |
| T-06-03-02 | R | `evaluate_gold_predictions` | mitigate | Report missing predictions, source benchmark path, and per-slice counts. |
| T-06-03-03 | I | docs/fixture | mitigate | Commit metadata-only fixture paths and labels; do not commit generated image binaries. |
</threat_model>

<verification>
Run Task 3 verification. Confirm the fixture contains only JSONL text metadata and no generated image/tensor/checkpoint assets.
</verification>

<success_criteria>
Users can classify evaluation outputs by Russian text difficulty and validate reward predictions against a small gold diagnostic benchmark using CPU-safe tests and documented schemas.
</success_criteria>

<output>
After completion, create `.planning/phases/06-reward-and-evaluation-validity/06-03-SUMMARY.md`.
</output>
