---
phase: 06-reward-and-evaluation-validity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/evaluation/heldout.py
  - scripts/run_heldout_evaluation.py
  - tests/test_heldout_evaluation_harness.py
  - docs/evaluation_harness.md
autonomous: true
requirements: [EVAL-03, RUN-05]
must_haves:
  truths:
    - "User can define held-out baseline and trained LoRA comparisons with fixed prompts, seeds, inference settings, and artifact paths."
    - "User can generate an evaluation plan/report that links every comparison target to run manifests."
    - "User can validate held-out evaluation contracts without loading FLUX, CUDA, Qwen, or OCR."
  artifacts:
    - path: "src/evaluation/heldout.py"
      provides: "Held-out evaluation config parsing, validation, and report planning"
      exports: ["HeldoutEvaluationConfig", "EvaluationTarget", "build_evaluation_plan", "write_evaluation_plan"]
    - path: "scripts/run_heldout_evaluation.py"
      provides: "Thin CLI for validating and materializing held-out evaluation plans"
    - path: "tests/test_heldout_evaluation_harness.py"
      provides: "CPU-safe held-out harness tests"
    - path: "docs/evaluation_harness.md"
      provides: "Held-out evaluation configuration and execution guide"
  key_links:
    - from: "scripts/run_heldout_evaluation.py"
      to: "src/evaluation/heldout.py"
      via: "imports build_evaluation_plan/write_evaluation_plan"
      pattern: "from src\.evaluation\.heldout import"
---

<objective>
Create the CPU-safe held-out evaluation harness contract for comparing baseline and trained checkpoints under fixed prompts, seeds, inference settings, and manifest links.

Purpose: Evaluation validity requires a reusable plan/report surface before expensive generation/scoring jobs run.
Output: Held-out evaluation module, CLI, tests, and docs.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-reward-and-evaluation-validity/06-RESEARCH.md
@.planning/phases/05-training-objective-and-pipeline-comparability/05-06-SUMMARY.md
@src/runtime/manifests.py
@src/evaluation/generate_baseline.py

<interfaces>
From `src/runtime/manifests.py`:
```python
MANIFEST_STAGES = {"generate", "score", "sft", "dpo", "masked_sft", "synthetic", "evaluation"}
def load_run_manifest(path: str | Path) -> RunManifest: ...
def create_run_manifest(*, stage: str, config_path: str | Path | None = None, command: str | list[str], run_root: str | Path = "runs", slug: str | None = None, inputs: dict[str, Any] | None = None, outputs: dict[str, Any] | None = None, metrics: dict[str, Any] | None = None, notes: list[dict[str, Any]] | None = None, root: str | Path | None = None) -> RunManifest: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify held-out evaluation config contracts</name>
  <files>tests/test_heldout_evaluation_harness.py</files>
  <behavior>
    - Test valid config requires fixed prompts JSONL path, fixed seed list, inference settings, output root, and at least baseline plus one trained target.
    - Test each `EvaluationTarget` records target name, optional LoRA checkpoint path, source run manifest path, generation output path, score output path, and notes.
    - Test missing fixed prompts/seeds/settings are blocking validation errors.
  </behavior>
  <action>Write RED tests for the held-out evaluation config/report contract using pytest `tmp_path` JSON fixtures only. Do not import generation pipelines, torch CUDA, Qwen, OCR, or image files.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_heldout_evaluation_harness.py -x</automated>
  </verify>
  <done>Tests fail only because `src.evaluation.heldout` and the CLI do not exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement held-out plan builder and CLI</name>
  <files>src/evaluation/heldout.py, scripts/run_heldout_evaluation.py, tests/test_heldout_evaluation_harness.py</files>
  <behavior>
    - `build_evaluation_plan` returns deterministic JSON with schema version, fixed prompt/seed/inference settings, targets, planned generation commands, planned scoring commands, and manifest links.
    - CLI supports `--config`, `--output-plan`, and `--markdown-summary`, validates input, writes reports atomically, and returns nonzero for invalid configs.
    - The CLI only materializes plans/reports; it must not run FLUX generation or reward scoring in default mode.
  </behavior>
  <action>Create `src/evaluation/heldout.py` and `scripts/run_heldout_evaluation.py`. Keep implementation pure JSON/path validation and deterministic command construction. Planned commands may reference existing generation/scoring entry points, but execution remains user-invoked and explicit.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_heldout_evaluation_harness.py -q</automated>
  </verify>
  <done>Held-out config validation and CLI report materialization pass CPU-safe tests without model imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Document held-out evaluation workflow</name>
  <files>docs/evaluation_harness.md, tests/test_heldout_evaluation_harness.py</files>
  <behavior>
    - Docs include fixed prompts, fixed seeds, inference settings, baseline target, trained LoRA target, manifest links, output paths, and explicit separation between plan materialization and expensive execution.
    - Docs include local and SLURM-compatible command templates without making GPU/model/OCR work part of default tests.
  </behavior>
  <action>Add docs assertions, then create `docs/evaluation_harness.md` with exact config fields, CLI examples, manifest linkage, generated-artifact safety, and comparison prerequisites from Phase 5. Do not add external tracking services.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_heldout_evaluation_harness.py -q && PATH="/root/.local/bin:$PATH" uv run --extra lint ruff check src/evaluation/heldout.py scripts/run_heldout_evaluation.py tests/test_heldout_evaluation_harness.py</automated>
  </verify>
  <done>Held-out harness docs and tests agree on exact field/command names; Ruff passes for plan-owned Python files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local evaluation config -> planned commands | User-provided paths and labels become shell-visible command strings. |
| run manifest paths -> evaluation report | Local manifest references become thesis evidence links. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | T | `build_evaluation_plan` | mitigate | Validate required keys and reject traversal/home-expansion where output roots are written. |
| T-06-02-02 | R | evaluation plan report | mitigate | Include source config path, target manifest paths, fixed seeds, and fixed inference settings in every report. |
| T-06-02-03 | D | planned command rendering | mitigate | Materialize commands only; do not execute expensive GPU/model/OCR work from default CLI tests. |
</threat_model>

<verification>
Run Task 3 verification. Inspect generated test outputs under `tmp_path` only; no `outputs/` or `runs/` artifacts should be produced by tests.
</verification>

<success_criteria>
Users can create and validate a held-out evaluation plan that binds fixed prompts, seeds, inference settings, baseline/trained targets, generated outputs, score outputs, and source manifests before running expensive jobs.
</success_criteria>

<output>
After completion, create `.planning/phases/06-reward-and-evaluation-validity/06-02-SUMMARY.md`.
</output>
