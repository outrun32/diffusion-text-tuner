---
phase: 06-reward-and-evaluation-validity
plan: 04
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - scripts/score_images.py
  - src/evaluation/evaluate_rewards.py
  - src/runtime/artifacts.py
  - tests/test_evaluation_scoring_outputs.py
  - docs/reward_evaluation.md
autonomous: true
requirements: [EVAL-01, EVAL-02, EVAL-04, STR-03]
must_haves:
  truths:
    - "User can score evaluation outputs with canonical OCR, VLM, product, exact-match, and character-level fields."
    - "User can reproduce product score files with schema sidecars and manifest links."
    - "User can validate score outputs before using them in diagnostics or thesis reports."
  artifacts:
    - path: "scripts/score_images.py"
      provides: "Canonical score CSV rows and product score metadata sidecar for generated-image scoring"
    - path: "src/evaluation/evaluate_rewards.py"
      provides: "Evaluation scoring path using canonical reward interface"
    - path: "src/runtime/artifacts.py"
      provides: "CPU-safe validation for Phase 6 score files and sidecars"
    - path: "tests/test_evaluation_scoring_outputs.py"
      provides: "CPU-safe scoring output contract tests"
    - path: "docs/reward_evaluation.md"
      provides: "Updated score-file and sidecar command documentation"
  key_links:
    - from: "scripts/score_images.py"
      to: "src/evaluation/reward_interface.py"
      via: "canonical row/metadata helpers"
      pattern: "reward_interface"
    - from: "src/runtime/artifacts.py"
      to: "scores CSV sidecar"
      via: "schema metadata validation"
      pattern: "score.*schema"
---

<objective>
Wire canonical reward/product score contracts into scoring and evaluation outputs.

Purpose: EVAL-01 and EVAL-04 require one shared reward interface across scoring, training-adjacent reward wrappers, evaluation, and reports; EVAL-02 requires reproducible product score files with metadata.
Output: Canonical score output fields, metadata sidecars, validation, tests, and docs.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-reward-and-evaluation-validity/06-RESEARCH.md
@.planning/phases/06-reward-and-evaluation-validity/06-01-SUMMARY.md
@.planning/phases/06-reward-and-evaluation-validity/06-02-SUMMARY.md
@scripts/score_images.py
@src/evaluation/evaluate_rewards.py
@src/runtime/artifacts.py

<interfaces>
From Plan 06-01 expected exports:
```python
from src.evaluation.reward_interface import (
    RewardResult,
    ProductScoreFormula,
    compute_product_score,
    build_score_metadata,
)
```

From `src/runtime/artifacts.py`:
```python
def validate_artifacts(stage: str, paths: Mapping[str, str | Path | bool] | None = None, *, require_ready: bool = False) -> ArtifactReport: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify canonical scoring output contracts</name>
  <files>tests/test_evaluation_scoring_outputs.py</files>
  <behavior>
    - Test CSV rows include canonical fields for VLM score, OCR reward, CER, entropy, detection status, exact text match, character-level metrics, product score, missing components, and manifest path.
    - Test score metadata sidecar includes formula name, weights, thresholds, scorer versions, source manifests, and schema version.
    - Test artifact validation accepts well-formed score CSV plus sidecar and rejects missing required Phase 6 fields.
  </behavior>
  <action>Write RED tests using small temporary CSV/JSON files and object-level fakes for score conversion. Tests must not initialize Qwen, PaddleOCR, CUDA, torchvision transforms, or generated images.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_evaluation_scoring_outputs.py -x</automated>
  </verify>
  <done>Tests fail on missing canonical scoring conversions/validation, not on optional dependency imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire score scripts and artifact validators to canonical interface</name>
  <files>scripts/score_images.py, src/evaluation/evaluate_rewards.py, src/runtime/artifacts.py, tests/test_evaluation_scoring_outputs.py</files>
  <behavior>
    - `scripts/score_images.py` writes canonical columns when VLM/OCR/product evidence is available and writes a `.schema.json` sidecar next to the CSV.
    - `src/evaluation/evaluate_rewards.py` emits canonical JSONL fields and metadata using the same interface rather than duplicate reward classes/formula logic.
    - `validate_artifacts("evaluation_scores", ...)` validates Phase 6 score CSV/JSONL and sidecar schema without loading images/models.
  </behavior>
  <action>Refactor only boundary conversion/output code. Keep heavy reward class imports inside scorer branches. Use Plan 06-01 helpers for product scores and sidecar metadata. Preserve existing `--scorer vlm|ocr|both`, resume, shard, and current required CSV fields while adding canonical Phase 6 fields.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_evaluation_scoring_outputs.py tests/test_reward_wrapper_contracts.py -q</automated>
  </verify>
  <done>Canonical scoring output tests and existing reward wrapper import-safety tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update reward evaluation docs for score files</name>
  <files>docs/reward_evaluation.md, tests/test_evaluation_scoring_outputs.py</files>
  <behavior>
    - Docs include exact score CSV/JSONL fields, sidecar fields, product formula metadata, manifest link arguments, and validation command examples.
    - Docs state generated score files and sidecars are runtime artifacts unless intentionally tiny fixtures.
  </behavior>
  <action>Add docs assertions, then update `docs/reward_evaluation.md` with canonical score-file fields, sidecar metadata, CLI examples for scoring and validation, and missing-evidence behavior. Do not remove Plan 06-01 formula documentation.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_evaluation_scoring_outputs.py -q && PATH="/root/.local/bin:$PATH" uv run --extra lint ruff check scripts/score_images.py src/evaluation/evaluate_rewards.py src/runtime/artifacts.py tests/test_evaluation_scoring_outputs.py</automated>
  </verify>
  <done>Score output docs are guarded by tests; Ruff passes for plan-owned Python files or any unrelated pre-existing Ruff failures are documented in the summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated images/tensors -> score rows | Runtime image/text evidence becomes score CSV/JSONL values. |
| score rows/sidecars -> artifact validation | Local files become accepted thesis evidence inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | T | scoring row conversion | mitigate | Use canonical finite numeric conversion and missing evidence fields from Plan 06-01. |
| T-06-04-02 | R | score metadata sidecar | mitigate | Include formula/scorer versions, thresholds, source manifests, and generated timestamp. |
| T-06-04-03 | D | artifact validator | mitigate | Validate shallow CSV/JSONL/JSON schema only; do not inspect generated images or tensors. |
</threat_model>

<verification>
Run Task 3 verification plus `PATH="/root/.local/bin:$PATH" uv run pytest tests/test_reward_wrapper_contracts.py -q` if import-safety changed during implementation.
</verification>

<success_criteria>
Scoring/evaluation outputs use the canonical reward interface, include product score metadata, expose missing evidence, and are CPU-safe to validate before downstream diagnostics and thesis output generation.
</success_criteria>

<output>
After completion, create `.planning/phases/06-reward-and-evaluation-validity/06-04-SUMMARY.md`.
</output>
