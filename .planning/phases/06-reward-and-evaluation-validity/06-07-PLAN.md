---
phase: 06-reward-and-evaluation-validity
plan: 07
type: execute
wave: 4
depends_on: [06-04, 06-05, 06-06]
files_modified:
  - tests/test_evaluation_command_docs.py
  - docs/commands.md
  - README.md
  - Makefile
  - docs/reward_evaluation.md
  - docs/evaluation_harness.md
  - docs/evaluation_diagnostics.md
  - docs/thesis_outputs.md
autonomous: true
requirements: [EVAL-01, EVAL-02, EVAL-03, EVAL-04, EVAL-05, EVAL-06, EVAL-07, EVAL-08, RUN-05, STR-03]
must_haves:
  truths:
    - "User can discover Phase 6 reward/evaluation commands from docs, README, and Makefile aliases."
    - "User can run CPU-safe docs drift tests proving command docs match implemented CLIs."
    - "User can distinguish default CPU-safe checks from explicit GPU/model/OCR evaluation jobs."
  artifacts:
    - path: "tests/test_evaluation_command_docs.py"
      provides: "Docs drift tests for Phase 6 command surface"
    - path: "docs/commands.md"
      provides: "Phase 6 command catalog"
    - path: "README.md"
      provides: "Front-door Phase 6 evaluation guidance"
    - path: "Makefile"
      provides: "Phase 6 aliases for plan/score/diagnostics/thesis outputs"
  key_links:
    - from: "Makefile"
      to: "scripts/run_heldout_evaluation.py"
      via: "held-out evaluation alias"
      pattern: "run_heldout_evaluation"
    - from: "Makefile"
      to: "scripts/analyze_reward_diagnostics.py"
      via: "reward diagnostics alias"
      pattern: "analyze_reward_diagnostics"
    - from: "Makefile"
      to: "scripts/build_thesis_outputs.py"
      via: "thesis outputs alias"
      pattern: "build_thesis_outputs"
---

<objective>
Publish the integrated Phase 6 command surface and docs drift checks.

Purpose: Reward/evaluation validity is only usable if the user can find exact CPU-safe validation, held-out evaluation planning, scoring, diagnostics, and thesis output commands while keeping expensive model/OCR jobs explicit.
Output: Docs drift tests, docs updates, README links, and Makefile aliases.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-reward-and-evaluation-validity/06-RESEARCH.md
@.planning/phases/06-reward-and-evaluation-validity/06-04-SUMMARY.md
@.planning/phases/06-reward-and-evaluation-validity/06-05-SUMMARY.md
@.planning/phases/06-reward-and-evaluation-validity/06-06-SUMMARY.md
@docs/commands.md
@README.md
@Makefile
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify Phase 6 command documentation drift tests</name>
  <files>tests/test_evaluation_command_docs.py</files>
  <behavior>
    - Test `docs/commands.md` includes exact commands for held-out plan materialization, score validation/product sidecars, reward diagnostics, gold checks, thesis output bundles, and CPU-safe pytest selections.
    - Test `README.md` links to Phase 6 reward/evaluation docs.
    - Test `make -n` aliases print expected scripts without executing GPU/model/OCR jobs.
  </behavior>
  <action>Write RED docs drift tests first. Use `subprocess.run(["make", "-n", ...], check=True, capture_output=True, text=True)` with dummy variables; do not run actual evaluation/scoring commands.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_evaluation_command_docs.py -x</automated>
  </verify>
  <done>Tests fail on missing Phase 6 docs/Makefile command surface.</done>
</task>

<task type="auto">
  <name>Task 2: Add Makefile aliases and command docs</name>
  <files>docs/commands.md, Makefile, tests/test_evaluation_command_docs.py</files>
  <action>Add Makefile variables and aliases for Phase 6 workflows: held-out plan materialization, evaluation score validation/product sidecar check, reward diagnostics, gold diagnostic check, thesis output bundle, and focused Phase 6 CPU-safe tests. Update `docs/commands.md` with exact commands, runtime output paths under ignored roots, and explicit GPU/model/OCR command boundaries. Preserve existing Phase 1-5 aliases.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_evaluation_command_docs.py -q && make -n phase6-heldout-plan phase6-reward-diagnostics phase6-thesis-outputs</automated>
  </verify>
  <done>Command docs and Makefile dry-runs match implemented CLI scripts and do not execute expensive work.</done>
</task>

<task type="auto">
  <name>Task 3: Link Phase 6 docs from README and verify focused suite</name>
  <files>README.md, docs/reward_evaluation.md, docs/evaluation_harness.md, docs/evaluation_diagnostics.md, docs/thesis_outputs.md, tests/test_evaluation_command_docs.py</files>
  <action>Update README with a concise Phase 6 reward/evaluation validity section linking the four docs and naming the Makefile aliases. Ensure each Phase 6 doc links to related docs where applicable. Keep generated score files, diagnostics, contact sheets, thesis bundles, plots, images, tensors, checkpoints, logs, and run outputs documented as runtime artifacts.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_evaluation_command_docs.py tests/test_evaluation_reward_interface.py tests/test_heldout_evaluation_harness.py tests/test_evaluation_slices_gold.py tests/test_evaluation_scoring_outputs.py tests/test_reward_diagnostics.py tests/test_thesis_outputs.py -q</automated>
  </verify>
  <done>Phase 6 docs/README/Makefile are discoverable and the focused CPU-safe Phase 6 test suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/Makefile -> user shell | Documented commands guide local execution of CPU-safe and expensive workflows. |
| README -> thesis workflow | Front-door documentation shapes what evidence users trust. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | D | Makefile aliases | mitigate | Use dry-run tests and keep expensive GPU/model/OCR work behind explicit aliases/arguments. |
| T-06-07-02 | R | command docs | mitigate | Docs drift tests assert exact commands and source doc links. |
| T-06-07-03 | I | runtime artifact docs | mitigate | Document ignored runtime output roots and never commit generated reports/images/tensors/checkpoints. |
</threat_model>

<verification>
Run Task 3 verification and `make -n` aliases from Task 2. Confirm no runtime outputs are created by docs tests.
</verification>

<success_criteria>
The Phase 6 command surface is discoverable and test-guarded across docs, README, and Makefile, with clear CPU-safe defaults and explicit expensive evaluation/scoring boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/06-reward-and-evaluation-validity/06-07-SUMMARY.md`.
</output>
