---
phase: 03-data-curriculum-and-dataset-quality
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/training/selection.py
  - scripts/materialize_training_data.py
  - tests/test_training_selection_artifacts.py
  - docs/data_selection.md
autonomous: true
requirements:
  - DATA-04
  - DATA-06
must_haves:
  truths:
    - "User can materialize selected SFT samples as versioned JSONL artifacts before training."
    - "User can materialize DPO preference pairs with explicit winner/loser semantics before training."
    - "Selection artifacts include provenance, thresholds, score columns, source hashes, counts, and schema metadata."
  artifacts:
    - path: "src/training/selection.py"
      provides: "SFT selected-sample and DPO preference-pair materialization"
      exports: ["materialize_sft_samples", "materialize_dpo_pairs"]
    - path: "scripts/materialize_training_data.py"
      provides: "CLI for selected_samples.jsonl and preference_pairs.jsonl"
    - path: "docs/data_selection.md"
      provides: "Versioned SFT/DPO selection artifact documentation"
  key_links:
    - from: "scripts/materialize_training_data.py"
      to: "src/training/selection.py"
      via: "materialize_sft_samples/materialize_dpo_pairs calls"
      pattern: "materialize_(sft_samples|dpo_pairs)"
    - from: "src/training/selection.py"
      to: "outputs/generated/scores.csv"
      via: "CSV score parsing"
      pattern: "scores_csv"
---

<objective>
Materialize training data selections as inspectable versioned artifacts.

Purpose: DATA-06 requires selected SFT samples and DPO preference pairs to exist as artifacts instead of being selected only inside dataset constructors; DATA-04 requires provenance for those artifacts.
Output: Selection module, CLI, tests, and docs.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-data-curriculum-and-dataset-quality/03-CONTEXT.md
@.planning/phases/03-data-curriculum-and-dataset-quality/03-RESEARCH.md
@.planning/phases/03-data-curriculum-and-dataset-quality/03-PATTERNS.md
@docs/runtime_contracts.md
@src/training/dataset.py

<interfaces>
Existing in-constructor SFT selection:
```python
if float(row["score"]) >= score_threshold:
    sample = {"prompt_id": row["id"], "version": int(row["version"]), "score": score}
```

Existing in-constructor DPO pair selection:
```python
versions.sort(key=lambda x: x["score"])
best = versions[-1]
worst = versions[0]
if best["score"] >= score_threshold and best["score"] - worst["score"] >= score_diff_min:
    pair = {"winner_version": best["version"], "loser_version": worst["version"]}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement selected SFT sample materialization</name>
  <files>src/training/selection.py, tests/test_training_selection_artifacts.py</files>
  <behavior>
    - Threshold mode writes every row above the selected score threshold.
    - Top-1-per-prompt mode writes the highest scoring version per prompt when it meets threshold.
    - Each JSONL row includes schema version, prompt ID, version, target text, selected score, score column, selection mode, source scores path/hash, and stable sample ID.
  </behavior>
  <action>Create `src/training/selection.py` with pure CSV/JSON helpers for `materialize_sft_samples(scores_csv, output_path, mode, score_column, threshold, top_k_per_prompt, manifest_path=...)`. Preserve existing default semantics as threshold selection on `score`. Sort output deterministically by prompt ID/version. Return a summary with counts and filtering stats.</action>
  <verify>
    <automated>uv run pytest tests/test_training_selection_artifacts.py -q</automated>
  </verify>
  <done>Tests prove selected sample artifacts are deterministic, versioned, schema-tagged, and equivalent to existing threshold behavior by default.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement DPO preference pair materialization</name>
  <files>src/training/selection.py, tests/test_training_selection_artifacts.py</files>
  <behavior>
    - Best-vs-worst mode writes one pair per prompt when score threshold and margin requirements pass.
    - Ambiguous pairs below margin are excluded with counted reasons.
    - Each pair records winner/loser versions, winner/loser scores, margin, pair-construction mode, source score path/hash, score column, and stable pair ID.
  </behavior>
  <action>Add `materialize_dpo_pairs(scores_csv, output_path, mode, score_column, threshold, margin, ambiguity_margin=...)` to `src/training/selection.py`. Preserve existing default semantics as best-vs-worst with threshold and score-difference filtering. Do not change trainer loaders in this plan; this plan creates artifacts and docs for reproducible selection.</action>
  <verify>
    <automated>uv run pytest tests/test_training_selection_artifacts.py -q</automated>
  </verify>
  <done>Tests prove winner/loser semantics, filtering stats, deterministic order, and schema metadata for DPO pair artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add materialization CLI and selection docs</name>
  <files>scripts/materialize_training_data.py, docs/data_selection.md, tests/test_training_selection_artifacts.py</files>
  <behavior>
    - CLI writes `selected_samples.jsonl` for SFT mode and `preference_pairs.jsonl` for DPO mode.
    - CLI writes summary/manifest JSON with thresholds, score column, mode, counts, source hash, and output path.
    - Documentation states how these artifacts connect to SFT/DPO configs and Phase 2 runtime contracts.
  </behavior>
  <action>Add `scripts/materialize_training_data.py` with subcommands or `--kind sft|dpo`, score/threshold/margin/mode/output/manifest arguments, and JSON stdout. Create `docs/data_selection.md` explaining artifact schemas, command examples, default equivalence to existing constructors, and generated-artifact safety.</action>
  <verify>
    <automated>uv run pytest tests/test_training_selection_artifacts.py -q</automated>
  </verify>
  <done>Users can materialize SFT selections and DPO pairs as versioned artifacts before training, with provenance and documented schemas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scores CSV -> selection tool | Generated score files control selected training examples and preference labels. |
| selection artifacts -> trainers/analysis | JSONL artifacts encode winner/loser semantics and must not silently invert labels. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | Score CSV parsing | mitigate | Validate required columns, numeric scores/versions, and source hash. |
| T-03-04-02 | Repudiation | Selection summary | mitigate | Record thresholds, mode, score column, source hashes, counts, and manifest path. |
| T-03-04-03 | Elevation of privilege | DPO winner/loser semantics | mitigate | Unit-test winner is strictly higher score than loser and reject ambiguous margins. |
</threat_model>

<verification>
Run `uv run pytest tests/test_training_selection_artifacts.py -q`. Inspect one tmp CLI output to confirm schema fields and counts.
</verification>

<success_criteria>
DATA-06 and selection-side DATA-04 are satisfied when SFT and DPO training selections are materialized as deterministic versioned JSONL artifacts with explicit provenance and semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-curriculum-and-dataset-quality/03-04-SUMMARY.md`.
</output>
