---
phase: 03-data-curriculum-and-dataset-quality
plan: 06
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
  - 03-04
  - 03-05
files_modified:
  - src/runtime/paths.py
  - src/runtime/artifacts.py
  - docs/runtime_contracts.md
  - docs/commands.md
  - README.md
  - Makefile
  - tests/test_data_quality_docs.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-07
must_haves:
  truths:
    - "User can discover Phase 3 curriculum, validation, quality, selection, and comparison commands from the standard command surface."
    - "Runtime contracts document Phase 3 dataset manifests, quality reports, selection artifacts, and comparison reports."
    - "Default docs/tests preserve generated-artifact safety and CPU-safe verification."
  artifacts:
    - path: "docs/runtime_contracts.md"
      provides: "Phase 3 artifact contract matrix updates"
      contains: "dataset manifests"
    - path: "docs/commands.md"
      provides: "Phase 3 command examples"
      contains: "validate_prompt_dataset"
    - path: "tests/test_data_quality_docs.py"
      provides: "Docs/runtime wiring regression tests"
  key_links:
    - from: "docs/commands.md"
      to: "scripts/validate_prompt_dataset.py"
      via: "documented command"
      pattern: "validate_prompt_dataset"
    - from: "docs/commands.md"
      to: "scripts/compare_data_sources.py"
      via: "documented command"
      pattern: "compare_data_sources"
    - from: "src/runtime/paths.py"
      to: "docs/runtime_contracts.md"
      via: "matching path names for Phase 3 artifacts"
      pattern: "selected_samples"
---

<objective>
Wire Phase 3 data-quality artifacts into runtime contracts, docs, and command surfaces.

Purpose: Phase 3 is complete only when users can find and run the new curriculum, validation, manifest, synthetic inspection, selection, and comparison workflows from the established toolkit surface.
Output: Runtime path/artifact updates, docs/README/Makefile wiring, and docs tests.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-data-curriculum-and-dataset-quality/03-CONTEXT.md
@.planning/phases/03-data-curriculum-and-dataset-quality/03-RESEARCH.md
@.planning/phases/03-data-curriculum-and-dataset-quality/03-PATTERNS.md
@docs/runtime_contracts.md
@docs/commands.md
@README.md
@Makefile
@src/runtime/paths.py
@src/runtime/artifacts.py

<interfaces>
Runtime path resolver currently includes:
```python
"synthetic": {
  "raw_dir": data_root / "synth_cyrillic" / "raw",
  "masked_dir": masked_root,
  "anyword_dir": data_root / "synth_cyrillic" / "anyword_format",
  "index_csv": masked_root / "index.csv",
  "selected_samples": masked_root / "selected_samples.jsonl",
}
"dpo": {"preference_pairs": generated_root / "preference_pairs.jsonl"}
```

Runtime artifact validator currently handles:
```python
elif stage_key in {"selected_samples", "preference_pairs"}:
    _validate_optional_index_file(context, normalized, stage_key, required=require_ready)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend runtime path and artifact contracts for Phase 3</name>
  <files>src/runtime/paths.py, src/runtime/artifacts.py, tests/test_data_quality_docs.py</files>
  <behavior>
    - `resolve_stage_paths` exposes prompt quality reports, dataset manifests, synthetic quality reports, contact sheets, selected samples, preference pairs, and source comparison report paths.
    - Artifact validation recognizes Phase 3 report/manifest JSON files and validates required presence when `require_ready=True`.
    - Git safety continues to classify generated data reports/contact sheets under runtime roots as non-committable unless they are approved fixtures/docs assets.
  </behavior>
  <action>Update `src/runtime/paths.py` with Phase 3 path keys for prompt quality reports/manifests, synthetic quality reports/manifests/contact sheets, selection summaries, and comparison reports. Update `src/runtime/artifacts.py` with CPU-safe JSON/JSONL existence/schema checks for `dataset_manifest`, `prompt_quality_report`, `synthetic_quality_report`, `data_source_comparison`, `selected_samples`, and `preference_pairs`. Keep validation shallow and model-free.</action>
  <verify>
    <automated>uv run pytest tests/test_data_quality_docs.py tests/test_runtime_artifacts.py -q</automated>
  </verify>
  <done>Runtime contracts expose and validate Phase 3 artifact families without regressing existing runtime artifact tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish Phase 3 command and README surfaces</name>
  <files>docs/commands.md, README.md, Makefile, tests/test_data_quality_docs.py</files>
  <behavior>
    - `docs/commands.md` lists commands for prompt config generation, prompt validation, synthetic inspection, selection materialization, and source comparison.
    - `Makefile` exposes short CPU-safe aliases for Phase 3 validation/report commands.
    - `README.md` links Phase 3 data curriculum/quality docs and states generated reports/images/data remain out of git.
  </behavior>
  <action>Update command docs, README, and Makefile after all Phase 3 CLIs exist. Keep commands local/SLURM-compatible where applicable and clearly mark OCR/model-heavy checks as opt-in. Add `tests/test_data_quality_docs.py` assertions for required docs strings, Makefile targets, and README links.</action>
  <verify>
    <automated>uv run pytest tests/test_data_quality_docs.py -q</automated>
  </verify>
  <done>Users can discover and run Phase 3 workflows through the standard docs and Makefile surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update runtime contracts documentation for Phase 3 artifacts</name>
  <files>docs/runtime_contracts.md, tests/test_data_quality_docs.py</files>
  <behavior>
    - Runtime contracts include dataset manifests, prompt quality reports, synthetic quality reports, contact sheets, selected samples, preference pairs, and source comparison reports.
    - Docs state schema names, producers, consumers, required fields, preflight hooks, and git-safety classification.
    - Existing Phase 2 docs tests continue to pass.
  </behavior>
  <action>Update `docs/runtime_contracts.md` artifact matrix and preflight sections with Phase 3 artifact families and schema names. Add docs-test assertions while preserving current Phase 2 required strings and generated artifact safety guidance.</action>
  <verify>
    <automated>uv run pytest tests/test_data_quality_docs.py tests/test_runtime_docs.py tests/test_runtime_artifacts.py -q</automated>
  </verify>
  <done>Runtime documentation matches the implemented Phase 3 path/artifact contracts and existing runtime docs tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/commands -> user execution | Published commands influence what users run locally or on SLURM. |
| runtime validators -> generated artifacts | Validators inspect user-provided file paths and report readiness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Spoofing | Command docs | mitigate | Document exact module paths and test docs/Makefile strings. |
| T-03-06-02 | Tampering | Artifact validators | mitigate | Validate presence/schema for reports/manifests and aggregate errors. |
| T-03-06-03 | Information disclosure | README/docs | mitigate | Reiterate private prompt/generated image safety and non-committable runtime roots. |
</threat_model>

<verification>
Run `uv run pytest tests/test_data_quality_docs.py tests/test_runtime_docs.py tests/test_runtime_artifacts.py -q` and then `uv run pytest` if context allows.
</verification>

<success_criteria>
All DATA-01 through DATA-07 workflows are documented, path-resolved, and discoverable from the standard command surface with CPU-safe validation and generated-artifact safety preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-curriculum-and-dataset-quality/03-06-SUMMARY.md`.
</output>
