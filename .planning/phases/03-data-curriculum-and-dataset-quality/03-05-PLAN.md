---
phase: 03-data-curriculum-and-dataset-quality
plan: 05
type: execute
wave: 2
depends_on:
  - 03-02
  - 03-03
  - 03-04
files_modified:
  - src/data_quality/source_comparison.py
  - scripts/compare_data_sources.py
  - tests/test_data_source_comparison.py
  - docs/data_source_comparison.md
autonomous: true
requirements:
  - DATA-07
must_haves:
  truths:
    - "User can compare generated-image reward-filtered data against synthetic masked-SFT data."
    - "User can see where each data source is expected to help or fail by coverage, quality, score, and artifact-family evidence."
  artifacts:
    - path: "src/data_quality/source_comparison.py"
      provides: "Generated-vs-synthetic dataset comparison reports"
      exports: ["DataSourceComparison", "compare_data_sources"]
    - path: "scripts/compare_data_sources.py"
      provides: "CLI for data source comparison reports"
    - path: "docs/data_source_comparison.md"
      provides: "Interpretation guidance for reward-filtered generated data vs synthetic masked-SFT data"
  key_links:
    - from: "scripts/compare_data_sources.py"
      to: "src/data_quality/source_comparison.py"
      via: "compare_data_sources call"
      pattern: "compare_data_sources"
    - from: "src/data_quality/source_comparison.py"
      to: "selected_samples/preference_pairs and synthetic quality reports"
      via: "JSON/JSONL report inputs"
      pattern: "selected_samples"
---

<objective>
Add generated-vs-synthetic dataset comparison reports for Phase 3.

Purpose: DATA-07 requires users to compare generated-image reward-filtered data with synthetic masked-SFT data and identify expected strengths/failures before training comparisons.
Output: Comparison module, CLI, tests, and interpretation docs.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-data-curriculum-and-dataset-quality/03-CONTEXT.md
@.planning/phases/03-data-curriculum-and-dataset-quality/03-RESEARCH.md
@.planning/phases/03-data-curriculum-and-dataset-quality/03-PATTERNS.md
@docs/pipeline_inventory.md
@docs/runtime_contracts.md
@docs/dataset_quality.md
@docs/synthetic_quality.md
@docs/data_selection.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement source comparison metrics</name>
  <files>src/data_quality/source_comparison.py, tests/test_data_source_comparison.py</files>
  <behavior>
    - Comparison accepts prompt quality reports/manifests, synthetic quality reports/manifests, selected sample artifacts, and preference pair artifacts as optional inputs.
    - Output includes counts, rare-character coverage overlap/gaps, content/style/resolution distribution differences, score/margin summaries, synthetic mask/contrast health, and provenance links.
    - Output includes explicit expected-help and expected-failure sections for generated reward-filtered data and synthetic masked-SFT data.
  </behavior>
  <action>Create `DataSourceComparison` and `compare_data_sources(...)` in `src/data_quality/source_comparison.py`. Read JSON/JSONL inputs from Phase 3 plans 02-04 and aggregate deterministic metrics. Encode interpretation rules grounded in project context: generated reward-filtered data helps align to actual FLUX outputs/reward preferences; synthetic masked-SFT helps controlled text-region reconstruction; generated data can inherit reward/OCR false positives and prompt distribution gaps; synthetic data can miss natural scene complexity and domain realism.</action>
  <verify>
    <automated>uv run pytest tests/test_data_source_comparison.py -q</automated>
  </verify>
  <done>Tests prove metrics and interpretation sections are produced from tiny JSON/JSONL fixtures with deterministic ordering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add comparison CLI</name>
  <files>scripts/compare_data_sources.py, tests/test_data_source_comparison.py</files>
  <behavior>
    - CLI accepts paths to generated prompt/selection reports and synthetic quality reports.
    - CLI writes JSON comparison report and prints concise summary to stdout.
    - CLI handles missing optional inputs by reporting unavailable evidence, not by fabricating metrics.
  </behavior>
  <action>Add `scripts/compare_data_sources.py` with arguments for generated prompt quality report, selected samples, preference pairs, generated dataset manifest, synthetic quality report, synthetic manifest, output report path, and markdown summary path. Keep missing inputs explicit in `evidence_available` and `evidence_missing` fields.</action>
  <verify>
    <automated>uv run pytest tests/test_data_source_comparison.py -q</automated>
  </verify>
  <done>Users can create a comparison report from Phase 3 artifacts and see which evidence sources are present or missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Document source comparison interpretation</name>
  <files>docs/data_source_comparison.md, tests/test_data_source_comparison.py</files>
  <behavior>
    - Documentation explains generated reward-filtered SFT/DPO data, synthetic masked-SFT data, comparison fields, and expected help/failure interpretations.
    - Tests verify docs mention reward-filtered generated images, synthetic masked-SFT, rare-character coverage, reward/OCR false positives, natural scene realism, manifests, and thesis comparison use.
  </behavior>
  <action>Create `docs/data_source_comparison.md` with command examples, input/output schemas, interpretation guidance, and thesis caveats that training loss or DPO accuracy are internal signals until Phase 6 evaluation validates rendered-text quality.</action>
  <verify>
    <automated>uv run pytest tests/test_data_source_comparison.py -q</automated>
  </verify>
  <done>Docs give users an actionable, reproducible comparison workflow for DATA-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| report artifacts -> comparison tool | JSON/JSONL reports may be incomplete, malformed, or generated from different runs. |
| comparison report -> thesis interpretation | Report can influence research conclusions if provenance and missing evidence are unclear. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Tampering | Input report parsing | mitigate | Validate schemas minimally, record source paths/hashes, and list missing evidence explicitly. |
| T-03-05-02 | Repudiation | Comparison conclusions | mitigate | Include provenance links and evidence availability in every report. |
| T-03-05-03 | Information disclosure | Comparison summaries | accept | Reports use aggregate metrics and paths; users are warned not to publish private prompt/image artifacts. |
</threat_model>

<verification>
Run `uv run pytest tests/test_data_source_comparison.py -q`.
</verification>

<success_criteria>
DATA-07 is satisfied when users can compare generated reward-filtered data against synthetic masked-SFT data with explicit evidence, coverage gaps, expected strengths, expected failures, and provenance.
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-curriculum-and-dataset-quality/03-05-SUMMARY.md`.
</output>
