---
phase: 03-data-curriculum-and-dataset-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data_quality/__init__.py
  - src/data_quality/curriculum.py
  - src/prompt_pipeline/generate.py
  - configs/prompts/simple.json
  - configs/prompts/full.json
  - configs/prompts/curriculum.json
  - tests/test_prompt_curriculum.py
  - docs/data_curriculum.md
autonomous: true
requirements:
  - DATA-01
  - DATA-03
must_haves:
  truths:
    - "User can choose named prompt/data curriculum stages for Cyrillic and multilingual rendering."
    - "User can run prompt generation from explicit simple, full, and curriculum config files."
    - "Prompt generation preserves the existing CLI while removing hidden mode choices from code edits."
  artifacts:
    - path: "src/data_quality/curriculum.py"
      provides: "Prompt curriculum config models, validation, and stage expansion"
      exports: ["PromptGenerationConfig", "CurriculumStage", "load_prompt_generation_config"]
    - path: "configs/prompts/curriculum.json"
      provides: "Versioned curriculum stages for Cyrillic and multilingual prompt generation"
      contains: "single_letters"
    - path: "src/prompt_pipeline/generate.py"
      provides: "Config-driven prompt generation CLI"
      contains: "--config"
  key_links:
    - from: "src/prompt_pipeline/generate.py"
      to: "src/data_quality/curriculum.py"
      via: "load_prompt_generation_config in CLI path"
      pattern: "load_prompt_generation_config"
    - from: "configs/prompts/curriculum.json"
      to: "src/data_quality/curriculum.py"
      via: "JSON config parsed into PromptGenerationConfig"
      pattern: "curriculum"
---

<objective>
Create explicit prompt/data curriculum and config contracts for Phase 3.

Purpose: DATA-01 and DATA-03 require users to define curriculum stages and prompt-generation modes without editing code or monkey-patching constants.
Output: Config models, simple/full/curriculum config files, prompt CLI wiring, docs, and CPU-safe tests.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-data-curriculum-and-dataset-quality/03-CONTEXT.md
@.planning/phases/03-data-curriculum-and-dataset-quality/03-RESEARCH.md
@.planning/phases/03-data-curriculum-and-dataset-quality/03-PATTERNS.md
@docs/pipeline_inventory.md
@src/prompt_pipeline/generate.py
@src/prompt_pipeline/config.py

<interfaces>
Existing prompt generation contract:
```python
def generate_dataset(n: int, output_path: str, llm=None, seed: int = 42, batch_size: int = 1): ...
```

Existing prompt records include:
```python
{
  "id": "p_00000",
  "prompt": prompt,
  "target_text": target_text,
  "tier": tier,
  "content_type": content_type,
  "scene_id": scene["id"],
  "style": style,
  "lang": lang,
  "char_coverage": char_cov,
}
```

Existing config constants available to reuse:
```python
CYRILLIC_LOWER = "абвгдеёжзийклмнопрстуфхцчшщъыьэюя"
RARE_CHARS = set("щЩъЪёЁэЭюЮфФцЦ")
CONTENT_TYPES = {...}
TIER_WEIGHTS = {...}
CASE_WEIGHTS = {...}
LANG_RU_RATIO = 0.80
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define prompt curriculum config contracts</name>
  <files>src/data_quality/__init__.py, src/data_quality/curriculum.py, tests/test_prompt_curriculum.py</files>
  <behavior>
    - Loading `configs/prompts/simple.json` returns a config with one or more curriculum stages and deterministic seed/output settings.
    - Invalid stage weights, empty stage names, unsupported scripts, or negative sample counts raise a clear config error without importing heavy ML modules.
    - Curriculum expansion produces named stages for single letters, short words, phrases, digits, punctuation, mixed case, multiline, style, and scene cases.
  </behavior>
  <action>Create frozen config models in `src/data_quality/curriculum.py` for `PromptGenerationConfig`, `CurriculumStage`, and related threshold/distribution structures. Include `load_prompt_generation_config(path)`, deterministic stage sample allocation, allowed script/character policies, and helpers that map config values onto the existing prompt generator constants. Keep the module CPU-safe and dependency-light; do not import FLUX, Qwen, PaddleOCR, vLLM, MLX, or SynthTIGER.</action>
  <verify>
    <automated>uv run pytest tests/test_prompt_curriculum.py -q</automated>
  </verify>
  <done>Tests prove explicit curriculum configs validate, expand stages deterministically, reject invalid values, and expose all DATA-01 stage families.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add simple, full, and curriculum prompt config files</name>
  <files>configs/prompts/simple.json, configs/prompts/full.json, configs/prompts/curriculum.json, tests/test_prompt_curriculum.py</files>
  <behavior>
    - `simple.json` covers quick CPU-safe no-LLM prompt generation with letters/short words.
    - `full.json` covers the existing broad distribution with explicit content type, tier, style, case, language, seed, model, backend, and output settings.
    - `curriculum.json` contains named stages for single letters, short words, multi-word phrases, digits, punctuation, mixed case, multiline, style-heavy, and scene-heavy examples.
  </behavior>
  <action>Add committed prompt config files under `configs/prompts/` using repository-relative output paths. Ensure each file records `schema_version`, `mode`, `seed`, `output_path`, `generation`, `curriculum_stages`, and validation threshold references. These configs are source contracts only; generated prompt JSONL outputs stay under `data/` or runtime roots and are not committed unless intentionally tiny fixtures.</action>
  <verify>
    <automated>uv run pytest tests/test_prompt_curriculum.py -q</automated>
  </verify>
  <done>All three configs load through `load_prompt_generation_config`, expose explicit modes, and contain the required curriculum stage coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire prompt generation CLI to explicit configs</name>
  <files>src/prompt_pipeline/generate.py, docs/data_curriculum.md, tests/test_prompt_curriculum.py</files>
  <behavior>
    - `python -m src.prompt_pipeline.generate --config configs/prompts/simple.json --no-llm` generates from config values without changing Python constants.
    - Existing flag-only invocation remains runnable and maps to the same defaults as before.
    - CLI errors for invalid config paths are clear and occur before heavy LLM/model imports.
  </behavior>
  <action>Extend `src/prompt_pipeline/generate.py` with `--config` support that loads `PromptGenerationConfig`, resolves sample counts/output/seed/no-LLM/backend/model/batch settings, and tags generated records with stage/mode provenance where available. Preserve existing CLI flags as compatibility inputs. Document the config-driven workflow in `docs/data_curriculum.md` with local command examples and artifact safety notes.</action>
  <verify>
    <automated>uv run pytest tests/test_prompt_curriculum.py -q</automated>
  </verify>
  <done>Users can select simple/full/curriculum prompt modes through config files; tests confirm heavy modules are not imported for config validation and existing CLI behavior remains compatible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user config -> prompt CLI | JSON config values control output paths, sample counts, model IDs, and distribution settings. |
| prompt CLI -> filesystem | Generated prompt JSONL and docs reference local paths that must remain repo-relative or generated-root safe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | `load_prompt_generation_config` | mitigate | Validate schema, stage names, counts, weights, scripts, and paths before generation. |
| T-03-01-02 | Information disclosure | Prompt output paths | mitigate | Reject home/off-repo output paths for committed configs and document generated prompt privacy. |
| T-03-01-03 | Denial of service | Sample counts/batch settings | mitigate | Validate positive bounded values in config tests and fail before model import. |
</threat_model>

<verification>
Run `uv run pytest tests/test_prompt_curriculum.py -q`. Confirm `python -m src.prompt_pipeline.generate --help` shows `--config` without importing heavy runtime modules.
</verification>

<success_criteria>
DATA-01 and DATA-03 are satisfied for planning scope when prompt generation modes are explicit config files, curriculum stages are named and validated, and the existing prompt CLI can consume those configs without monkey-patching.
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-curriculum-and-dataset-quality/03-01-SUMMARY.md`.
</output>
