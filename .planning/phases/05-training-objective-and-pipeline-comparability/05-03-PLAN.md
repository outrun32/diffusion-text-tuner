---
phase: 05-training-objective-and-pipeline-comparability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/training/comparability.py
  - scripts/check_training_comparability.py
  - tests/test_training_comparability.py
  - docs/training_comparability.md
autonomous: true
requirements: [TRN-05, TRN-06]
must_haves:
  truths:
    - "User can detect training/inference mismatches before comparing run outputs."
    - "User can define controlled comparison inputs for baseline, SFT, DPO, masked-SFT, and combined/curriculum approaches."
    - "Mismatch reports explicitly identify prompts, seeds, inference settings, data sources, rewards, metrics, and artifact paths."
  artifacts:
    - path: "src/training/comparability.py"
      provides: "Pure comparability checks and report formatting"
      exports: ["compare_training_configs", "format_comparability_report"]
    - path: "scripts/check_training_comparability.py"
      provides: "CLI for config/manifest mismatch reports"
      contains: "--left-config"
    - path: "docs/training_comparability.md"
      provides: "User guide for controlled training comparisons"
      contains: "training/inference mismatches"
  key_links:
    - from: "scripts/check_training_comparability.py"
      to: "src/training/comparability.py"
      via: "CLI delegates pure comparison logic"
      pattern: 'from src\.training\.comparability import'
---

<objective>
Add a CPU-safe comparability layer that identifies uncontrolled differences before training outputs are compared.

Purpose: TRN-05 and TRN-06 require controlled prompts, seeds, inference settings, data sources, rewards, metrics, and mismatch detection.
Output: Pure comparability module, CLI, tests, and user guide.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-training-objective-and-pipeline-comparability/05-RESEARCH.md
@src/runtime/config_io.py
@src/runtime/manifests.py
@docs/commands.md

<interfaces>
From `src.runtime.config_io`:
```python
def load_stage_config(stage: str, path: str | Path) -> SFTConfig | DPOConfig | MaskedSFTConfig: ...
def resolve_config_snapshot(config: SFTConfig | DPOConfig | MaskedSFTConfig) -> dict[str, Any]: ...
```
From `src.runtime.manifests`:
```python
def load_run_manifest(path: str | Path) -> RunManifest: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify mismatch and controlled-field checks</name>
  <files>tests/test_training_comparability.py</files>
  <read_first>src/runtime/config_io.py, src/runtime/manifests.py, .planning/phases/05-training-objective-and-pipeline-comparability/05-RESEARCH.md</read_first>
  <behavior>
    - Pure dict comparison reports mismatches for `num_training_steps`, `num_inference_steps`, `guidance_scale`, `prompt_embedding_padding`, `model_id`, `seed`, `sample_prompt`, `sample_target_text`, `latents_dir`, `text_embeds_dir`, `scores_csv`, `data_dir`, `score_column`, and reward/scorer fields.
    - Report severity is `blocking` for model ID, prompt set, seed, and inference setting mismatches; `warning` for metric/artifact availability differences.
    - A controlled comparison with identical controlled fields returns no blocking mismatches.
  </behavior>
  <action>Create failing tests for `compare_training_configs(left, right, left_label="sft", right_label="dpo")` and `format_comparability_report(report)`. Use plain dictionaries rather than trainer imports. Assert exact report keys: `schema_version`, `left_label`, `right_label`, `blocking_mismatches`, `warnings`, `controlled_fields`, and `summary`. Include cases for prompt embedding padding and guidance mismatches even if current configs lack those keys, so missing-vs-present differences are explicit.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_training_comparability.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_training_comparability.py` imports `src.training.comparability`.
    - The targeted pytest command fails before implementation because the module is missing.
  </acceptance_criteria>
  <done>Mismatch detection behavior is specified before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pure comparability report logic</name>
  <files>src/training/comparability.py, tests/test_training_comparability.py</files>
  <read_first>tests/test_training_comparability.py, src/runtime/config_io.py, src/runtime/manifests.py, src/training/config.py</read_first>
  <behavior>
    - Pure comparison accepts dictionaries, dataclasses via `asdict`, or loaded manifest config snapshots.
    - Missing controlled fields are reported as `missing_left` or `missing_right` instead of being ignored.
    - Report formatting is deterministic Markdown with `## Blocking mismatches` and `## Warnings` sections.
  </behavior>
  <action>Create `src/training/comparability.py` with `COMPARABILITY_SCHEMA_VERSION = "training-comparability/v1"`, controlled field groups (`training`, `inference`, `prompt`, `model`, `data_source`, `reward`, `metrics`, `artifacts`), `compare_training_configs(...)`, `compare_training_manifests(...)`, and `format_comparability_report(...)`. Keep the module import-safe: standard library only plus optional imports from `src.runtime.manifests` inside manifest-specific functions. Do not import torch, diffusers, transformers, accelerate, or PEFT.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_training_comparability.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/training/comparability.py` contains `COMPARABILITY_SCHEMA_VERSION = "training-comparability/v1"`.
    - Missing controlled fields are represented in report output.
    - Targeted pytest exits 0.
  </acceptance_criteria>
  <done>Pure comparability reports identify controlled-field mismatches deterministically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add comparability CLI and guide</name>
  <files>scripts/check_training_comparability.py, tests/test_training_comparability.py, docs/training_comparability.md</files>
  <read_first>src/training/comparability.py, tests/test_training_comparability.py, docs/commands.md, README.md</read_first>
  <behavior>
    - CLI compares either `--left-config/--left-stage` with `--right-config/--right-stage` or `--left-manifest` with `--right-manifest`.
    - CLI exits 1 when blocking mismatches exist unless `--allow-blocking` is provided.
    - Docs explain controlled comparison fields and exact commands without requiring GPU/model work.
  </behavior>
  <action>Create `scripts/check_training_comparability.py` with argparse options `--left-config`, `--left-stage`, `--right-config`, `--right-stage`, `--left-manifest`, `--right-manifest`, `--markdown`, `--output`, and `--allow-blocking`. Config mode must call `load_stage_config` and `resolve_config_snapshot`; manifest mode must call `compare_training_manifests`. Add tests for exit code 1 on blocking mismatch and exit code 0 with `--allow-blocking`. Create `docs/training_comparability.md` with sections `Controlled fields`, `Training/inference mismatches`, `Config comparison command`, `Manifest comparison command`, and `Generated artifact safety`.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_training_comparability.py -q && PATH="/root/.local/bin:$PATH" uv run --extra lint ruff check src/training/comparability.py scripts/check_training_comparability.py tests/test_training_comparability.py</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check_training_comparability.py` contains `--left-config`, `--right-config`, `--left-manifest`, `--right-manifest`, and `--allow-blocking`.
    - `docs/training_comparability.md` contains `training/inference mismatches` and `prompt_embedding_padding`.
    - Targeted pytest and Ruff both exit 0.
  </acceptance_criteria>
  <done>Users can run documented CPU-safe mismatch checks before comparing training approaches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config/manifest JSON → mismatch report | Local user-controlled files determine reported comparability status. |
| CLI mismatch status → user decisions | Exit codes may block or allow comparison workflows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | T | config parsing | mitigate | Use existing `load_stage_config` validation in CLI config mode and pure dict tests for malformed/missing fields. |
| T-05-03-02 | R | mismatch reports | mitigate | Include labels, field groups, exact left/right values for controlled fields, and deterministic Markdown output. |
| T-05-03-03 | D | false blocking in automation | mitigate | Exit 1 only for `blocking_mismatches`; provide explicit `--allow-blocking` override tested by CLI tests. |
</threat_model>

<verification>
Run targeted comparability tests and Ruff on new module/CLI/test file.
</verification>

<success_criteria>
- TRN-05 and TRN-06 have a CPU-safe mismatch detector usable before long GPU runs.
- Reports include controlled prompts, seeds, inference settings, data sources, rewards, metrics, and artifact paths.
- CLI behavior is documented and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/05-training-objective-and-pipeline-comparability/05-03-SUMMARY.md`.
</output>
