---
phase: 05-training-objective-and-pipeline-comparability
plan: 06
type: execute
wave: 3
depends_on: ["05-02", "05-03", "05-04", "05-05"]
files_modified:
  - scripts/compare_training_runs.py
  - tests/test_training_comparison_docs.py
  - docs/training_comparability.md
  - docs/commands.md
  - README.md
  - Makefile
autonomous: true
requirements: [TRN-05, RUN-02, TRN-07, STR-04]
must_haves:
  truths:
    - "User can run one documented CPU-safe command to summarize controlled training-run comparability."
    - "Command docs cover baseline, SFT, DPO, masked-SFT, combined, and curriculum comparison posture."
    - "Makefile/README/docs stay synchronized through docs drift tests."
  artifacts:
    - path: "scripts/compare_training_runs.py"
      provides: "Integrated CLI combining manifest diff and comparability mismatch checks"
      contains: "compare_run_manifests"
    - path: "tests/test_training_comparison_docs.py"
      provides: "Docs drift and integrated CLI tests"
      contains: "compare-training-runs"
    - path: "Makefile"
      provides: "Make alias for Phase 5 comparison checks"
      contains: "compare-training-runs"
  key_links:
    - from: "scripts/compare_training_runs.py"
      to: "src/runtime/manifest_diff.py"
      via: "integrated run diff"
      pattern: "compare_run_manifests"
    - from: "scripts/compare_training_runs.py"
      to: "src/training/comparability.py"
      via: "integrated mismatch report"
      pattern: "compare_training_manifests"
---

<objective>
Publish the Phase 5 comparison command surface tying manifests, mismatch detection, shared seams, and docs together.

Purpose: TRN-05 requires controlled approach comparisons; RUN-02, TRN-07, and STR-04 must be discoverable through the standard command surface.
Output: Integrated comparison CLI, docs drift tests, command docs, README link, and Makefile alias.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-training-objective-and-pipeline-comparability/05-RESEARCH.md
@.planning/phases/05-training-objective-and-pipeline-comparability/05-02-SUMMARY.md
@.planning/phases/05-training-objective-and-pipeline-comparability/05-03-SUMMARY.md
@.planning/phases/05-training-objective-and-pipeline-comparability/05-04-SUMMARY.md
@.planning/phases/05-training-objective-and-pipeline-comparability/05-05-SUMMARY.md
@src/runtime/manifest_diff.py
@src/training/comparability.py
@docs/commands.md
@README.md
@Makefile

<interfaces>
From Plan 05-02 expected output:
```python
from src.runtime.manifest_diff import compare_run_manifests, format_manifest_diff_markdown
```
From Plan 05-03 expected output:
```python
from src.training.comparability import compare_training_manifests, format_comparability_report
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify integrated comparison CLI and docs drift</name>
  <files>tests/test_training_comparison_docs.py</files>
  <read_first>src/runtime/manifest_diff.py, src/training/comparability.py, docs/commands.md, README.md, Makefile</read_first>
  <behavior>
    - Integrated CLI reads `--left-manifest` and `--right-manifest` and writes a JSON report with both `manifest_diff` and `comparability` keys.
    - Markdown output contains `# Training run comparison`, `## Manifest diff`, and `## Comparability mismatches`.
    - Docs/Makefile/README mention the exact alias `compare-training-runs` and the exact script `python -m scripts.compare_training_runs`.
  </behavior>
  <action>Create failing tests in `tests/test_training_comparison_docs.py` for `scripts.compare_training_runs.main(argv)`, docs strings in `docs/commands.md`, `README.md`, and `Makefile` dry-run output. Use tiny manifest JSON fixtures under `tmp_path`; do not create real `runs/` artifacts. Assert the docs mention baseline, SFT, DPO, masked-SFT, combined, and curriculum comparison approaches.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_training_comparison_docs.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_training_comparison_docs.py` imports `scripts.compare_training_runs`.
    - Targeted pytest fails before implementation because the integrated script/docs are missing.
  </acceptance_criteria>
  <done>Integrated comparison command/docs behavior is specified by failing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement integrated training comparison CLI</name>
  <files>scripts/compare_training_runs.py, tests/test_training_comparison_docs.py</files>
  <read_first>src/runtime/manifest_diff.py, src/training/comparability.py, tests/test_training_comparison_docs.py, scripts/compare_run_manifests.py, scripts/check_training_comparability.py</read_first>
  <behavior>
    - CLI combines manifest diff and comparability report without launching training, CUDA, FLUX, OCR, or model code.
    - CLI exits 1 when comparability has blocking mismatches unless `--allow-blocking` is present.
    - JSON output is stable and Markdown output is deterministic.
  </behavior>
  <action>Create `scripts/compare_training_runs.py` with argparse flags `--left-manifest`, `--right-manifest`, `--markdown`, `--output`, and `--allow-blocking`. The JSON payload must include `schema_version: "training-run-comparison/v1"`, `manifest_diff`, and `comparability`. For Markdown, include the manifest diff Markdown and comparability Markdown under clear headings. Use `compare_run_manifests` and `compare_training_manifests`; do not duplicate their comparison logic.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_training_comparison_docs.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/compare_training_runs.py` contains `training-run-comparison/v1`, `compare_run_manifests`, and `compare_training_manifests`.
    - Targeted pytest exits 0 for integrated CLI behavior.
  </acceptance_criteria>
  <done>Integrated CLI produces comparison reports from two recorded run manifests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Publish Phase 5 command surface</name>
  <files>docs/training_comparability.md, docs/commands.md, README.md, Makefile, tests/test_training_comparison_docs.py</files>
  <read_first>docs/training_comparability.md, docs/commands.md, README.md, Makefile, tests/test_training_comparison_docs.py</read_first>
  <behavior>
    - `docs/commands.md` has a Phase 5 section with exact commands for selection materialization, config comparability, manifest diff, and integrated run comparison.
    - `Makefile` exposes `compare-training-runs` as a dry-run-safe documented alias requiring `LEFT_MANIFEST` and `RIGHT_MANIFEST` variables.
    - `README.md` links to `docs/training_comparability.md` and names Phase 5 comparability commands.
  </behavior>
  <action>Update `docs/commands.md` with section `Phase 5 training comparability` and commands: `python -m scripts.compare_run_manifests --left runs/<a>/manifest.json --right runs/<b>/manifest.json`, `python -m scripts.check_training_comparability --left-manifest runs/<a>/manifest.json --right-manifest runs/<b>/manifest.json`, and `python -m scripts.compare_training_runs --left-manifest runs/<a>/manifest.json --right-manifest runs/<b>/manifest.json --markdown --output runs/comparisons/training-run-comparison.md`. Add Makefile target `compare-training-runs` that invokes the integrated CLI using `$(LEFT_MANIFEST)` and `$(RIGHT_MANIFEST)`. Update README to point users to `docs/training_comparability.md`. Extend docs tests to guard all exact strings and `make -n compare-training-runs LEFT_MANIFEST=runs/a/manifest.json RIGHT_MANIFEST=runs/b/manifest.json`.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_training_comparison_docs.py tests/test_training_comparability.py tests/test_runtime_manifest_diff.py -q && make -n compare-training-runs LEFT_MANIFEST=runs/a/manifest.json RIGHT_MANIFEST=runs/b/manifest.json && PATH="/root/.local/bin:$PATH" uv run --extra lint ruff check scripts/compare_training_runs.py tests/test_training_comparison_docs.py</automated>
  </verify>
  <acceptance_criteria>
    - `docs/commands.md` contains `Phase 5 training comparability`.
    - `README.md` contains `docs/training_comparability.md`.
    - `Makefile` contains `compare-training-runs`.
    - Combined pytest, Makefile dry-run, and Ruff commands exit 0.
  </acceptance_criteria>
  <done>Phase 5 comparison capabilities are discoverable through the standard project command surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local manifests → integrated comparison report | User-controlled JSON determines comparison output and exit status. |
| docs/Makefile commands → user execution | Published commands steer users toward safe or unsafe workflows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | R | integrated report | mitigate | Include both manifest diff and comparability sections with left/right run IDs inherited from source reports. |
| T-05-06-02 | D | accidental heavy execution | mitigate | CLI only reads manifests; docs state comparison commands are CPU-safe and do not launch training/model/OCR work. |
| T-05-06-03 | T | command docs drift | mitigate | Docs drift tests and Makefile dry-run assert exact command strings and aliases. |
</threat_model>

<verification>
Run integrated docs/comparison tests, targeted dependency tests from Plans 05-02/05-03, Makefile dry-run, and Ruff on the new CLI/docs test.
</verification>

<success_criteria>
- TRN-05 is operationalized through documented controlled-comparison commands.
- RUN-02 manifest diffs and TRN-06 mismatch detection are discoverable together.
- TRN-07/STR-04 shared seams are linked from Phase 5 docs and README.
</success_criteria>

<output>
After completion, create `.planning/phases/05-training-objective-and-pipeline-comparability/05-06-SUMMARY.md`.
</output>
