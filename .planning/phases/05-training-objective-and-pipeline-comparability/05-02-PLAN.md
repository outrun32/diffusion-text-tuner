---
phase: 05-training-objective-and-pipeline-comparability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/runtime/manifest_diff.py
  - scripts/compare_run_manifests.py
  - tests/test_runtime_manifest_diff.py
  - docs/runtime_contracts.md
autonomous: true
requirements: [RUN-02]
must_haves:
  truths:
    - "User can compare two local run manifests and see changed configs, data sources, rewards, seeds, inference settings, metrics, and artifacts."
    - "Manifest comparison is CPU-safe and reads only local JSON manifests/config snapshots."
    - "Secret and cache metadata remain presence-only in diff output."
  artifacts:
    - path: "src/runtime/manifest_diff.py"
      provides: "Manifest diff model and pure comparison function"
      exports: ["compare_run_manifests"]
    - path: "scripts/compare_run_manifests.py"
      provides: "CLI for JSON and Markdown run-manifest diffs"
      contains: "--left"
    - path: "tests/test_runtime_manifest_diff.py"
      provides: "CPU-safe diff fixture coverage"
      contains: "config_changes"
  key_links:
    - from: "scripts/compare_run_manifests.py"
      to: "src/runtime/manifest_diff.py"
      via: "CLI delegates to compare_run_manifests"
      pattern: 'from src\.runtime\.manifest_diff import compare_run_manifests'
---

<objective>
Add the RUN-02 manifest diff surface needed for training-run comparability.

Purpose: Users need a deterministic way to see what changed between recorded runs before using results as evidence.
Output: CPU-safe manifest diff module, CLI, tests, and runtime docs.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-training-objective-and-pipeline-comparability/05-RESEARCH.md
@.planning/phases/02-runtime-contracts-and-run-provenance/02-03-SUMMARY.md
@src/runtime/manifests.py
@scripts/run_manifest.py
@docs/runtime_contracts.md

<interfaces>
From `src/runtime/manifests.py`:
```python
MANIFEST_SCHEMA_VERSION = "run-manifest/v1"
@dataclass(frozen=True)
class RunManifest:
    config_snapshot: dict[str, Any]
    seeds: dict[str, Any]
    models: dict[str, Any]
    inputs: dict[str, Any]
    outputs: dict[str, Any]
    metrics: dict[str, Any]
def load_run_manifest(path: str | Path) -> RunManifest: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify manifest diff categories</name>
  <files>tests/test_runtime_manifest_diff.py</files>
  <read_first>src/runtime/manifests.py, tests/test_runtime_manifests.py, docs/runtime_contracts.md</read_first>
  <behavior>
    - Comparing two tiny manifest JSON files returns sections `config_changes`, `data_source_changes`, `reward_changes`, `seed_changes`, `inference_changes`, `metric_changes`, and `artifact_changes`.
    - Diff output includes `left_run_id`, `right_run_id`, and `schema_version`.
    - Environment secret/cache fields are diffed as booleans or presence strings only, never raw token/path values.
  </behavior>
  <action>Create failing tests in `tests/test_runtime_manifest_diff.py` using pytest `tmp_path` manifests with embedded `config_snapshot` dictionaries. Include config keys `stage`, `score_threshold`, `pair_construction_mode`, `seed`, `num_inference_steps`, `guidance_scale`, `scores_csv`, `reward_model`, and output paths. Assert the diff categorizes changed keys into the exact section names above and can render Markdown with headings `## Config changes`, `## Data source changes`, `## Reward changes`, `## Seed changes`, `## Inference changes`, `## Metric changes`, and `## Artifact changes`.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_runtime_manifest_diff.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_runtime_manifest_diff.py` exists and imports `src.runtime.manifest_diff`.
    - The targeted pytest command fails before implementation because `src.runtime.manifest_diff` is missing.
  </acceptance_criteria>
  <done>RUN-02 diff behavior is specified by failing CPU-safe tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pure manifest diff module</name>
  <files>src/runtime/manifest_diff.py, tests/test_runtime_manifest_diff.py</files>
  <read_first>src/runtime/manifests.py, tests/test_runtime_manifest_diff.py, src/runtime/reproducibility.py</read_first>
  <behavior>
    - `compare_run_manifests(left, right)` accepts manifest paths or loaded `RunManifest` objects.
    - Returned payload is JSON-serializable with stable sorted keys.
    - Markdown rendering is deterministic and omits empty sections only when the section has no changes.
  </behavior>
  <action>Create `src/runtime/manifest_diff.py` with `DIFF_SCHEMA_VERSION = "run-manifest-diff/v1"`, `compare_run_manifests(left, right) -> dict[str, Any]`, and `format_manifest_diff_markdown(diff: Mapping[str, Any]) -> str`. Categorize keys as: data sources for `scores_csv`, `latents_dir`, `text_embeds_dir`, `data_dir`, `selected_samples_path`, `preference_pairs_path`; rewards for keys containing `reward`, `scorer`, or `score_column`; seeds for keys ending in `seed` or equal to `seeds`; inference for `num_inference_steps`, `guidance_scale`, `resolution`, `sample_prompt`, `sample_target_text`, and `prompt_embedding_padding`; metrics from manifest `metrics`; artifacts from manifest `outputs`; all other config changes in `config_changes`. Use only `load_run_manifest` and JSON/dict traversal; do not import torch, diffusers, transformers, or OCR libraries.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_runtime_manifest_diff.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/runtime/manifest_diff.py` contains `DIFF_SCHEMA_VERSION = "run-manifest-diff/v1"`.
    - Diff payload has `left_run_id`, `right_run_id`, and all seven change sections.
    - Targeted pytest exits 0.
  </acceptance_criteria>
  <done>Manifest diff module categorizes comparison-relevant run differences without heavy imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add manifest diff CLI and docs</name>
  <files>scripts/compare_run_manifests.py, tests/test_runtime_manifest_diff.py, docs/runtime_contracts.md</files>
  <read_first>scripts/run_manifest.py, src/runtime/manifest_diff.py, tests/test_runtime_manifest_diff.py, docs/runtime_contracts.md, docs/commands.md</read_first>
  <behavior>
    - CLI prints JSON by default and Markdown with `--markdown`.
    - CLI supports `--output` for writing JSON or Markdown while still returning exit code 0.
    - Runtime docs show exact command examples for comparing two manifests.
  </behavior>
  <action>Create `scripts/compare_run_manifests.py` with argparse flags `--left`, `--right`, `--markdown`, and `--output`. Implement `main(argv: list[str] | None = None) -> int` matching existing script patterns and return `2` with concise stderr on malformed input. Extend tests to call `main([...])` directly and assert stdout/file outputs. Update `docs/runtime_contracts.md` with a `Run manifest diff` section and commands `python -m scripts.compare_run_manifests --left runs/<a>/manifest.json --right runs/<b>/manifest.json` and the same command with `--markdown --output runs/comparisons/run-diff.md`.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_runtime_manifest_diff.py -q && PATH="/root/.local/bin:$PATH" uv run --extra lint ruff check src/runtime/manifest_diff.py scripts/compare_run_manifests.py tests/test_runtime_manifest_diff.py</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/compare_run_manifests.py` contains `--left`, `--right`, `--markdown`, and `--output`.
    - `docs/runtime_contracts.md` contains `python -m scripts.compare_run_manifests`.
    - Targeted pytest and Ruff both exit 0.
  </acceptance_criteria>
  <done>Users can compare local run manifests from a CPU-safe CLI and documented commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local manifest JSON → diff report | Local files are parsed and transformed into user-facing comparison output. |
| CLI args → filesystem read/write | User-supplied manifest/output paths control reads and optional report writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | T | `load_run_manifest` inputs | mitigate | Reuse existing manifest loader validation and add malformed-input CLI tests returning code 2. |
| T-05-02-02 | I | environment/cache/secret metadata | mitigate | Diff only existing presence flags from manifests; never read environment variables or cache paths directly. |
| T-05-02-03 | R | diff output provenance | mitigate | Include left/right manifest paths and run IDs in JSON/Markdown output. |
</threat_model>

<verification>
Run `PATH="/root/.local/bin:$PATH" uv run pytest tests/test_runtime_manifest_diff.py -q` and Ruff on the new diff module, CLI, and test file.
</verification>

<success_criteria>
- RUN-02 is covered by an import-safe manifest diff module and CLI.
- Diff reports classify configs, data sources, rewards, seeds, inference settings, metrics, and artifacts.
- No heavy ML dependencies are imported.
</success_criteria>

<output>
After completion, create `.planning/phases/05-training-objective-and-pipeline-comparability/05-02-SUMMARY.md`.
</output>
