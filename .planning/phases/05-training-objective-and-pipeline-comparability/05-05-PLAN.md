---
phase: 05-training-objective-and-pipeline-comparability
plan: 05
type: execute
wave: 2
depends_on: ["05-03"]
files_modified:
  - src/training/sampling.py
  - src/training/checkpointing.py
  - src/training/schedulers.py
  - src/training/runtime.py
  - tests/test_training_shared_utilities.py
  - docs/training_comparability.md
autonomous: true
requirements: [TRN-07, STR-04]
must_haves:
  truths:
    - "User can add or modify trainer variants through focused shared training modules rather than unrelated scripts."
    - "Shared sampling, checkpointing, scheduler, and runtime helpers are import-safe and CPU-testable."
    - "Existing SFT, DPO, and masked-SFT trainer behavior is preserved by compatibility-focused helper extraction."
  artifacts:
    - path: "src/training/sampling.py"
      provides: "Sampling interval and prompt-suite helper contracts"
      exports: ["should_sample_step"]
    - path: "src/training/checkpointing.py"
      provides: "Checkpoint interval/path helper contracts"
      exports: ["should_save_checkpoint"]
    - path: "src/training/schedulers.py"
      provides: "Import-safe schedule helper re-exports/wrappers"
      exports: ["compute_sigma"]
    - path: "src/training/runtime.py"
      provides: "Training runtime metadata helper contracts"
      exports: ["training_run_inputs"]
  key_links:
    - from: "tests/test_training_shared_utilities.py"
      to: "src/training/sampling.py"
      via: "direct pure helper tests"
      pattern: 'from src\.training\.sampling import'
---

<objective>
Create focused shared training utility modules that support new variants without expanding large trainer files.

Purpose: TRN-07 and STR-04 require stable seams for sampling, checkpointing, schedulers, objective helpers, and runtime plumbing while preserving current behavior.
Output: Import-safe shared modules, tests, and extension guidance.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-training-objective-and-pipeline-comparability/05-RESEARCH.md
@.planning/phases/04-cpu-safe-characterization-tests/04-03-SUMMARY.md
@src/training/dpo_objective.py
@src/training/sft_trainer.py
@src/training/dpo_trainer.py
@src/training/masked_sft_trainer.py
@docs/training_comparability.md

<interfaces>
From `src/training/dpo_objective.py`:
```python
def compute_sigma(t: torch.Tensor, shift: float = 3.0) -> torch.Tensor: ...
def time_dependent_beta(t: torch.Tensor, beta_conf: float, shift: float = 3.0) -> torch.Tensor: ...
def compute_dpo_objective(...) -> tuple[torch.Tensor, dict[str, torch.Tensor]]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify shared utility contracts</name>
  <files>tests/test_training_shared_utilities.py</files>
  <read_first>src/training/dpo_objective.py, src/training/sft_trainer.py, src/training/dpo_trainer.py, src/training/masked_sft_trainer.py, .planning/codebase/ARCHITECTURE.md</read_first>
  <behavior>
    - `should_sample_step(step, interval)` returns false for interval 0 and true when `step > 0 and step % interval == 0`.
    - `should_save_checkpoint(step, interval)` follows the same interval semantics for checkpoints.
    - `normalize_eval_suite_items(items, limit)` returns deterministic prompt/target dictionaries without mutating input objects.
    - Scheduler wrappers re-export DPO `compute_sigma` behavior exactly.
    - Runtime helper returns sorted manifest input/output metadata from config snapshots without touching files.
  </behavior>
  <action>Create failing tests in `tests/test_training_shared_utilities.py` for `src.training.sampling`, `src.training.checkpointing`, `src.training.schedulers`, and `src.training.runtime`. Include an import-safety test asserting importing these modules does not newly import `diffusers`, `transformers`, `accelerate`, `peft`, `paddleocr`, or `synthtiger`. Use only dictionaries and tiny tensors where needed for scheduler parity.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_training_shared_utilities.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_training_shared_utilities.py` imports all four planned modules.
    - Targeted pytest fails before implementation because modules are missing.
  </acceptance_criteria>
  <done>Shared module contracts are specified before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement import-safe shared modules</name>
  <files>src/training/sampling.py, src/training/checkpointing.py, src/training/schedulers.py, src/training/runtime.py, tests/test_training_shared_utilities.py</files>
  <read_first>tests/test_training_shared_utilities.py, src/training/dpo_objective.py, src/runtime/manifests.py, src/runtime/config_io.py</read_first>
  <behavior>
    - Helpers are pure, deterministic, and do not mutate input dictionaries/lists.
    - `src.training.schedulers.compute_sigma` delegates to `src.training.dpo_objective.compute_sigma`.
    - Runtime metadata helper recognizes `latents_dir`, `text_embeds_dir`, `scores_csv`, `data_dir`, `selected_samples_path`, `preference_pairs_path`, `output_dir`, and `eval_suite_path` keys.
  </behavior>
  <action>Create four new modules with `from __future__ import annotations`, small public functions, and module docstrings. `sampling.py` should define `should_sample_step(step: int, interval: int) -> bool` and `normalize_eval_suite_items(items: Sequence[Mapping[str, Any]], limit: int | None = None) -> list[dict[str, Any]]`. `checkpointing.py` should define `should_save_checkpoint(step: int, interval: int) -> bool` and `checkpoint_dir(output_dir: str | Path, step: int) -> Path` returning `<output_dir>/checkpoints/step_<step:06d>`. `schedulers.py` should import and re-export `compute_sigma` and `time_dependent_beta` from `src.training.dpo_objective`. `runtime.py` should define `training_run_inputs(config_snapshot: Mapping[str, Any]) -> dict[str, Any]` and `training_run_outputs(config_snapshot: Mapping[str, Any]) -> dict[str, Any]` using sorted keys only.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_training_shared_utilities.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All four modules exist with the public functions named in the action.
    - Import-safety test confirms no heavy optional modules are newly imported.
    - Targeted pytest exits 0.
  </acceptance_criteria>
  <done>Shared training utility modules are import-safe and covered by CPU tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Document trainer extension seams</name>
  <files>docs/training_comparability.md, tests/test_training_shared_utilities.py</files>
  <read_first>docs/training_comparability.md, tests/test_training_shared_utilities.py, .planning/codebase/ARCHITECTURE.md, .planning/codebase/CONVENTIONS.md</read_first>
  <behavior>
    - Docs name the shared modules and state which responsibility belongs in each.
    - Docs explicitly instruct not to add unrelated sampling/checkpoint/scheduler/runtime code directly to large trainer modules.
    - Docs show how a new trainer variant should use config validation, selection artifacts, manifest metadata, comparability checks, and shared helpers.
  </behavior>
  <action>Extend `docs/training_comparability.md` with a section `Shared trainer seams` containing exact module names `src.training.sampling`, `src.training.checkpointing`, `src.training.schedulers`, and `src.training.runtime`. Add a section `Adding a trainer variant` with a numbered flow: define config fields, materialize data artifacts, validate config through `load_stage_config`, use shared sampling/checkpointing/scheduler/runtime helpers, create/compare run manifests, run CPU-safe tests. Add docs assertions to `tests/test_training_shared_utilities.py` for these exact strings.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_training_shared_utilities.py -q && PATH="/root/.local/bin:$PATH" uv run --extra lint ruff check src/training/sampling.py src/training/checkpointing.py src/training/schedulers.py src/training/runtime.py tests/test_training_shared_utilities.py</automated>
  </verify>
  <acceptance_criteria>
    - `docs/training_comparability.md` contains `Shared trainer seams` and `Adding a trainer variant`.
    - Targeted pytest and Ruff both exit 0.
  </acceptance_criteria>
  <done>Users have documented shared seams for adding trainer variants without hidden unrelated edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config snapshot dicts → runtime metadata | Local config values become manifest input/output metadata. |
| shared helper contracts → trainer variants | Helper semantics influence later trainer behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | T | shared helper semantics | mitigate | CPU tests lock interval, scheduler, path, and metadata behavior with exact expected outputs. |
| T-05-05-02 | D | import-time heavy dependencies | mitigate | Import-safety test asserts helpers do not newly import CUDA/model/OCR stacks. |
| T-05-05-03 | R | extension guidance | mitigate | Docs name exact modules and flow so new variants are traceable through configs, manifests, and comparability checks. |
</threat_model>

<verification>
Run `PATH="/root/.local/bin:$PATH" uv run pytest tests/test_training_shared_utilities.py -q` and Ruff on shared modules/tests.
</verification>

<success_criteria>
- STR-04 shared training modules exist for sampling, checkpointing, schedulers, and runtime plumbing.
- TRN-07 extension seams are documented and tested without modifying trainer loops.
- Existing DPO objective helper remains the source for scheduler math.
</success_criteria>

<output>
After completion, create `.planning/phases/05-training-objective-and-pipeline-comparability/05-05-SUMMARY.md`.
</output>
