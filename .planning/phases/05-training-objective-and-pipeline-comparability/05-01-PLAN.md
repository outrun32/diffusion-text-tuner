---
phase: 05-training-objective-and-pipeline-comparability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/training/selection.py
  - scripts/materialize_training_data.py
  - tests/test_training_selection_artifacts.py
  - docs/data_selection.md
autonomous: true
requirements: [TRN-02, TRN-03]
must_haves:
  truths:
    - "User can materialize and name distinct SFT sample-selection modes for comparison-grade training."
    - "User can materialize and name distinct DPO pair-construction modes with strict winner/loser semantics."
    - "Selection artifacts record mode, thresholds, weights/margins, source score hash, counts, and filtering stats."
  artifacts:
    - path: "src/training/selection.py"
      provides: "Pure CPU-safe SFT/DPO selection modes and artifact summaries"
      contains: "materialize_sft_samples"
    - path: "scripts/materialize_training_data.py"
      provides: "CLI flags for explicit SFT and DPO mode materialization"
      contains: "--mode"
    - path: "tests/test_training_selection_artifacts.py"
      provides: "Tiny fixture coverage for explicit SFT and DPO modes"
      contains: "score_weighted"
  key_links:
    - from: "scripts/materialize_training_data.py"
      to: "src/training/selection.py"
      via: "CLI imports materialize_sft_samples/materialize_dpo_pairs"
      pattern: 'from src\.training\.selection import materialize_dpo_pairs, materialize_sft_samples'
    - from: "docs/data_selection.md"
      to: "scripts/materialize_training_data.py"
      via: "documented command examples"
      pattern: "materialize_training_data.py --kind"
---

<objective>
Make SFT and DPO data-mode choices explicit before trainer comparability work depends on them.

Purpose: TRN-02 and TRN-03 require users to distinguish SFT sample-selection and DPO pair-construction modes instead of relying on hidden constructor behavior.
Output: Extended selection helpers, CLI flags, tests, and docs for explicit mode artifacts.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-training-objective-and-pipeline-comparability/05-RESEARCH.md
@.planning/phases/03-data-curriculum-and-dataset-quality/03-04-SUMMARY.md
@src/training/selection.py
@scripts/materialize_training_data.py
@tests/test_training_selection_artifacts.py
@docs/data_selection.md

<interfaces>
From `src/training/selection.py`:
```python
SELECTED_SAMPLES_SCHEMA_VERSION = "selected-samples/v1"
PREFERENCE_PAIRS_SCHEMA_VERSION = "preference-pairs/v1"
def materialize_sft_samples(scores_csv, output_path, mode="threshold", score_column="score", threshold=0.3, top_k_per_prompt=1, manifest_path=None) -> dict[str, Any]: ...
def materialize_dpo_pairs(scores_csv, output_path, mode="best_vs_worst", score_column="score", threshold=0.5, margin=0.1, ambiguity_margin=0.0, manifest_path=None) -> dict[str, Any]: ...
```
From `scripts/materialize_training_data.py`:
```python
parser.add_argument("--kind", required=True, choices=["sft", "dpo"])
parser.add_argument("--mode", default=None, help="Selection or pair construction mode")
parser.add_argument("--score-column", default="score")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify explicit SFT selection mode behavior</name>
  <files>tests/test_training_selection_artifacts.py</files>
  <read_first>tests/test_training_selection_artifacts.py, src/training/selection.py, .planning/phases/05-training-objective-and-pipeline-comparability/05-RESEARCH.md</read_first>
  <behavior>
    - Test `threshold` still selects every row with `score >= threshold` and preserves existing artifact fields.
    - Test `top_k_per_prompt` selects the highest-scoring eligible rows per prompt with deterministic score-desc/version-asc ordering.
    - Test `score_weighted` selects rows above threshold and writes `sample_weight` equal to `selected_score / max_selected_score` rounded to 12 decimals.
    - Test `hard_positive` selects rows with `score >= threshold` only when the prompt also has at least one version below `hard_negative_threshold`, and writes that threshold in the summary.
  </behavior>
  <action>Add failing tests for SFT modes `threshold`, `top_k_per_prompt`, `score_weighted`, and `hard_positive`. Use tiny temporary CSV fixtures with columns `id,version,score,target_text`. Assert summary keys include `selection_mode`, `score_column`, `threshold`, `source_scores_sha256`, `selected_count`, and `filtering_stats`. Assert JSONL rows include `schema_version`, `sample_id`, `prompt_id`, `version`, `selected_score`, `selection_mode`, and mode-specific `sample_weight` for `score_weighted`.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_training_selection_artifacts.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_training_selection_artifacts.py` contains `score_weighted` and `hard_positive` assertions.
    - The targeted pytest command fails before implementation because `src.training.selection` does not yet accept at least one new mode.
  </acceptance_criteria>
  <done>SFT mode tests exist and fail for missing implementation before production code is changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement explicit SFT and DPO mode materialization</name>
  <files>src/training/selection.py, tests/test_training_selection_artifacts.py</files>
  <read_first>src/training/selection.py, tests/test_training_selection_artifacts.py, .planning/phases/03-data-curriculum-and-dataset-quality/03-04-SUMMARY.md</read_first>
  <behavior>
    - DPO `best_vs_worst` remains equivalent to the existing default behavior.
    - DPO `all_separated_pairs` emits every winner/loser row pair for the same prompt where winner score is at least threshold and `(winner-loser) >= margin`.
    - DPO `margin_weighted` emits best-vs-worst pairs and adds `pair_weight` equal to `margin / max_margin` rounded to 12 decimals.
    - DPO `ambiguity_filtered` rejects prompts whose best-vs-second-best margin is `<= ambiguity_margin` even when best-vs-worst margin passes.
  </behavior>
  <action>Update `materialize_sft_samples` to accept `mode in {"threshold", "top_k_per_prompt", "score_weighted", "hard_positive"}` and add a `hard_negative_threshold: float = 0.2` keyword. Update `materialize_dpo_pairs` to accept `mode in {"best_vs_worst", "all_separated_pairs", "margin_weighted", "ambiguity_filtered"}`. Keep strict winner-over-loser semantics: never emit equal-score pairs. Preserve deterministic ordering by `(prompt_id, winner_version, loser_version)` for DPO and `(prompt_id, version)` for SFT. Include mode-specific summary fields: `hard_negative_threshold`, `pair_count`, `pair_construction_mode`, `ambiguity_margin`, and `filtering_stats` counters for rejected/selected cases.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_training_selection_artifacts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/training/selection.py` contains `score_weighted`, `hard_positive`, `all_separated_pairs`, `margin_weighted`, and `ambiguity_filtered` in mode validation.
    - JSONL rows for weighted modes contain `sample_weight` or `pair_weight`.
    - Targeted pytest exits 0.
  </acceptance_criteria>
  <done>Selection helpers produce deterministic explicit artifacts for all required SFT and DPO modes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose mode flags through CLI and docs</name>
  <files>scripts/materialize_training_data.py, tests/test_training_selection_artifacts.py, docs/data_selection.md</files>
  <read_first>scripts/materialize_training_data.py, tests/test_training_selection_artifacts.py, docs/data_selection.md, docs/commands.md</read_first>
  <behavior>
    - CLI accepts `--hard-negative-threshold` for SFT hard-positive mode.
    - CLI materializes DPO `all_separated_pairs`, `margin_weighted`, and `ambiguity_filtered` with JSON stdout summaries.
    - Docs list every SFT and DPO mode with exact command examples and generated-artifact safety notes.
  </behavior>
  <action>Add argparse flag `--hard-negative-threshold` defaulting to `0.2` and pass it to `materialize_sft_samples`. Ensure `--mode` forwards the exact string without implicit aliases. Extend CLI tests to call `main([...])` for one new SFT mode and one new DPO mode, asserting stdout JSON has `selection_mode` or `pair_construction_mode`. Update `docs/data_selection.md` with sections `SFT selection modes` and `DPO pair-construction modes`, listing exact mode names and commands.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_training_selection_artifacts.py -q && PATH="/root/.local/bin:$PATH" uv run --extra lint ruff check src/training/selection.py scripts/materialize_training_data.py tests/test_training_selection_artifacts.py</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/materialize_training_data.py` contains `--hard-negative-threshold`.
    - `docs/data_selection.md` contains `score_weighted`, `hard_positive`, `all_separated_pairs`, `margin_weighted`, and `ambiguity_filtered`.
    - Targeted pytest and Ruff both exit 0.
  </acceptance_criteria>
  <done>Users can materialize and document all explicit SFT/DPO data modes from the command line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scores CSV → selection helpers | User-provided local score rows become training metadata artifacts. |
| CLI args → filesystem writes | User-provided output paths and thresholds control generated JSONL/manifest files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | T | `src/training/selection.py` CSV parsing | mitigate | Validate required columns, numeric `version`/score fields, strict winner-over-loser margins, and deterministic ordering in tests. |
| T-05-01-02 | R | selection summaries | mitigate | Write `source_scores_sha256`, mode, threshold/margin/weight fields, input counts, selected counts, and filtering stats into summary JSON. |
| T-05-01-03 | I | generated artifacts | mitigate | Docs state selection artifacts and score-derived outputs remain runtime artifacts under ignored roots unless intentionally tiny fixtures. |
</threat_model>

<verification>
Run `PATH="/root/.local/bin:$PATH" uv run pytest tests/test_training_selection_artifacts.py -q` and `PATH="/root/.local/bin:$PATH" uv run --extra lint ruff check src/training/selection.py scripts/materialize_training_data.py tests/test_training_selection_artifacts.py`.
</verification>

<success_criteria>
- All TRN-02 SFT modes and TRN-03 DPO modes are represented by exact mode names in code, CLI tests, and docs.
- Existing default `threshold` and `best_vs_worst` behavior remains covered by tests.
- No CUDA/model/OCR imports are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/05-training-objective-and-pipeline-comparability/05-01-SUMMARY.md`.
</output>
