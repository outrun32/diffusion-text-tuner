---
phase: 05-training-objective-and-pipeline-comparability
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-03"]
files_modified:
  - src/training/config.py
  - src/runtime/config_io.py
  - tests/test_runtime_config_io.py
  - tests/test_characterization_config_artifacts.py
  - configs/experiments/sft/README.md
  - configs/experiments/dpo/README.md
  - configs/experiments/masked_sft/README.md
autonomous: true
requirements: [TRN-02, TRN-03, TRN-04]
must_haves:
  truths:
    - "User can load configs that explicitly name SFT selection, DPO pair-construction, and masked-SFT loss/LoRA/dataset/evaluation choices."
    - "Config snapshots and run manifests expose these choices before training starts."
    - "Invalid explicit training choices fail through CPU-safe config validation."
  artifacts:
    - path: "src/training/config.py"
      provides: "Dataclass fields for explicit Phase 5 training choices"
      contains: "selection_mode"
    - path: "src/runtime/config_io.py"
      provides: "Pydantic validation for explicit training choices"
      contains: "pair_construction_mode"
    - path: "tests/test_runtime_config_io.py"
      provides: "CPU-safe config validation coverage"
      contains: "masked_lambda"
  key_links:
    - from: "src/runtime/config_io.py"
      to: "src/training/config.py"
      via: "validated models convert to dataclasses"
      pattern: "return SFTConfig"
---

<objective>
Wire explicit training objective/data choices into config validation and manifest snapshots.

Purpose: TRN-02, TRN-03, and TRN-04 require comparison-grade configs to show SFT, DPO, and masked-SFT choices directly.
Output: Dataclass fields, strict config validation, tests, and experiment config docs.
</objective>

<execution_context>
@/Users/udmurtpsycho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/udmurtpsycho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-training-objective-and-pipeline-comparability/05-RESEARCH.md
@.planning/phases/05-training-objective-and-pipeline-comparability/05-01-SUMMARY.md
@.planning/phases/05-training-objective-and-pipeline-comparability/05-03-SUMMARY.md
@src/training/config.py
@src/runtime/config_io.py
@tests/test_runtime_config_io.py
@tests/test_characterization_config_artifacts.py

<interfaces>
From `src/training/config.py`:
```python
@dataclass
class SFTConfig: ...
@dataclass
class DPOConfig: ...
@dataclass
class MaskedSFTConfig: ...
```
From `src/runtime/config_io.py`:
```python
def load_stage_config(stage: str, path: str | Path) -> SFTConfig | DPOConfig | MaskedSFTConfig: ...
def resolve_config_snapshot(config: SFTConfig | DPOConfig | MaskedSFTConfig) -> dict[str, Any]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify explicit config fields and validation</name>
  <files>tests/test_runtime_config_io.py, tests/test_characterization_config_artifacts.py</files>
  <read_first>tests/test_runtime_config_io.py, tests/test_characterization_config_artifacts.py, src/runtime/config_io.py, src/training/config.py</read_first>
  <behavior>
    - SFT config accepts `selection_mode`, `selected_samples_path`, `score_column`, `hard_negative_threshold`, and `sample_weighting`.
    - DPO config accepts `pair_construction_mode`, `preference_pairs_path`, `score_column`, `ambiguity_margin`, and `pair_weighting`.
    - Masked-SFT config snapshot exposes `masked_lambda`, `lora` ranks/targets, `data_dir`, `eval_suite_path`, `validation_interval`, and `eval_suite_n_per_step`.
    - Invalid modes are rejected with `RuntimeConfigError` and field context.
  </behavior>
  <action>Add failing tests to the existing runtime config and characterization config test files. Use committed config fixtures or `tmp_path` JSON configs. Assert `resolve_config_snapshot(load_stage_config(...))` includes exact keys and values: `selection_mode: "score_weighted"`, `selected_samples_path: "outputs/generated/selected_samples.jsonl"`, `pair_construction_mode: "margin_weighted"`, `preference_pairs_path: "outputs/generated/preference_pairs.jsonl"`, `masked_lambda: 0.65`, `eval_suite_path: "configs/eval_suite.json"`. Assert invalid mode strings fail without echoing raw secret-like values.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_runtime_config_io.py tests/test_characterization_config_artifacts.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Tests contain `selection_mode`, `pair_construction_mode`, and `preference_pairs_path` assertions.
    - Targeted pytest fails before implementation because config models reject or omit the new fields.
  </acceptance_criteria>
  <done>Explicit training config contracts are specified by failing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement config dataclass and validation fields</name>
  <files>src/training/config.py, src/runtime/config_io.py, tests/test_runtime_config_io.py, tests/test_characterization_config_artifacts.py</files>
  <read_first>src/training/config.py, src/runtime/config_io.py, tests/test_runtime_config_io.py, tests/test_characterization_config_artifacts.py, src/training/selection.py</read_first>
  <behavior>
    - Config dataclasses remain backward compatible with existing root configs by providing defaults matching current behavior.
    - Pydantic models forbid unknown fields and validate mode values exactly.
    - Path fields use existing `validate_path_policy` and allow only canonical relative roots.
  </behavior>
  <action>Update `SFTConfig` defaults with `selection_mode: str = "threshold"`, `selected_samples_path: str | None = None`, `score_column: str = "score"`, `hard_negative_threshold: float = 0.2`, and `sample_weighting: str = "uniform"`. Update `DPOConfig` defaults with `pair_construction_mode: str = "best_vs_worst"`, `preference_pairs_path: str | None = None`, `score_column: str = "score"`, `ambiguity_margin: float = 0.0`, and `pair_weighting: str = "uniform"`. Update `_SFTModel` and `_DPOModel` to validate exact mode literals from Plan 05-01 and add path validation for `selected_samples_path` and `preference_pairs_path`. Ensure `resolve_config_snapshot` includes new fields without mutating dataclasses.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_runtime_config_io.py tests/test_characterization_config_artifacts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/training/config.py` contains `selection_mode`, `selected_samples_path`, `pair_construction_mode`, and `preference_pairs_path`.
    - `src/runtime/config_io.py` validates `selected_samples_path` and `preference_pairs_path` through `_validate_path_fields`.
    - Targeted pytest exits 0.
  </acceptance_criteria>
  <done>Training configs and snapshots expose explicit SFT, DPO, and masked-SFT choices.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Document experiment config choice fields</name>
  <files>configs/experiments/sft/README.md, configs/experiments/dpo/README.md, configs/experiments/masked_sft/README.md, tests/test_runtime_config_io.py</files>
  <read_first>configs/experiments/sft/README.md, configs/experiments/dpo/README.md, configs/experiments/masked_sft/README.md, tests/test_runtime_config_io.py, docs/commands.md</read_first>
  <behavior>
    - SFT experiment docs list every selection field and mode.
    - DPO experiment docs list every pair-construction field and mode.
    - Masked-SFT experiment docs list loss, LoRA, dataset, and evaluation-suite fields required by TRN-04.
  </behavior>
  <action>Update the three experiment README files with exact field tables. SFT table must include `selection_mode`, `selected_samples_path`, `score_column`, `score_threshold`, `hard_negative_threshold`, `sample_weighting`. DPO table must include `pair_construction_mode`, `preference_pairs_path`, `score_column`, `score_threshold`, `score_diff_min`, `ambiguity_margin`, `pair_weighting`, `beta`. Masked-SFT table must include `masked_lambda`, `lora.attn_r`, `lora.joint_attn_r`, `data_dir`, `eval_suite_path`, `validation_interval`, `eval_suite_n_per_step`. Add a docs assertion to `tests/test_runtime_config_io.py` or a nearby docs test so these strings are guarded.</action>
  <verify>
    <automated>PATH="/root/.local/bin:$PATH" uv run pytest tests/test_runtime_config_io.py tests/test_characterization_config_artifacts.py -q && PATH="/root/.local/bin:$PATH" uv run --extra lint ruff check src/training/config.py src/runtime/config_io.py tests/test_runtime_config_io.py tests/test_characterization_config_artifacts.py</automated>
  </verify>
  <acceptance_criteria>
    - Each experiment README contains the exact fields listed in the action.
    - Targeted pytest and Ruff both exit 0.
  </acceptance_criteria>
  <done>Users can see and validate comparison-grade training choice fields before running trainers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed/runtime JSON configs → dataclasses | User-provided JSON controls training mode, paths, and objective choices. |
| config snapshots → manifests/comparison reports | Config values become provenance evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | T | `src/runtime/config_io.py` | mitigate | Validate exact mode literals and canonical relative paths; keep extra fields forbidden. |
| T-05-04-02 | I | config error messages | mitigate | Preserve secret-safe `RuntimeConfigError` formatting without echoing raw config values. |
| T-05-04-03 | R | config snapshots | mitigate | Ensure `resolve_config_snapshot` includes explicit choice fields for manifest provenance. |
</threat_model>

<verification>
Run targeted runtime config/characterization tests and Ruff on config modules/tests.
</verification>

<success_criteria>
- TRN-02/TRN-03 choices load through config validation and appear in snapshots.
- TRN-04 masked-SFT loss/LoRA/dataset/evaluation choices are explicit in docs and snapshots.
- Existing root configs remain loadable with backward-compatible defaults.
</success_criteria>

<output>
After completion, create `.planning/phases/05-training-objective-and-pipeline-comparability/05-04-SUMMARY.md`.
</output>
